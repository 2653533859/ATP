# 2026-08-11 修复状态与后续计划

## 2026-08-13 通用用例数据集版本固定

- 通用用例抽屉现在可以选择固定数据集版本；API、GraphQL、WebSocket、gRPC 和 iOS 用例的创建/编辑均会持久化 `dataset_version`。
- 切换或清空数据集会清理旧版本；未选择固定版本时沿用后端“使用最新版本”的默认行为。
- 本轮后端非集成 `2022 passed`，前端 `49 files / 205 tests passed`，type-check/build 通过。

## 2026-08-13 Web/Android 数据集绑定

- Web/Android 专用用例创建/编辑页面已补齐测试数据集选择、固定版本和参数化执行策略，编辑保存不会再静默丢失已有绑定。
- 共享数据集组件会按项目加载数据集和版本；清除数据集时同时清掉旧版本及参数化配置。
- 本轮前端全量 `49 files / 205 tests passed`，type-check/build 通过；真实 Worker 执行和大数据集压力仍需目标环境验收。

## 2026-08-13 发布前部署校验

- `validate-deployment-readiness.py` 增加严格模式：默认模式显式列出环境依赖 `SKIP`，`--strict` 会把缺少 Compose、Helm、`.env` 或 POSIX shell 转为失败；Makefile 支持 `ARGS=--strict`。
- 该改动只改善验收判定语义，不代表真实 Linux/Kubernetes 集群或灾备演练已经通过。
- 本次对 `172.31.27.133` 的只读 Linux MCP `ping_host`/系统概览仍为 `Transport closed`，未写入任何外部主机或性能验收通过结论。

## 2026-08-13 Web/Android 入口一致性

- 通用用例抽屉不再显示 Web/Android 的未实现占位入口；Web/Android 创建和编辑统一由专用抽屉承载，并已补静态回归。
- 本轮后端非集成回归 `2021 passed`，前端 `49 files / 203 tests passed`，type-check/build 和 Ruff 通过。

## 2026-08-13 Web/Android 用例编辑回归收口

- Web/Android 专用抽屉的低代码创建与编辑路径新增 4 条回归，覆盖步骤保存、Android 标准步骤生成以及关键配置回显/持久化。
- 修复 Web 编辑抽屉把已有浏览器配置强制改成 Chromium 的问题；Firefox/WebKit 用例再次编辑和保存时会保留原选择。
- 前端全量回归更新为 `49 files / 203 tests passed`，type-check 通过；这只验证配置链路，不替代真实 Linux Worker、Android 真机或外部通知验收。

## 2026-08-13 审计日志查询与证据导出收口

- 管理员审计日志支持项目、用户、动作和 ISO-8601 时间范围筛选；结束时间早于开始时间时返回 `422`。
- 新增受限 CSV 导出：页面最多 5000 条、服务端最多 10000 条，使用 UTF-8 BOM 并防护表格公式注入。
- 成功导出写入 JSON 编码的 `audit_log_export` 审计事件；动作筛选补齐 `audit_log_cleanup` / `audit_log_export`，便于追踪治理操作。
- 后端审计 API 定向 `4 passed`，完整非集成后端 `2018 passed`，覆盖率 `82.03%`（门禁 82%），`269` 个测试文件逐文件独立通过；前端全量 `47 files / 199 tests passed`，type-check、生产构建和 Ruff 通过；导出审批、归档和进一步脱敏仍需生产策略确认。

## 2026-08-13 发布前质量门禁复核

- Python 3.12.11 下后端完整回归、覆盖率门禁、逐文件隔离扫描、Ruff、格式检查和 mypy 均通过。
- Python 3.14.3 条件依赖环境下完整非集成后端回归同样为 `2018 passed`；该次未作为覆盖率门禁口径。
- Bandit 未发现中高风险；npm audit 未发现漏洞；pip-audit 使用锁定 requirements 的 `--disable-pip --no-deps` 模式未发现已知漏洞。
- 仓库 pre-commit 全量通过：YAML、文件格式、密钥扫描、后端 Ruff/mypy 和前端 Vitest 均通过。
- 部署配置校验通过；Docker Compose/Helm 因当前 Windows 未安装对应命令而按工具缺失跳过，不代表真实集群部署已验收。

## 2026-08-13 Linux/Kubernetes 性能栈完善（代码阶段完成）

- 修复手动、Webhook 和定时任务的性能节点 `max_concurrency` 校验在多 API/Beat 副本并发派发时的容量超卖竞态：节点选择使用数据库行锁，并保持到运行记录提交。
- 性能节点容量锁、队列路由、draining、心跳状态和幂等触发回归已通过；定向性能回归 `91 passed`，完整非集成后端 `1980 passed`，前端 `45 files / 189 tests passed`，Ruff/格式检查通过。
- 手动压测和 Webhook 支持显式 `idempotency_key`；同键同请求复用 Run，同键不同请求返回 `409`，数据库唯一约束和前端触发键已补齐。
- 真实 Linux/Kubernetes、Prometheus、TLS、目标服务和资源采样仍需目标环境证据。

## 2026-08-12 本轮开发收口

- 完成 Mock 条件匹配与多规则确定性优先级；数据集准备动作增加公网 URL/DNS 安全校验，显式拒绝非数组配置。
- 完成 MinIO 数据集元数据响应、存储表格行模型和项目导入导出存储模式支持；大数据集按 50MB 校验并在导入失败时清理已上传对象。
- 最终验证：非集成后端 `1967 passed`；前端 `45 files / 188 tests passed`；type-check、build、Ruff、格式检查和 `git diff --check` 通过。

## 2026-08-12 Mock 条件响应增强

- Mock 规则原有字符串精确匹配保持兼容，并新增 `$exists`、`$contains`、`$in` 三种安全操作符，Query、Header、Body 条件均可按同一语法分流响应。
- API 校验会拒绝未知操作符、多个操作符、复杂 `$in` 值和过大的条件集合；运行时对历史异常条件按不匹配处理，不执行用户提供的表达式或正则。
- 多规则冲突现在按 HTTP 方法精确匹配、路径静态段更具体、占位符更少、条件字段更多、规则 ID 倒序选择；规则变更会清理项目缓存，避免新增专用规则仍命中旧通用规则。
- Mock 页面已把条件类型放宽为 JSON 值，并增加操作符示例提示；后端创建/匹配回归 `34 passed`，Ruff 和 `git diff --check` 通过。
- 该项不需要数据库迁移；定向 Mock 后端 `34 passed`，完整非集成后端 `1967 passed`，前端 Mock 页面 `5 passed`、全量前端 `45 files / 188 tests passed`、type-check/build 通过。真实项目规则的多场景响应仍需在目标接口环境做冒烟验收。

## 2026-08-12 运行级数据准备 Hook

- API 用例关联数据集后，可通过 `config.dataset_prepare_actions` 在参数化执行前执行一次受限 seed 流程；支持变量动作、HTTP request、状态/响应断言和响应变量提取，不修改数据集持久化内容。
- 数据准备 request 已统一使用公网 URL/DNS 校验，拒绝解析到本机、内网、链路本地或保留地址的目标；动作配置必须是数组，非法结构会阻断运行。
- MinIO 数据集的元数据编辑会回读当前对象并保持响应 rows 完整；存储治理 UI 已将孤儿对象名称转换为可显示的表格行。
- Worker 只在准备成功后创建 child runs；共享变量进入每个 child run，行字段覆盖同名变量；失败会记录 parent 失败摘要并阻断后续迭代。
- 已补服务层、Worker 回归和前端配置入口；真实 seed 服务、MinIO 集群大数据量、对象生命周期仍是环境验收项。
- 修复 MinIO 数据集上传/预览仍采用数据库 500 行上限的问题，入口现在按当前存储模式校验。

## 2026-08-12 MinIO 数据集对象生命周期治理

- 新增管理员项目级对象核对接口：默认 dry-run，对比 `datasets/{project_id}/` 下的 MinIO 对象与 PostgreSQL 当前/版本引用；显式 `purge=true` 才执行孤儿对象清理。
- 清理结果包含扫描、引用、孤儿、删除、截断和逐项错误统计；删除严格限制在项目目录前缀，且每次操作写入审计日志。
- 更新/上传/回滚在已有当前对象时先写唯一对象，提交成功后才删除旧对象；提交或版本快照失败会清理本次新对象并保留旧引用。
- 项目导入导出快照现在携带 `database/minio` 存储方式；MinIO 大数据集按 50MB 对象上限校验，导入会写入目标项目对象前缀并在事务失败时清理已上传对象。
- 服务/API 定向回归 `33 passed`；真实 MinIO 集群备份恢复、权限、补偿和定期治理仍待目标环境验收。

## 2026-08-12 执行记录清理预览一致性

- 全局执行记录预览现在排除配置了项目级保留天数覆盖的项目，和实际清理的全局兜底范围一致。
- Plan、Suite、TestRun、MobileSpecialRun 均复用项目过滤查询，前端提示和用户手册已同步；定向回归 `18 passed`。
- 清理完成结果会在前端展开显示各项目的实际删除明细；前端全量回归 `45 files / 183 tests passed`，type-check/build 通过。

## 2026-08-12 性能 Run 通知闭环

- 性能 Worker 的测试缺失、执行器/节点校验失败、节点容量不足和启动前取消等提前终止分支，现统一在落库后发送项目级性能通知。
- 正常完成、threshold/baseline 回归、节点和资源异常继续使用同一摘要；邮件、企微和钉钉正文现在展示 RPS、P95/P99、错误率、阈值状态和触发原因。通知通道异常只保留 Worker 日志，不改变 Run 终态。
- 性能 Worker/通知定向回归 `14 passed`；真实 SMTP、企业微信、钉钉和发布环境通知仍需外部联调。

## 2026-08-12 Q18 发布就绪清单补强

- 发布清单已升级为 Q18 扩展版，加入 MinIO 数据集对象治理、项目级运行记录清理、性能通知正文/渠道和 Windows/Linux/Web/iOS 外部验收入口；覆盖率门禁已同步为当前 `82%`。
- 发布证据文件已增加本地门禁快照与外部证据待办；发布/灾备文档契约回归 `23 passed`。
- 真实 staging、备份恢复、SMTP/企微/钉钉、Android 设备、Linux/Kubernetes、专用 Web Worker 和 macOS/iOS 证据仍待目标环境，不以本地结果替代。

## 2026-08-12 产品化：大型测试数据集 MinIO 引用模式

- 测试数据集新增 `database` / `minio` 存储方式；MinIO 模式把当前行和版本行写入 `datasets/{project_id}/{dataset_id}/`，数据库仅保留对象引用与行数，单对象上限 50MB。
- 用例参数化、性能执行、AI 数据集上下文、项目导出和 Dataset Library CRUD/上传/回滚已通过统一存储 helper 读取；MinIO 读取或写入失败会明确返回错误。
- 后端数据集/存储/迁移定向回归 `45 passed`，前端 DatasetLibrary `8 passed`、type-check 通过；真实 MinIO 大数据量和对象生命周期清理仍待目标环境。
- 本轮完整非集成后端回归 `1944 passed`，前端全量 `45 files / 183 tests passed`，前端生产构建通过；本地结果不替代真实 MinIO 集群验收。
- 后端 `scripts/pytest-standalone-sweep.py --jobs 4` 已逐文件验证 `264 passed, 0 failed`，确认本轮新增数据集、Appium 和验收脚本没有依赖整套测试副作用。

## 2026-08-12 iOS/Appium 验收入口

- 新增 `scripts/ios-appium-acceptance.py`：status-only 只检查 Appium readiness；显式 `--session-smoke` 才创建和销毁 XCUITest 会话，并可执行受控步骤、截图、录屏和 syslog。
- 报告不写入 UDID、输入文本、完整 URL、日志或二进制，只保留步骤状态和附件 SHA-256；录屏在步骤执行前启动、结束后停止，会话最终销毁。
- iOS/Appium 脚本、Worker、路由、任务和租约相关回归共 `90 passed`；真实 macOS/Xcode/WDA/iPhone/Simulator 与 ATP `ios` 队列仍需环境验收。

## 2026-08-12 Web 专用 Worker 探针补强

- Web Recording Worker 成功注册或刷新 Redis 心跳后更新健康标记；停止或心跳中断时清理/停止更新标记。
- Docker Compose 和 Helm 的 Web Worker 已接入 readiness/liveness 检查，避免仅凭 Pod Running 状态承载录制会话；真实 Linux/Xvfb 与跨副本 E2E 仍待目标环境。
- 浏览器矩阵脚本现在可按需生成每个浏览器的 Trace/HAR、Console、失败请求和 HTTP 错误摘要，证据 URL 会去除查询参数；默认无 artifact 目录时仍保持轻量输出。
- Windows 本机 Chromium、Firefox、WebKit 均访问 `/login` 返回 HTTP 200、页面标题正确并检测到 4 个输入框，失败请求和错误响应均为空；脱敏汇总见 `docs/evidence/web-browser-matrix-local-smoke-2026-08-12.json`。

## 2026-08-12 Windows Web 录制状态冒烟预检

- `scripts/windows-local-smoke.ps1` 登录后会调用 `/api/v1/web-recordings/workers`：`local` 模式要求 API 本地录制就绪，`worker` 模式要求至少一个已注册且有空闲容量的录制 Worker。
- 冒烟报告只写入模式、注册数和可用数，不写 Worker 主机名、进程号或凭据；Worker 未注册/满载时会作为必需检查失败，避免 Web 录制不可用却误报冒烟通过。
- PowerShell 语法解析、Windows 脚本契约回归 `10 passed`；真实 worker 模式仍需在目标 Windows 运行档案执行并留存报告。

## 2026-08-12 Linux/Kubernetes Prometheus 验收入口补强

- `scripts/performance-environment-smoke.py` 现在支持 `--prometheus-url` 和 `--prometheus-query`，会在同一份验收报告中检查 readiness=200、PromQL `status=success` 及 `data.result`。
- Prometheus URL 拒绝用户名、密码、查询参数和片段；定向回归已通过，真实 Linux/Kubernetes 集群、生产 Prometheus 和外部目标仍需独立环境执行。

## 2026-08-12 下一阶段开发路线

已将后续开发统一同步到 [`docs/next-development-plan-2026-08-12.md`](next-development-plan-2026-08-12.md)。Windows ADB 验收入口已完成并等待真实设备；当前已继续推进 Linux/Kubernetes 性能栈的 Prometheus 验收入口，真实环境仍必须单独执行，不以本地队列或健康检查结果代替。

- P1：Windows Android 设备验收、真实低代码执行和设备日志/截图回传。
- P1：Linux/Kubernetes 性能节点、真实目标服务、Prometheus、取消和资源采样。
- P1：Linux/Xvfb Web 专用 Worker、Firefox/WebKit、Trace、网络日志和跨副本 E2E。
- P1：macOS/iOS/Appium 真实设备最小闭环。
- P2：大型数据集、项目级清理、通知渠道、关键流程 E2E 和发布证据。

## 2026-08-12 存储治理 UI 与 Android 顺序调整

- Android 真机连接按当前工作安排暂缓；Windows ADB 验收脚本、Worker 路径发现和脱敏证据保留，真实设备执行仍为 pending。
- 存储管理页现在可以选择项目并执行只读数据集对象核对，展示扫描、数据库引用、孤儿数量、对象明细、截断和错误信息；只有当前核对发现孤儿对象时，才可通过二次确认调用 `purge=true`。
- 新增前端 `storageApi.reconcileDatasetStorage` 类型契约、中文/英文文案和 `StorageManagementView` 回归；定向页面测试 `7 passed`，type-check 通过。
- 存储管理页进一步接入现有容量告警接口，显示正常/告警状态、占用 GB、阈值和触发时间，并支持单独刷新；告警读取失败不会阻断其他存储治理功能。页面回归更新为 `8 passed`，前端全量为 `45 files / 185 tests passed`。
- 仍需真实 MinIO 集群验收权限边界、备份恢复、删除失败补偿和大数据量对象治理；本地测试不替代外部环境结论。

## 2026-08-12 测试套件并行会话隔离

- 并行套件执行不再让多个子用例共享同一个 SQLAlchemy `AsyncSession`；真实 AsyncSession 场景按子用例创建独立会话，顺序执行保持不变。
- 套件配置与执行链定向回归 `43 passed`，Ruff 通过；该修复不依赖 Android 真机或 Linux 集群。

## 2026-08-12 API 登录态复用边界

- 并行套件创建/编辑现在会检查 API 用例的登录态策略；复用项目 Cookie 登录态的 API 用例必须放在串行套件中，否则后端返回明确的配置错误。
- SuiteList 已展示后端具体保存错误，避免用户只能看到笼统的“保存失败”；串行套件的 API 登录态复用能力保持不变。
- 后端套件校验 `20 passed`、SuiteList `6 passed`、type-check/Ruff 通过。

## 2026-08-12 Windows Android SDK/ADB 路径发现

- 三个 Windows 入口统一支持 `ATP_ADB_HOME`、`ANDROID_HOME`/`ANDROID_SDK_ROOT` 和用户级 Android SDK/ATP 工具目录，路径只对当前进程及子 Worker 生效，不修改系统 PATH。
- Android Worker 在加载启动档案后重新刷新 ADB 路径，独立网络诊断脚本也使用同一发现逻辑；相关合约回归 `12 passed`，PowerShell 解析通过。
- 当前主机仍没有在线 Android 设备，因此设备扫描、真实 Android 用例和日志回传保持 pending。

## 2026-08-12 Windows Prometheus 目标指标与界面闭环

- 性能中心新建/编辑压测定义现在可以配置目标服务 Prometheus：支持直连 URL 或环境变量、0.2-10 秒查询超时和多条 PromQL；保存时写入 `default_options.target_metrics`，关闭开关会移除旧配置，前端会拒绝非 HTTP(S) 地址。
- 压测详情的资源时间线增加指标来源和指标选择，可区分 Worker 资源样本与 `target-service-prometheus` 目标服务样本，并能展示未预置名称的 Prometheus 指标。
- Windows 本机已安装并校验 Prometheus v3.13.1，`scripts/windows-prometheus.ps1 doctor/up/down/restart/status/logs` 可用；Prometheus readiness、Backend `/metrics` scrape、带目标指标的 k6 run `11` 均已通过，证据见 `docs/evidence/performance-windows-local-prometheus-target-metrics-2026-08-12.json`。
- 前端定向回归 `5 passed`、全量回归 `44 files / 180 tests passed`，`vue-tsc --noEmit` 和生产构建通过。真实外部目标、Linux/Kubernetes Prometheus、生产 SLO 连续历史、通知和 Android 设备仍需独立环境验收；Linux MCP 当前仍为传输层关闭，未宣称远程结果。
- 完整非集成后端回归 `1899 passed`，259 个测试文件单独运行 `259 passed, 0 failed`；本轮未引入新的后端失败，外部验收缺口仍不以本地测试结果替代。
- 修复 Windows 冒烟脚本固定读取根 `.env` 导致运行档案错配的问题，新增 `-EnvFile` 并让 doctor、登录、服务启停和临时用例闭环使用同一档案；使用当前 `remote-infra.env` 重跑通过，10 项 Playwright、三浏览器矩阵、临时 Web 下载对象及清理均成功，脱敏证据见 `docs/evidence/windows-local-smoke-remote-infra-web-seed-2026-08-12.json`。
- 未传 `-EnvFile` 时，冒烟脚本会读取运行元数据自动选择当前档案；在同一运行态下的轻量回归已通过，确认不会再次回退到根 `.env`。

## Worker 测试外部依赖隔离修复（2026-08-12）

- 修复 `test_tasks_mobile_special_dispatch.py` 与 `test_tasks_performance.py` 对真实 Redis 取消控制客户端的外部依赖；Worker 全量 `427 passed`，完整非集成后端 `1889 passed`，覆盖率 `82.13%`。
- 远端 SSH/MCP 会话恢复失败，真实 k6、metrics 及后续外部协议验收继续保持 pending。

## 2026-08-12 Linux 性能验收栈部署与脚本修复

- Windows bundle 已完成可移植 ZIP 修复并部署到 `172.31.27.133:/opt/atp-q17-acceptance`；远端镜像构建成功，隔离 Compose 栈未复用主机现有 PostgreSQL/Redis/MinIO。
- 远端 Backend `/health`、数据库迁移、四类性能执行器、`worker-a` online、队列和 allowlist API/节点 smoke 已通过，报告为远端 `docs/evidence/performance-api-node-2026-08-12.json`。
- 真实 k6 run 首次发现验收脚本的报告 `Path` 序列化与 CSRF header 缺陷，已修复并补充回归；本地脚本测试 `17 passed`。真实 run、metrics 和其余协议验收仍待重跑，当前不能关闭 Q17-04。

## 2026-08-12 Windows 性能验收 bundle

- 新增 `scripts/package-performance-acceptance.ps1`，使用 allowlist 打包性能 Compose、Backend/Worker 构建上下文、TLS/Locust/JMeter 夹具、验收工具和 Runbook；明确排除真实 `.env`、启动档案、虚拟环境、测试缓存、前端依赖和 `.local-run`。
- 压缩包内生成 `bundle-manifest.json`，逐文件记录大小和 SHA-256；包旁生成 `.sha256` 校验文件，并记录源 commit 与 `worktree_dirty`。已有输出必须显式 `-Force` 覆盖。
- 本机实际生成 323 个文件的 bundle，ZIP SHA-256 与 sidecar 一致，未发现密钥/证书条目；该入口只完成可审计传输准备，不代表 Linux 性能栈已部署或 smoke 已通过。

## 2026-08-12 Web 录制浏览器选择

- Web 录制 API 与 `WebRecorderModal` 新增 Chromium、Firefox、WebKit 选择，启动请求会把选择传递给 Playwright Worker；默认仍为 Chromium，录制开始后锁定浏览器选择。
- 会话快照回传实际使用的浏览器，跨 Worker 查询、停止录制和错误排查都能保留该配置。
- 中英文界面已补充浏览器选择和安装提示；如果 Worker 镜像缺少所选浏览器，仍由 Worker 返回明确错误，不会静默回退到其他浏览器。
- Web 录制 API/传输层定向回归 `33 passed`；完整非集成后端 `1886 passed`、Python 3.12 覆盖率 `82.13%` 且门禁通过；前端全量 `44 files / 177 tests passed`，覆盖率 statements/branches/functions/lines 为 `36.25% / 31.37% / 28.58% / 37.83%`，`vue-tsc --noEmit` 和生产构建通过。

## 2026-08-12 Windows Worker 模式隔离

- 修复 `scripts/windows-local.ps1` 将带 `--hostname android-win-*` 或 `--hostname performance-win-*` 的专用 Android/性能 Worker 误识别为普通 Celery Worker 的问题；普通 Worker 进程匹配增加专用主机名排除规则。
- 增加双向队列冲突保护：本地 `CELERY_QUEUES` 包含 `android` 或 `mobile_special`，且专用 Android Worker 已运行时，`windows-local.ps1 up/restart` 会在停止现有本地服务前阻止启动；反向启动 `windows-android-worker.ps1 up/restart` 时，也会识别正在消费 Android 队列的普通 Worker 并阻止启动，避免 Android 任务被重复消费。
- `local-all` 用于 Backend/普通 Worker/Beat/Frontend 与本机 ADB 的完整栈；`android-agent` 用于远程 Backend/Redis/MinIO 加本机 ADB。两种模式不能在同一台 Windows 主机同时消费 Android 队列，相关说明已同步到启动档案、Windows 运行手册和 Celery 队列文档。
- 验证：Windows 合约测试 `5 passed`、两个 PowerShell 脚本语法检查通过；当前 `local-all` 普通 Worker 运行时尝试启动 Android Agent 会被 PID `28104` 冲突保护拒绝；停止专用 Android Worker 后恢复 `local-all`，Backend/Worker/Beat/Frontend 均运行，`/health` 返回 `ok`，`/api/v1/devices/workers` 已被运行中的 OpenAPI 暴露；当前 ADB 无设备，因此没有伪造 Android 执行结果。

## 2026-08-12 Linux 目标主机只读审计

- 配置的 Linux 主机具备 Docker/Compose，已有 PostgreSQL、Redis、MinIO 容器；但现有 `/opt/testhub_platform` 是独立 Django 项目，`8001` 端口未提供 ATP 的 `/health` 或 `/api/v1/health`，不能直接用于当前 ATP 验收。
- 未发现 ATP Q17 性能隔离栈或 Prometheus；本次仅读取系统、容器和 API 探针，没有停止、重启、部署或修改远端服务。Q17-04、真实性能节点和外部目标服务继续保持环境验收状态。

## 2026-08-12 性能验收启动竞态修复

- Compose 性能隔离栈为 Backend 增加 `/health` healthcheck，性能 Worker 和验收工具改为等待 Backend `service_healthy`，减少服务刚启动时的连接误报。
- `scripts/performance-environment-smoke.py` 增加节点心跳等待上限 `--node-ready-timeout-seconds`（默认 60 秒），在节点从 offline/未注册变为 online 后再检查队列、能力和 allowlist。
- 定向性能部署/验收回归 `27 passed`，Compose YAML 解析通过；此修复改善验收可靠性，但不替代真实 Linux/Kubernetes、TLS、外部目标服务和资源采样证据。

## 2026-08-12 Windows 部署 readiness 解码修复

- 修复 `scripts/validate-deployment-readiness.py` 在 Windows 系统代码页下读取 shell 输出时的 `UnicodeDecodeError`，并处理子进程 `stdout/stderr` 为空导致的二次 `TypeError`。
- 当前实际 readiness 命令已通过仓库文件、YAML/JSON、shell 语法和文档检查；Docker/Helm 未安装仅显示 SKIP。部署文档回归 `13 passed`，Ruff 通过。

## 2026-08-12 Web 录制 Worker 并发路由补强

- API 创建录制会话时仍以 Redis 心跳的活动会话数选择候选 Worker，但把 Worker 的最终容量检查作为权威结果：候选返回 `busy` 或 `not_ready` 时自动切换到其他有容量候选。
- 对超时和未知错误不做盲目重试，因为命令可能已被 Worker 接收而只是响应丢失；这样避免重试造成重复浏览器会话。
- 新增多 Worker busy→fallback、超时不重试和启动响应异常清理回归；录制传输层测试为 `13 passed`。Worker 在成功启动但返回无效快照时会主动停止远端会话，避免残留浏览器占用容量。真实 Linux 多副本、Xvfb、Firefox/WebKit 仍需目标环境验收。

## 2026-08-12 Web 录制停止失败保护

- `WebRecorderModal` 在停止接口失败时不再把停止前的步骤自动导入用例，也不再误关闭弹窗；错误状态会保留在当前录制窗口，用户可以明确看到失败原因。
- 新增组件回归覆盖 `autoApply` 失败路径；前端全量为 `44 files / 177 tests passed`，`vue-tsc --noEmit` 和生产构建通过。

## 2026-08-12 API gRPC 流式调用

- API gRPC 执行器已根据 Proto 方法描述支持 Unary、Server Streaming、Client Streaming 和 Bidi Streaming；客户端流式请求使用非空 JSON 数组，服务端流式响应进入统一 body、提取和断言契约。
- gRPC/HTTP 家族定向回归 `68 passed`；完整非集成后端回归更新为 `1886 passed`，Python 3.12 覆盖率 `82.13%`，前端 `44 files / 177 tests passed`，type-check/build 通过。
- 真实 Unary/Streaming 目标服务与 TLS 联调仍待外部环境验收。

## 2026-08-12 gRPC Proto 文件读取

- gRPC 用例编辑器新增主 Proto 与 import 文件选择入口，协议源码不会上传到对象存储；保存后由 Worker 在临时编译目录重建文件包。
- 浏览器端拒绝非 `.proto`、空文件、单文件超过 1MB 或文件包超过 8MB/64 个文件；后端 gRPC/HTTP 家族回归 `68 passed`，`grpcProtoFile` 前端回归 `5 passed`，前端全量为 `44 files / 177 tests passed`，type-check/build 通过。

## 2026-08-12 Windows 冒烟 CSRF/会话修复

- 修复 Windows 本地冒烟文件上传返回 403/401：所有状态变更请求统一携带 `X-Requested-With`，multipart 上传通过 `CookieContainer` 复用 HttpOnly 会话，不再手工拼接 Cookie。
- 静态合约回归 `5 passed`，PowerShell 解析通过；修复后真实 Windows 冒烟通过 10 项 Playwright、Chromium/Firefox/WebKit 页面矩阵、管理员登录、文件上传和 MinIO 临时对象清理。
- 最新脱敏报告：`.local-run/windows-smoke-20260812-003248.json`；Android、性能可选检查仍按当前机器资源跳过或 warning。
- 随后运行自包含 Web 下载 seed 也通过：临时项目 9、用例 5 审核/执行成功，1 个下载对象写入，终态清理删除项目、1 个环境和 5 个运行产物；报告为 `.local-run/windows-smoke-20260812-003448.json`。

## 2026-08-12 性能节点诊断展示

- 性能节点卡片现在会展示后端返回的 `last_error`，队列不一致等问题不再只显示“离线”，用户可以直接看到诊断原因。
- 前端新增节点诊断回归测试；本轮前端全量为 `42 files / 170 tests passed`，`vue-tsc --noEmit` 和生产构建通过。

## 本轮补充：归档项目配置只读

- 项目编辑接口改用 `require_project_writable_access(ProjectRole.owner)`；归档项目通过 API 修改名称、描述或 AI 绑定时返回 409，不能只依赖前端按钮限制。
- 项目列表同步禁用归档项目的“编辑”按钮，并保留恢复、查看、报告和导出入口。
- 回归：项目后端权限/路由 `36 passed`，项目列表前端 `4 passed`，`vue-tsc` 和生产构建通过。

## 本轮补充：Windows Web 下载验收夹具

- 前端新增 `public/atp-windows-download.html` 与 `atp-windows-smoke.txt`，用于在本机生成稳定的浏览器下载事件；Web 低代码用例可用 `goto` + `download` 步骤引用 `#atp-download-link`。
- Playwright 下载夹具回归 `1 passed`；该夹具只解决页面准备问题，真实 Worker、MinIO 和 `/runs/{id}` 对象证据已由 seed 冒烟验收。
- Windows 冒烟新增 `-SeedWebDownloadCase`：主动传入后会创建临时 Web 项目、模块和低代码用例，自动提交审核并批准为 `active/approved` 后执行网络守卫允许的内联下载夹具，再在运行终态后删除；超时/未知状态会保留项目并在报告中提示手工清理。首次无历史运行记录时可配合 `-SkipReports` 使用。
- 最近一次聚焦真实 seed 冒烟已通过：临时项目 8、用例 4、运行 4 完成创建/审批/Worker 执行，下载对象证据存在，终态清理删除 1 个临时环境和 5 个运行产物；报告为 `.local-run/windows-seed-smoke-latest.json`。

## 本轮新增：三方 OpenAI 兼容模型配置

- AI 模型配置新增独立的 `OpenAI 兼容（三方）` 协议，适配 Open WebUI、One-API、LiteLLM 等兼容 OpenAI `/v1` 的服务。
- 模型发现和实际调用均按兼容协议走 `/models` 与 `/chat/completions`；Endpoint 必填，API Key 不会回传或写入日志。
- 配置页已提供明确的 Endpoint 说明和模型拉取入口，避免将三方服务配置为原生 Ollama 导致 405；多模态与思考参数仍由服务商/模型能力决定。
- 定向验证：LLM 客户端、模型发现和配置 API `31 passed`；真实第三方服务 Token 与可用模型仍需外部联调。

## 本轮新增：数据集页面 AI 生成

- 数据集管理页现在可以直接生成测试数据：新建数据集或选择已有数据集，填写生成要求和行数后调用项目绑定的 AI 模型。
- 生成结果先回填到编辑器，不会自动写入数据库；用户确认后沿用原有 Schema 校验、软/硬校验策略和版本快照流程，避免 AI 输出未经审核覆盖数据。
- 后端新增 `/datasets/ai-generate`，对项目权限、AI 配置、返回 JSON 对象数组、200 行/256KB 限制和模型异常做统一处理；没有 Schema 时自动推断字段类型。
- 定向验证：后端数据集相关 `24 passed`，前端数据集页面 `7 passed`，`npm run type-check` 和 Ruff 通过；完整回归已在本轮改动稳定后复跑。

## 本轮新增：Mock 规则 AI 生成

- Mock 服务页面新增独立“AI 生成 Mock”按钮；原有“AI 生成用例”入口保持原行为，不再让用户误以为它会创建 Mock 响应。
- 新入口支持按自然语言要求生成，也可以把选中的现有 Mock 规则作为参考。生成结果先进入可编辑 JSON 预览，确认后复用普通创建接口保存。
- 后端新增 `/mock-rules/ai-generate`，校验项目编辑权限、项目 AI 配置和参考规则项目归属；服务层限制对象数组、规则数量和 256KB 结果大小，并规范化响应字段。
- 定向验证：后端 Mock AI 服务/API `9 passed`，前端 Mock 页面 `5 passed`，type-check 和 Ruff 通过；本轮完整门禁仍需在所有改动稳定后复跑。

## 本轮收口：Worker 测试隔离

- 根级测试配置在模块收集前和每个测试 setup 前补齐缺失的 MinIO、Redis、数据库和权限 stub 字段，保留历史测试显式设置的替身，不再依赖测试文件执行顺序。
- `backend/tests/worker` 当前全量验证为 `415 passed`；旧记录中的 8 个测试桩隔离失败已解决。全仓非集成回归为 `1867 passed`，256 个测试文件独立运行通过。

## 本轮补充：性能中心 JMeter 配置入口

- 性能中心前端执行器切换现在与后端能力列表一致，选择 JMeter 后自动进入脚本模式，并按执行器能力限制上传 `.jmx` 文件；此前虽然后端已支持 JMeter，前端切换处理会静默拒绝该值。
- 回归：性能中心组件新增 JMeter 切换测试；前端全量 `42 files / 169 tests passed`，`vue-tsc --noEmit` 和生产构建通过。

## 本轮新增：性能压测节点注册与配置

- 性能中心新增“注册节点”入口，支持维护节点 ID、名称、队列、启用状态、可执行器能力（k6/Locust/gRPC/JMeter）、最大 VU、最大并发和目标出口 allowlist。
- 已注册节点会在节点卡片中展示能力与约束，并支持编辑、删除；运行配置中的节点选择也会显示节点能力，减少把任务派发到不支持对应执行器的节点的误配。
- 节点注册/编辑/删除复用现有后端接口并受工程师权限保护；Worker 心跳、在线状态和实际容量校验仍由后端/Worker 负责，页面配置不会伪造节点在线状态。
- 回归：性能中心组件新增节点注册 payload、allowlist 去重和表单校验测试；前端全量 `42 files / 169 tests passed`，type-check/build 通过。

## 本轮补充：Windows 性能节点队列与配置同步（2026-08-12）

- 修复 Windows 本地 Worker 过滤 `performance.worker-a` 专用队列的问题；显式性能节点启动时会自动监听共享 `performance` 队列和 `PERFORMANCE_NODE_QUEUE`，保证心跳、定时任务和节点定向任务都能被消费。
- 页面注册节点现标记为 UI 管理，Worker 心跳只刷新在线状态；节点名称、队列、执行器能力、最大 VU、最大并发和出口 allowlist 不会再被 `.env` 默认值覆盖。纯环境变量自动创建的节点继续由 Worker 配置管理。
- 页面节点队列与 Worker 配置不一致时，节点保持离线并记录错误，避免任务进入无人消费的队列。
- 回归：后端相关定向测试 `82 passed`；完整非集成回归 `1871 passed`，Python 3.12 覆盖率 `82.08%`，门禁通过。

## 本轮新增：Web 录制 Worker 服务化

- 录制会话已支持 `local`/`worker` 两种模式：Windows 默认 local；worker 模式由 Redis 控制面选择 Worker、保存会话路由并刷新 TTL，浏览器对象只在独立 Worker 进程内存活。
- 已补齐独立 Worker 入口、Windows `local-dev.cmd` 进程托管、Compose profile、Helm Deployment、启动配置 UI 和部署/故障排查文档。
- 已补充 API 远程路由、Worker 生命周期/容量、Redis 回复队列和部署静态契约回归；Linux/Xvfb、真实 Firefox/WebKit、跨 API 副本 E2E 仍待目标环境验收，因此 P1 仍保持“部分实现/待验收”。

本文是 `docs/q18-latest-status-2026-08-07.md` 的后续校正记录；旧文档中的历史测试数量和
覆盖率只代表当时的提交，不作为当前质量门禁结论。

## 本轮新增：Windows Worker 启动前诊断

- `local-dev.cmd doctor` 已覆盖 Web 录制 `local`/`worker` 模式、Python Playwright、Chromium 可执行文件，以及 Worker 入口、Redis 队列前缀和正整数并发上限。
- `scripts/windows-android-worker.ps1 doctor` 已覆盖 Celery/Redis Python 依赖和 ADB；该历史记录中的 k6/Locust 告警已由当前安装和真实平台执行验收关闭，当前仅保留 Android 未连接警告。
- Windows 冒烟新增 `-WebCaseId`、`-SeedWebDownloadCase`、`-RequireWebLowcode`、`-RequireWebDownload` 和超时参数，可从已认证会话触发已有 Web 低代码用例，或按需创建临时下载用例后轮询运行结果，并强制检查 download 步骤是否产生对象引用；不传 ID/seed 开关时保持跳过。
- PowerShell 解析和 Windows 合约测试通过；这项本地诊断不替代真实 Android 设备、Linux/Xvfb、跨副本 Web Worker 验收。

## 本轮新增：Android Agent 扫描状态回传

- `ADB_SCAN_MODE=worker` 的设备扫描现在返回任务 ID，设备页轮询 Celery 状态并在 Windows Worker 将结果写回 PostgreSQL 后再提示完成；`local` 模式仍同步执行。
- Windows Android Worker 启动时会注入稳定的 `ANDROID_WORKER_ID`，通过 Redis TTL 心跳注册；新增 `/devices/workers` 状态接口和设备页在线提示，普通 Worker 未配置 ID 时不会注册为 Android Agent。
- Windows 全量冒烟已接入 Worker 注册和扫描回调：worker 模式或 `-RequireAndroid` 下强制验证在线 Agent，并轮询设备扫描 task ID；无真实设备时只验证状态链路。
- 已覆盖 queued/running/completed/failed、无效 task ID 和 Worker 任务写回失败；真实设备连接与 Android 执行数据面仍需外部验收。

## 本轮已修复

前述 Android 设备矩阵并行调度、运行详情展示、报告导出、浏览器网络层隔离、覆盖率补强和低代码 Python 脚本生成已完成；当前最新本地验证以本文件“当前验证结果”章节为准。

- 浏览器认证改为 HttpOnly access/refresh Cookie；前端不再把 JWT 写入 `localStorage`。
- WebSocket 浏览器连接不再把 JWT 放入 URL；服务端优先读取 Cookie，并暂时保留旧的查询参数
  回退以兼容外部非浏览器集成。
- 增加 `APP_AUTH_COOKIE_SECURE`、`APP_AUTH_COOKIE_SAMESITE`、`/auth/logout`，并保留 Bearer
  认证以支持 CLI/CI 客户端。
- 混合 Web/API/Android/iOS 套件由 `default` Worker 编排，设备子用例投递到 `android`/`ios`
  队列；纯设备套件仍进入对应专用队列，避免专用 Worker 父任务等待自身。
- 启动配置 UI 与 `.env.example` 对齐为 100 项，补充认证 Cookie、ADB 扫描方式、性能节点和
  混合套件子任务等待参数，并标明用途及可选/必填状态。
- Windows 冒烟脚本、性能验收脚本和 Q12 证据采集器改为兼容 Cookie 会话。
- APK、用例、套件、计划、iOS、通知、缺陷跟踪器、Mock、移动专项和统计列表统一按用户可见
  项目过滤；执行记录、报告导出、诊断、修复反馈、快照和移动专项详情增加资源级项目权限校验。
- Web 录制强制关联项目并要求编辑权限；当前镜像只支持 Chromium，因此 API 与 UI 移除无效的
  Firefox/WebKit 选项。录制 URL 和报告封面 URL 增加协议、凭据、显式 IP 与 DNS 解析地址校验，
  拒绝本机、私网、链路本地和保留地址。
- Web 低代码执行与录制器新增共享 Playwright `BrowserContext` 路由守卫：每次导航、重定向、
  WebSocket 和子资源请求均重新做公网/DNS 校验，录制器默认拒绝内网 egress；`about:`、`data:`
  和 `blob:` 等浏览器内部协议不发起网络连接。被拦截请求只保存脱敏 URL、资源类型和原因。
- 移动专项停止操作增加 Redis 取消信号，性能、稳定性和流畅度执行器会在运行中轮询并终止；
  流畅度执行器的 ADB 操作移出异步事件循环。
- Android/iOS 设备租约在检查和创建前锁定设备行，避免并发申请时重复占用同一设备。
- 分页参数增加下界校验，修复两处乱码错误消息，并修复仓库中已有的 Ruff 格式门禁问题。

## 当前验证结果（2026-08-12）

- 后端非集成测试：`1886 passed`，Python 3.12 覆盖率基线 `82.13%` 且门禁通过；256 个测试文件单独运行通过；新增网络守卫、Web 执行器、录制器、用户管理、设备扫描状态、Android Worker 注册、数据集/Mock AI 生成、三方模型协议和性能/租约/路由回归已纳入全量套件。
- 后端覆盖率：Python 3.12.11 基线为 `82.13%`；Python 3.14.3 的上一轮基线为 `82.05%`，两者均通过
  `--cov-fail-under=82`；Python 3.14 不再使用旧基线，已按本轮新增测试完成复验。
- 前端 Vitest：`44 files / 177 tests passed`；当前覆盖率为 statements `36.25%`、branches
  `31.37%`、functions `28.58%`、lines `37.83%`，达到 `31.5 / 26.5 / 24.5 / 32.5` 门禁。
- Ruff lint/格式检查、mypy、`npm run type-check`、`npm run build`：通过；当前 Windows 未安装 `make`，因此 Python 门禁使用等价的虚拟环境命令执行。
- 本文记录的是本次提交前的本地结果；Windows Web seed 已完成真实 Worker/MinIO 下载对象闭环，JMeter 本地 `/login` 烟测已完成，但真实 integration、物理设备、镜像扫描和部署验收尚未
  重跑，因此当前版本仍不满足发布条件。

## 后续开发计划

| 优先级 | 工作项 | 暂不在本轮直接修改的原因 | 完成标准 |
| --- | --- | --- | --- |
| P0（已完成） | 复验 Python 3.14 后端覆盖率 | 已完成 Python 3.14 隔离环境依赖准备，并补齐录制器、用户管理边界回归 | Python 3.12/3.14 后端均达到 82%；前端四项保持现有阈值；252 个测试文件单文件扫描通过 |
| P1 | Web 录制器服务化 | Redis 控制面、独立录制 Worker、Windows 托管和 Compose/Helm 入口已实现；真实 Linux/Xvfb、Firefox/WebKit 与跨副本 E2E 仍属于环境验收 | 独立录制 Worker/服务；共享会话路由或可靠粘性；Linux 显示服务；跨副本 E2E 通过 |
| P1 | 完整浏览器网络隔离 | 浏览器路由层已按每次请求重新校验，但独立录制 Worker 和部署级网络策略仍属于后续架构/环境工作 | 已拦截每次导航与子资源请求；录制器默认拒绝内网 egress；已覆盖请求级 DNS 变化回归；仍需专用 Worker 与网络层策略验收 |
| P1 | 修复 Worker 镜像 HIGH/CRITICAL 漏洞 | 当前 Trivy 命中 JMeter 5.6.3 附带 JAR 与 k6 二进制；升级需重建镜像并做协议兼容和压测冒烟 | 三个镜像 Trivy HIGH/CRITICAL 为零；JMeter、k6、Locust 冒烟和回滚验证通过 |
| P1 | 真实环境回归 | 本机没有可用 Docker/Kubernetes、远程基础设施和物理 Android/iOS 设备，无法伪造验收结果 | integration、E2E、Helm、迁移、设备取消/租约并发、Windows 与性能节点验收留证 |
| P2 | 强制分支门禁 | 私有仓库当前套餐无法配置 required status checks，本地钩子仍可被绕过 | 升级套餐或调整仓库可见性后，将 CI、安全与覆盖率检查设为 required |

执行顺序：覆盖率和解释器兼容性门禁已收口；下一步处理录制器隔离、镜像漏洞和真实环境验收。全部
P0/P1 验收通过后才能进入发布候选，门禁阈值保持不变。
## 2026-08-11 性能与启动收口验证

本轮完成 Windows 启动配置注入与 Android Worker 启动前诊断，并补充 JMeter 本地性能烟测；外部性能节点验收仍需目标环境。

- 前端 Vitest：`42 files / 169 tests passed`。
- 前端 `vue-tsc --noEmit`、`npm run type-check` 和生产构建：通过。
- 后端 Python 3.12：非集成 `1871 passed`，覆盖率 `82.08%`，`--cov-fail-under=82` 通过。
- 单文件扫描：`256 passed, 0 failed`。
- 后端 Ruff、format-check、mypy 和 `git diff --check`：通过。
- JMeter 5.6.3 已执行 `deploy/performance-acceptance/jmeter_smoke.jmx`，本地 `/login` 1 请求、0 错误，并生成 `.local-run/jmeter-smoke-20260811-233647/` 下的 JTL/HTML 报告。

因此，原待办中的“补前端行为测试并恢复前端覆盖率门禁”和“复验 Python 3.14 覆盖率”均已完成；真实设备/外部环境验收和镜像扫描仍保持为独立环境任务。

## 2026-08-11 Web/Android Python 脚本生成补齐

- `WebCaseDrawer` 打开时加载当前项目的元素资产和页面对象；Web 低代码生成器会展开 `element_asset_id`、`page_object_id`，并覆盖文件上传、下载等待/保存和视觉基线像素差异断言。
- 上传文件、下载目标和视觉基线通过 `ATP_WEB_UPLOAD_N`、`ATP_WEB_DOWNLOAD_N`、`ATP_VISUAL_BASELINE_N` 环境变量传入，生成脚本脱离平台后仍可执行；缺少定位器、未加载页面对象、空公共动作或未知动作会生成明确的 `pytest.fail`，避免静默漏步骤。
- 本轮前端定向生成器测试 `6 passed`；全量 `40 files / 161 tests passed`，coverage statements/branches/functions/lines 为 `31.94% / 27.09% / 24.94% / 33.21%`，`vue-tsc --noEmit` 和生产构建通过。
# 2026-08-12 权限与执行安全修复

- 用例、套件和计划手动执行会校验环境与资源属于同一项目，避免跨项目读取加密环境变量。
- 用例/套件/计划批量操作与脚本文件接口补齐项目角色校验；项目 editor/owner 可操作，viewer 保持只读。
- 全局变量列表按可见项目收口；密钥明文仅项目 editor/owner 或全局管理员可查看，全局变量修改与删除仅管理员可执行。
- 归档项目不会再被 Cron 或 Webhook 触发；现有资源写入/手动执行继续由统一项目断言阻断。
- Web 录制器支持 macOS 本地可见浏览器；Helm 多副本使用 Pod 名生成唯一 Worker ID。
- 项目工作台入口会把 `project_id` 传递并应用到环境、数据集、Mock、套件、计划、Web/API 资产和变量页面；用例创建菜单补齐 iOS。
- 清理误提交的根目录 Vitest 缓存，并忽略所有根级 `node_modules/`。
