# Hermes 目标环境项目角色与审计验收（2026-09-25）

## 范围

[运行前冻结计划](hermes-role-audit-plan-2026-09-25.md)已由提交 `87a2cda9` 推送，SHA-256 为 `61a1fdfb4ea7aad8f699be3e5737e49116a19cb4848f2920a5762a74076253aa`。在正式单节点 K3s Backend 使用现有管理员，以及全局 `tester`、仅在临时项目 A 授予项目 `viewer` 的现有验收账号。临时项目 A/B 分别写入一条相互矛盾的合成需求；A 绑定已启用模型配置 1，B 不给验收账号授权。密码仅在服务器受限文件内读取，仓库和报告不记录明文、Token、Cookie、供应商端点或回答正文。

本轮验证项目级 Viewer、管理员、跨项目和跨用户会话边界。没有创建新的长期账号，也未验证全局 Engineer 或项目 Editor 的完整行为，因此“全角色矩阵”仍未关闭。只读工具没有失败任务数据，实际状态为 `empty`；这只证明权限、调用与审计链，不证明失败任务证据内容。

## 结果

[结构化报告](hermes-role-audit-2026-09-25.json)记录了补充已知凭据回显检查后的同题复跑，事前九项检查 **9/9 通过**。前一轮也为 9/9，但缺少该显式检查，仅保存在发布机私有目录，不用于最终结论：

| 编号 | 结果 | 关键证据 |
| --- | --- | --- |
| P01 | 通过 | 管理员读取 A/B、Viewer 读取 A 均 200；项目成员角色为 `viewer` |
| P02 | 通过 | Viewer 对 A 的真实模型回答为 `llm_grounded`，只引用 A 的需求来源 `[S1]`，回答标记正确；自己的会话可读；已知登录凭据、会话 Token 与敏感字段均未回显 |
| P03 | 通过 | Viewer 的 `failed_tasks` 工具 HTTP 200、状态 `empty`；A 的 `hermes_read_tool` 审计 0→1 |
| P04 | 通过 | Viewer 在 A 新建模块和 Hermes 草稿均 403；A 的 `access_denied` 0→2 |
| P05 | 通过 | Viewer 读取 B 项目、Hermes 查询/编排/工具/会话均 403；B 的 `access_denied` 0→5 |
| P06 | 通过 | 管理员与 Viewer 跨用户提交反馈均 404；Viewer 会话列表只含自己的会话 |
| P07 | 通过 | Viewer 对自己的回答同值反馈两次均 200，`helpful` 只计 1 次 |
| P08 | 通过 | 移除 A 成员后 Viewer 查询 A 返回 403，A 的拒绝审计 2→3；管理员仍可读取 A |
| P09 | 通过 | A/B 删除 204，项目和两条需求回查 404；项目 77 仍为 200，模型绑定前后均为空 |

最终复跑的临时项目为 A=104、B=105。清理后通过管理员 API 复核：A 的 `access_denied` 3 条、`hermes_read_tool` 1 条，B 的 `access_denied` 5 条，审计按策略留存；两个项目的 Hermes 会话均为 0。正式单节点 Helm revision 55 保持 `deployed`，30 秒四次采样均为 6/6 Pod Ready、零重启、Backend `/health` 200。本轮没有对项目 77 执行写操作，其模型绑定前后均为空。

## 后续边界

本轮把现有验收账号作为项目 Viewer，与管理员做了目标环境隔离检查；还需独立验证项目 Editor、全局 Engineer 的完整写权限矩阵。供应商侧调用/账单、真实业务需求与知识的代表性回答、长期 SLO 和 P4/P9 发布门禁仍有各自的验收出口，不能由这九项权限检查推断通过。
