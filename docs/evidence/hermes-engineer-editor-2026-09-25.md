# Hermes Engineer 与项目 Editor 目标环境验收（2026-09-25）

## 范围与冻结判据

[事前计划](hermes-engineer-editor-plan-2026-09-25.md)在执行前完成接口契约修正并以提交 `88890ce7` 推送，SHA-256 为 `aa905529b97380e39a61595cda5ad0db13cef966e46faf08f3eba14fd41298ae`。通过正式管理员 API 建立两个临时项目 A/B，仅在 A 给临时全局 Engineer 账号项目 `editor` 权限；A 绑定现有已启用模型配置，B 无该账号成员关系。随机密码只在验收进程内使用，没有写盘或写入[结构化报告](hermes-engineer-editor-2026-09-25.json)。现存 Engineer 与定时 canary 账号均未修改。

首次准备运行时，夹具需求的 `source` 长度超过接口 32 字符上限，九项业务检查尚未开始；清理路径已删除项目 106/107 并停用新建账号 22。缩短夹具字段后复用该停用账号执行同一冻结计划，没有产生第二个账号。首次失败的私有报告留在发布机 `/opt/atp-hermes-engineer-editor-20260925/`；本文件的结论只来自修正后的完整复跑。

## 十项结果

复跑在临时项目 A=108、B=109 上 **10/10 通过**：

| 编号 | 结果 | 直接证据 |
| --- | --- | --- |
| E01 | 通过 | 临时账号全局角色为 `engineer`、A 项目角色为 `editor`，项目读取 200 |
| E02 | 通过 | A 的真实模型回答为 `llm_grounded`，只引用 A 的需求 `[S1]`，标记正确，已知密码和 Token 未回显 |
| E03 | 通过 | Engineer 创建并确认自己的 Hermes 草稿均 200；产生的计划 8 属于 A，是禁用的手工 `draft`，没有运行时间 |
| E04 | 通过 | Engineer 在 A 增加成员和删除项目均 403；A 的拒绝审计 0→2 |
| E05 | 通过 | Engineer 读取 B、查询/编排/工具及草稿五项均 403；B 的拒绝审计 0→5 |
| E06 | 通过 | 管理员确认 Engineer 的待处理草稿返回 404，无法跨用户操作会话 |
| E07 | 通过 | A 项目角色降为 Viewer 后仍能读取（200），新增草稿被拒（403）；A 拒绝审计 2→3 |
| E08 | 通过 | 移除成员后查询 A 返回 403，管理员仍能读取；A 拒绝审计 3→4 |
| E09 | 通过 | 停用临时账号后登录 403、旧 Token 读取 401；管理员回查 `is_active=false` |
| E10 | 通过 | A/B、需求 33/34 和计划 8 回查 404，两个项目会话归零；项目 77 仍可读且模型绑定未变 |

清理后，项目 106～109 均回查 404，账号 22 保持 `engineer` 角色但已停用，不能登录或使用旧 Token。A 的 `access_denied` 审计留存 4 条，B 留存 5 条。用户管理 API 没有删除账号的正式入口，因此保留该**停用审计身份**；这不是零残留用户的清理结论。

## 部署状态与剩余事项

正式单节点 Helm revision 55 保持 `deployed`。验收后 30 秒四次采样均为 **6/6 Pod Ready、Backend `/health` 200**，Backend 自身重启数为 0；集群总重启数为 **1**。重启发生在本轮验收开始前：Flower Pod 于 2026-09-25 05:24:52 UTC 因 `OOMKilled` 退出并恢复，内存上限 512Mi，检查时使用 158Mi。这次 Hermes 权限验收不能用于宣称发布环境“零重启”或关闭 C3.4 稳定性门禁；Flower 内存增长需单独排查。

结合[前一轮 Viewer/管理员验收](hermes-role-audit-2026-09-25.md)，目标单节点的 Hermes 项目 Viewer、Editor 与管理员关键边界已有受控 API 证据。供应商侧调用/账单、完整真实业务内容、长期 SLO、跨节点故障域和 P4/P9 仍需各自验收。
