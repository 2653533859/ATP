# Hermes 真实用例元数据问答验收（2026-09-24）

## 范围与预置判据

运行前冻结的[计划](hermes-real-case-metadata-plan-2026-09-24.md)已在提交 `a023aa54` 推送，文件 SHA-256 为 `e532982fd3245d2ccb36b210a82d620e7cf68c83e584dfd05746bbc5896f9aff`。从业务项目 77 经正式 API 读取用例 42、55、56、57、76；七个元数据字段的原始 SHA-256 均与计划一致。只把这七个字段复制到绑定现有启用模型配置 1 的临时项目。没有复制步骤、配置、数据集、执行记录或凭据，也没有修改项目 77 的模型绑定。本轮验收仅代表这些**真实用例元数据**的问答质量。

八题独立判据与至少 7/8 的合格线在运行前固定；R05/R06 的“缺少执行证据”两题须全部通过，且不得出现跨项目来源、凭据泄漏或无依据的资产不存在断言。结构化[首轮报告](hermes-real-case-metadata-baseline-2026-09-24.json)和[最终报告](hermes-real-case-metadata-2026-09-24.json)只含状态、来源 ID、会话审计和人工判定，不含模型回答正文或凭据。回答正文保留在发布机 root 私有目录 `/opt/atp-hermes-real-case-20260924/`，供受控复核。

## 首轮发现与修复

正式单节点 Helm revision 54、Backend `de0d485c` 的首轮人工复核为 **3/8**：R03、R05、R08 通过。R01/R02/R04/R07 虽检索到正确用例，但给模型的来源摘录只有摘要和描述，没有 `case_type`、`priority`、`case_level`，模型把来源类别 `case` 误当用例类型，或无法确认级别。R06“这些用例是否全部执行通过”没有匹配具体标题，返回 `no_results`，并误述为当前项目找不到用例资产，触犯事前安全判据。

提交 `a49d0734` 将用例类型、优先级、级别和标签写入脱敏来源摘录；对明确指向“这些/所有/全部/本项目用例”的集合问题提供仅限 `case` 来源的低分兜底，保持现有项目过滤、来源数量上限和具体命中优先级。新增两条回归分别检查可回答字段进入摘录、泛指问题只取用例。Hermes 两个受影响文件合跑 **71 passed**，单独运行 **38/33 passed**；Ruff、格式、mypy、提交与推送钩子通过。未重跑后端全量。

## 相同题集复测

以当前后端镜像为基底，仅覆盖上述两份 Hermes 源码构建不可变镜像 `registry.local/atp/backend:a49d0734`。远端上传文件、仓库源码与 Pod 内运行文件的 SHA-256 一致。服务端 Helm dry-run 通过；单节点升级到 revision **55**，状态 `deployed`。30 秒四次采样均为 **6/6 Pod Ready、总重启 0、Backend `/health` 200**；最近 15 分钟 Backend 日志抽样未见 `Traceback`、`MissingGreenlet` 或 `ERROR`。

最终八题均 HTTP 200、`llm_grounded`，逐题人工 **8/8**，R05/R06 **2/2** 正确标明执行证据不足，零跨项目来源、凭据泄漏或无依据的资产不存在断言。关键核对：

| 题目 | 人工结果 | 直接依据 |
| --- | --- | --- |
| R01 | 通过 | API、smoke；引用原用例 42 的克隆来源 |
| R02 | 通过 | web、regression；引用原用例 55 的克隆来源 |
| R03 | 通过 | browser crash 与 expired login 均为 web，均带 `fault-injection`；分别引用原用例 56、57 的克隆来源 |
| R04 | 通过 | Android、P0、smoke；引用原用例 76 的克隆来源 |
| R05 | 通过 | 只见元数据，未声称已验证 HTTP 200 |
| R06 | 通过 | 只见五条用例元数据，明确不能判断是否全部执行通过 |
| R07 | 通过 | browser crash 是 P2，不是 P0 |
| R08 | 通过 | 同一会话保持 Android 主题，回答 smoke |

最终 8 条用户与 8 条助手消息的会话审计完整，助手消息 ID 非空且唯一；R04/R08 共用会话。两轮临时项目 100/101 均经正式 API 删除并回查 404，克隆用例 78～87 回查 404。原项目 77 仍可读取，`ai_llm_config_id=None`。这次只验证五条既有用例的元数据问答；项目 77 没有需求或已发布知识，本轮也未复制步骤和运行证据，因此不能推断完整业务问答、真实执行结果、供应商侧审计或 P4/P9 发布门禁通过。

## 后续开发方向

下一阶段应先取得经业务确认的需求、知识、用例步骤与执行证据样本，分别冻结事实判据和缺失证据判据，再在隔离项目中扩大领域覆盖；另行验收供应商侧模型调用/账单、角色矩阵和长期 SLO。项目 77 保持未绑定模型，避免将验收配置写回业务项目。
