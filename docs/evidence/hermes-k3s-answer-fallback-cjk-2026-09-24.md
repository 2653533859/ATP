# Hermes K3s 回答、异常回退与中文检索修复（2026-09-24）

## 范围

目标为 `192.168.3.196` 的单节点 K3s `atp-single-node`。本次所有业务资产均通过正式 API 建在临时项目中；实际模型回答只使用原有已启用配置 1。受控异常使用一份另建的临时模型配置和仅监听本机回环的响应桩。密码、Cookie、API Key、供应商 Endpoint、原始响应和真实模型回答正文均未写入本记录。

## 发现与修复

- 原 Backend revision 47 上，包含“登录锁定策略”的长问句对同名临时需求返回 HTTP 200、`no_results`、来源 0；该项目随后删除。固定短问句在另一临时项目中返回 `llm_grounded`、一条需求来源及有效 `[S1]`。对回答正文做了限于该合成需求的人工核对：正确说明连续失败五次、锁定十五分钟，并指出解锁通知方式未定义。该项目随后删除。
- 原因是检索词提取把长中文连续文本最多按 20 字一块保留；整块文本与需求标题/正文不完全相同，相关来源被漏掉。
- 提交 `9f161362` 在原有词匹配得分为 0 时，追加最多 64 个不同的四字中文片段做有界兜底匹配，标题优先于标签和正文；新增长问句命中及无关问题仍不命中的回归。
- 定向回归 `32 passed`，受影响测试文件单独运行 `26 passed`；非集成后端全量 `2664 passed / 2 skipped`，Ruff、格式、mypy、提交与推送钩子通过。首次全量命令误收集 integration 目录而在收集阶段拒绝，按 `Makefile` 的 `--ignore=backend/tests/integration` 重跑通过。

## K3s 受控回退

在临时项目中创建一条合成登录需求，使用临时兼容模型配置：

| 场景 | 实际结果 |
| --- | --- |
| 本机桩返回有正文但无 `[S#]` 引用 | 查询 HTTP 200，来源 1，模式 `project_retrieval`，返回可追溯检索摘要 |
| 模型连接失败 | 查询 HTTP 200，来源 1，模式 `project_retrieval` |
| 模型连接失败时尝试候选规划 | HTTP 200，状态 `no_match`，规划 `deterministic_fallback` / `failed`，原因 `model_call_failed`，记录一次调用 |

临时项目和临时模型配置通过正式 API 删除，删除后均返回 404；模型配置 1 保持启用，原项目 77 的模型绑定仍为空。连接失败为本机拒绝连接的受控故障，未覆盖 60 秒超时、每日限额或真实供应商故障。

收尾只读数据库复核：本次临时项目关联的项目、需求、Hermes 会话和项目成员记录均为 0，临时模型配置记录为 0。

## 部署后复核

- 在旧镜像 `registry.local/atp/backend:71b0b0b3` 上只叠加修复后的 `knowledge.py`，构建镜像 `registry.local/atp/backend:9f161362`，Docker/OCI 镜像摘要为 `sha256:975e3d17d47b158f1a24412e21fd98d61ff4455e22067f64071052b9d256664b`。本地、上传文件及新 Pod 内源码 SHA-256 均为 `5748136ec5dd8727aa18f655d58f5588df6140f36860dfd50abf7ae2791fbfe3`。
- Helm 服务端预演通过；仅设置 Backend 镜像标签升级至 revision 48，状态 `deployed`。升级后以同样的长问句和合成需求再试：HTTP 200、`llm_grounded`、一条需求来源、得分 8、命中词“登录锁定”、有效 `[S1]`；回答包含“五次”“十五分钟”和通知方式缺口。无关固定缺失资产仍为 `no_results`、来源 0。临时项目已删除。
- 30 秒内四次采样均为六个 Pod `1/1 Running`、Backend `/health` 200；新 Backend Pod 重启数 0，其余 Pod 无新增重启。最近十分钟、最多一千行 Backend 日志中 `Traceback`、`MissingGreenlet` 及 Hermes 工具审计持久化失败标记为 0。

## 证据边界

真实模型回答核对仅覆盖一条合成需求；无效引用和连接失败使用受控桩/故障，不是供应商侧故障演练。K3s 固定十题所需的编号 1 运行、需求和知识仍未齐备，不能把这次结果称为 K3s 十题质量通过。供应商调用审计与账单、超时/限额、代表性 7/14 天 SLO、独立 MinIO 和多节点故障域仍需单独验收。
