# 产品导航与能力扩展开发计划（2026-08-24）

> 2026-08-25 当前跟踪版本已升级为开发计划 2.2.0：五组导航边界、P0-P5 顺序、最小验收出口和交付门禁统一以 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.2.0 为准。本路线图保留历史方案与实施记录；N2 Karing 单设备专项/事件/报告闭环已完成，当前执行顺序转为 P0/N4 真实性能环境 → P1/N5～P4/N8 外部依赖复核 → P5/N9 发布收口。

> 2026-08-25 当前开发计划已升级为 2.3.0：参考导航的五组职责现在作为模块跟踪边界，执行顺序为 N0 导航壳与 N1 工作台 → N2-API、N2-APP、N3-UI、N4 性能、N5 AI、N6 测试资产 → N7 智能中枢 → N8 系统治理 → N9 发布收口。N4 真实性能环境仍因缺少 Kubernetes、发布级 Prometheus 和独立 MinIO 保持阻塞；Windows 是默认开发/验收环境，Android 真机由本地 Windows Worker 执行，不作为 Web/API 的前置条件。详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.0。

> 2026-08-25 当前开发计划已升级为 2.4.0，最新检查点为 2.4.5：q19 已完成 N6/N7 管理员基础资产、来源和清理链路，P8 本地治理验收入口已补齐；下一项仍是普通 viewer 角色矩阵，随后复核受控真实 AI 和 P8 目标部署。P4 因 Kubernetes、发布级 Prometheus 和独立 MinIO 缺失保持阻塞。Windows/Android Worker 边界、真实环境证据和模块级审查规则不变，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.4.0～2.4.5。

> 2026-08-25 P7 Hermes 项目级检索本地交付已完成：新增项目 viewer 权限保护的需求/知识/用例检索和来源深链，Hermes 自由提问可进入检索链路，知识中心支持 `knowledge_id` 深链；N7 验收脚本覆盖临时数据、详情读取、三类引用、可选 AI 草稿、viewer 隔离和清理。后端定向 `21 passed`、前端 Hermes/知识定向 `11 passed`；真实模型、角色账号和远端数据仍待受控环境验收，详见 [`n7-intelligence-acceptance.md`](n7-intelligence-acceptance.md)。

> 2026-08-25 P7/N6 验收缺陷已修复：blank 项目无模块时脚本自动创建临时模块；需求创建接口在 async commit 后序列化过期 ORM 属性导致的 `MissingGreenlet`/HTTP 500 已通过提交后 refresh 修复。q19 旧部署的失败证据已脱敏保存，后续已按 `716d1b3` 重建到迁移 `20260825_0066` 并完成基础链路复验。

> 2026-08-25 N6 q19 基础资产链路已复验：`--execute` 下用例评审、套件、计划、终态执行、内部缺陷关联和临时项目清理均通过；首次项目删除因 `plan_runs` 外键无级联返回 500，已由 `20260825_0066` 补齐模块/用例/运行/套件/计划历史级联，最新证据见 [`evidence/n6-project-asset-acceptance-2026-08-25.json`](evidence/n6-project-asset-acceptance-2026-08-25.json)。普通 viewer 角色矩阵仍未验证，N6 不关闭为完整通过。

> 2026-08-25 N7 q19 基础数据链路已复验：临时需求、知识条目和用例创建，需求解析、详情读取、Hermes 三类来源引用及清理均通过；最新证据见 [`evidence/n7-intelligence-acceptance-2026-08-25.json`](evidence/n7-intelligence-acceptance-2026-08-25.json)。普通 viewer 和真实 AI 草稿未启用，N7 仍保持环境待验收。

> 2026-08-25 N7 Hermes 跨任务失败诊断已完成本地交付：工作台统一为 suite/plan/android/performance 提供按项目权限保护的规则诊断，case 复用既有 LLM/规则链；Android 与性能诊断引用各自持久化的异常事件、错误事件和指标线索，Hermes 保留任务详情来源，不再把非 case 任务标记为不支持。后端定向 `13 passed`、非集成全量 `2345 passed`，前端全量 `69 files / 293 tests passed`，真实模型、需求/知识数据和角色矩阵仍待验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.3。

> 2026-08-25 N6 缺陷状态刷新项目隔离已补齐：刷新 case 执行记录关联缺陷状态前校验所属项目 viewer 权限，已保存 tracker 必须属于同一项目，跨项目配置在外部调用前拒绝；缺陷跟踪 API 定向 `11 passed`，真实项目角色矩阵、外部缺陷平台和可清理失败运行仍待环境验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.4。

> 2026-08-25 N6 缺陷证据跨类型报告导航已补齐：case/Android/性能/suite/plan 缺陷证据均跳转到对应报告入口，suite/plan 深链通过 `run_id` 解析资源并恢复 `project_id`；前端定向 `23 passed`、全量 `69 files / 297 tests passed`，真实角色和报告环境仍待验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.5。

> 2026-08-25 N6 计划报告套件明细导航已补齐：计划运行报告中的套件明细可携带 `project_id`/`suite_run_id` 跳转到套件执行记录并展开对应运行，缺失或非法运行 ID 不触发导航；计划列表定向 `7 passed`、前端全量 `69 files / 298 tests passed`，真实角色和报告环境仍待验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.6。

> 2026-08-25 N6 数据集影响范围项目上下文已补齐：测试数据集影响范围的案例/套件/计划入口保留 `project_id`，案例详情可加载项目环境并返回项目筛选；数据集定向 `9 passed`、前端全量 `69 files / 299 tests passed`，真实角色、跨项目可见性和数据清理仍待验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.7。

> 2026-08-25 N6 用例评审打开详情项目上下文已补齐：评审工作台打开案例详情时携带记录自身 `project_id`，保留项目环境和返回筛选；定向 `3 passed`、前端全量 `69 files / 300 tests passed`，真实角色、跨项目可见性和评审数据清理仍待验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.8。

> 2026-08-25 N6 工作台任务详情上下文已补齐：case、suite、plan、Android、performance 任务详情统一保留 `project_id`，suite/plan/performance 额外携带 `run_id`，任务中心可直接定位具体执行记录；工作台 API 定向 `14 passed`、后端非集成全量 `2352 passed`，数据集静态契约旧路由断言修复后相关回归 `17 passed`。真实角色、跨项目可见性、可清理运行和报告环境仍待验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.9。

> 2026-08-25 N6 项目资产与角色矩阵验收工具已补齐：新增临时项目全链路验收脚本，覆盖用例评审、套件/计划、可选执行/报告、缺陷关联、viewer 读写边界和删除后 404；凭据只读环境变量，`--allow-mutations`/`--execute` 显式授权，HTTP 错误和证据均脱敏。脚本定向 `6 passed`、质量门禁一致性 `10 passed`，真实角色、执行报告和可清理数据仍待验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.10。

> 2026-08-25 N5 AI 模型能力元数据解析已完成本地交付：模型发现识别 `capabilities`/`modalities` 等第三方字段中的多模态和思考阳性标记；能力无阳性证据时保持未知，缺字段时兼容模型名提示。模型发现/API 定向 `20 passed`、受影响 AI 定向 `69 passed`、后端非集成全量 `2340 passed`，真实供应商参数接受和项目级生成仍待环境验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3.1。

> 2026-08-25 N5 真实模型环境只读复核确认阻塞：q19 acceptance 无 AI LLM 配置，外部模型入口不带凭据返回 HTTP `401`；未修改远端、未记录凭据或响应正文。真实模型列表、连接、思考/多模态参数、可编辑生成和清理仍待受控配置，脱敏证据见 [`evidence/ai-model-environment-audit-2026-08-25.json`](evidence/ai-model-environment-audit-2026-08-25.json)。

> 2026-08-25 计划跟踪已同步为 2.2.0：后续顺序固定为 P0/N4 真实性能环境 → P1/N5 真实模型 → P2/N6 测试资产 → P3/N7 智能中枢 → P4/N8 系统治理 → P5/N9 发布收口；N4 缺少 Kubernetes、Prometheus 和独立 MinIO 继续阻塞，N5-N8 本地完成但待真实环境验收。每个模块仍须经过测试、独立审查、修复、文档/记忆同步和提交推送，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.2.0。

> 2026-08-25 N4 本地门禁补强已完成：性能 smoke 新增 `--require-kubernetes`，发布级 Kubernetes 验收必须显式提供 Deployment；定向相关回归 `46 passed`。172.31.27.133 的 q19 Prometheus 仅作为 Compose 观察，真实多节点、发布级指标覆盖和独立 MinIO 灾备仍保持待环境验收。

> 2026-08-25 N4 MinIO 灾备门禁补强已完成：source/target 端点会拒绝同机文本、回环别名和同 IP 解析结果；相关定向回归 `51 passed`。真实不同主机 MinIO、生命周期和恢复回读仍待环境验收。

> 2026-08-25 N5 本地 AI 密钥边界已完成：Ollama 可不填写 API Key，所有 AI 生成/诊断/自愈入口保持一致；非 Ollama 空密钥继续拒绝，相关定向 `88 passed`、后端非集成全量 `2315 passed`。真实模型和项目数据验收仍保持 `[E]`，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1.1。

> 2026-08-25 N5 AI 模型连接健康检查已完成本地交付：AI 配置页新增“测试连接”，后端复用已保存密钥并允许新建 keyless Ollama；健康请求固定短文本、超时和 token 上限，Endpoint 规范化，响应/错误/审计均不泄露密钥。后端非集成全量 `2317 passed`、前端全量 `69 files / 287 tests passed`，真实模型连接、多模态/思考参数和项目生成仍待环境验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1.2。

> 2026-08-25 N5 模型能力提示与思考快捷配置已完成本地交付：AI 配置页默认关闭思考，支持 `thinking`、`enable_thinking`、`reasoning_effort` 快捷设置，并提示模型的思考/多模态能力线索；前端全量 `69 files / 289 tests passed`，真实参数接受情况仍待受控模型验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1.3。

> 2026-08-25 N5 AI 生成安全边界已完成本地交付：用例、测试数据集、Mock 和模型列表的网络错误不再回显供应商异常正文；用例 `raw_response` 最多保留 12,000 个字符并脱敏 JSON 敏感字段、键值和 URL 凭据。AI 用例/治理定向 `21 passed`、数据集/Mock/LLM 相关回归 `78 passed`、后端非集成全量 `2322 passed`，真实供应商返回格式和项目级生成仍待受控模型验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1.4。

> 2026-08-25 N2 Karing 真机前置阶段已完成：目标设备 `172.16.102.91:5555` 的设备包管理确认 `com.nebula.karing` 和 `com.nebula.karing/.MainActivity`，Windows Android Worker、依赖 readiness、Worker registry 和设备扫描通过；后续低代码与录屏结果见下方最新记录。脱敏证据见 [`evidence/android-karing-acceptance-2026-08-25.json`](evidence/android-karing-acceptance-2026-08-25.json) 与 [`evidence/windows-android-karing-worker-2026-08-25.json`](evidence/windows-android-karing-worker-2026-08-25.json)。

> 2026-08-25 N2 Karing 低代码与录屏回传已通过：真实 `run 27` 完成启动/等待/截图 3/3，`run 29` 开启录屏后 3/3 步骤、3 张截图、3 个产物且专用 `screen_recording` 门禁通过；临时项目均已清理。`windows-local-smoke.ps1` 新增 `-RequireAndroidRecording`，脚本契约 `12 passed`、PowerShell 解析通过。稳定性/Monkey、性能、流畅度、事件/日志/报告和最终清理已由后续 N2 证据关闭，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.0.5～2.0.7。

> 2026-08-25 N2 Karing 性能与流畅度专项已完成真实单设备复核：性能运行 `10` 完成 CPU/内存/电量/温度/FPS/卡顿采样，流畅度运行 `13` 完成滑动/点击两个阶段并采集 FPS/jank；事件序列无重复，JSON 报告导出均 HTTP 200，临时项目 `51` 删除 204 且删除后查询 404。Android 14 内存 VmRSS 兜底和 gfxinfo UI HISTOGRAM/GPU 排除已修复并复验；脱敏证据见 [`evidence/android-karing-performance-fluency-2026-08-25.json`](evidence/android-karing-performance-fluency-2026-08-25.json)。

> 2026-08-25 N2 控件属性获取诊断已完成本地交付：UIAutomator/Worker/API 统一返回脱敏诊断状态，录制界面在不可用时提示并保留坐标回退；定向 `22/23 passed`、后端非集成全量 `2305 passed`、前端全量 `69 files / 284 tests passed`，真实 Karing 页面和 UIAutomator 权限仍待真机验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1。

> 2026-08-25 N0/N6 测试套件导航归位已完成本地交付：`/suites` 加入测试资产侧栏，深链选中和路由元数据补齐；导航定向 `8 passed`、前端全量 `69 files / 285 tests passed`，真实账号/项目权限复核仍待环境验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.2。

> 2026-08-25 N0 工作台运行记录入口收敛已完成本地交付：侧栏按参考导航保留首页、待办、项目中心和任务中心，`/runs` 与 `/runs/:id` 旧地址继续可用并归入任务中心选中态；导航定向 `9 passed`、前端全量 `69 files / 286 tests passed`，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.3。

> 2026-08-25 Android 低代码录制局部交付：可视化点击录制保存文本、resource-id、content-desc、className、bounds 与原始坐标，回放按控件属性优先、坐标兜底；后端定向 `42 passed`、非集成全量 `2297 passed`，前端定向 `4 passed`、全量 `68 files / 277 tests passed`，类型检查、构建、Ruff、差异检查和独立代码审查通过。真实 Karing/Worker 真机验收仍未关闭，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 1.3。

> 2026-08-25 Android 低代码动作语义补强：长按支持控件属性定位和真实同点 swipe 长按，输入步骤分离输入内容与目标控件并支持 targetText/resource-id/content-desc；后端定向 `45 passed`、非集成全量 `2300 passed`，前端定向 `11 passed`、全量 `68 files / 279 tests passed`，类型检查、构建、Ruff、差异检查和独立代码审查通过。真实 Karing/Worker 真机验收仍未关闭，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 1.4。

> 2026-08-25 Android 可视化滑动分辨率适配：录制滑动保存屏幕宽高，Worker 和生成的 Python 脚本按回放设备尺寸适配方向/坐标滑动，尺寸读取失败时兼容旧步骤；后端定向 `47 passed`、非集成全量 `2302 passed`，前端定向 `16 passed`、全量 `68 files / 282 tests passed`，类型检查、构建、Ruff、差异检查和独立代码审查通过。真实 Karing/Worker 不同分辨率与横竖屏回放仍未关闭，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 1.5。

> 状态：导航信息架构已按参考导航完成基线设计；N0 导航壳、N1 工作台与任务中心、N2.1/N2.2 内部缺陷与失败证据闭环、N2.3 报告中心、N2.4 用例评审工作台、N2.5 外部缺陷兼容、N3.1 API、N3.2 APP、N3.3 UI、N3.4 性能、N4.1 AI 智能测试工作台、N4.2 Hermes 助手、N4.3 需求与用例追踪、N4.4 知识中枢、N5.1 远程工具箱和 N5.2.1～N5.2.6 配置中心已完成本地实现、审查和回归，均待真实环境验收；P1-E.1 性能长期趋势、P1-E.2 性能基线回归门禁和 P1-E.3 性能报告/运行记录保留清理已完成本地 API、CLI、前端、回归和兼容修复；P1-F 已完成本地发布状态索引和边界同步，性能多节点/容量仍在推进；N6.1/N6.2 文档与浏览器回归、N6.3/N6.4 Windows API/Web readiness、N6.5 q19 真实迁移与临时 Web Worker 低代码验收、N6.6 q19 持久通用 Web Worker 验收、N6.7 q19 独立 Web 录制 Worker 部署、N6.8 Chromium 证据链、N6.9 Firefox/WebKit 与跨 API 副本、N6.10 性能节点与真实短压验收、N6.11 通知本地链路验收、N6.12 Android Backend/Agent 配置配对门禁、N6.13 全量质量门禁复核已形成证据，Android/Worker、真实通知供应商和外部平台验收仍待补齐。
> 目标：将现有 ATP 平台从“按已实现页面分组”调整为参考导航中的“工作台、测试能力、测试资产、智能中枢、系统”五层产品导航，并按模块逐步补齐统一入口与业务闭环。

> 使用方式：本文件是本轮导航重构的主计划。每个模块完成后必须依次完成“实现 → 测试 → 代码审查 → 修复 → 文档/记忆同步 → 提交推送”，并在下方实施记录和 `Task.md` 中更新同一状态。未完成联调的功能只能标记为“已实现，待环境验收”。

> 当前执行版：[`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 汇总本轮五组导航、模块台账、下一步顺序、风险解除条件和状态口径；本文件继续保存完整历史方案与实施记录。

> 2026-08-25 N5/N6 AI 用例来源追踪摘要已完成本地交付：生成结果携带配置/供应商/模型、接口数量、数据集版本、Mock 规则数量和生成时间；保存用例写入 `_ai_source`，详情页展示，审计记录模型摘要。来源摘要不含 Endpoint、提示词、API Key 或原始响应。后端非集成全量 `2323 passed`、前端全量 `69 files / 289 tests passed`，真实模型、项目权限和测试资产链路仍待环境验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1.5。

> 2026-08-25 N6 失败运行转内部缺陷入口已完成本地交付：Android 专项报告和性能压测详情支持失败/异常/取消/停止运行一键创建内部缺陷，详情展示关联项并跳转缺陷管理执行记录过滤；重复指纹由后端追加脱敏证据。性能定向前端/缺陷页 `17 passed`、后端缺陷/静态契约 `14 passed`、前端全量 `69 files / 292 tests passed`、后端非集成全量 `2326 passed`，真实项目角色和失败运行仍待环境验收，详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1.6。

> 2026-08-25 N9 发布证据索引与一致性校验子模块已完成本地交付：新增脱敏 JSON 索引、校验脚本及 Make/CI/预提交入口，定向校验与发布契约 `22 passed`、后端非集成全量 `2333 passed`、Ruff/格式/差异检查通过；候选 SHA 必须匹配当前 HEAD，最终 N9 仍待真实门禁收口。详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1.7。

> 2026-08-25 N9.2 发布文档同步一致性契约已完成本地交付：发布索引校验开发计划、Task、MEMORY、路线图、发布状态和发布清单的关键标记，缺少文件或标记时阻断发布校验；定向校验与发布契约 `23 passed`、后端非集成全量 `2334 passed`。详见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 2.1.8。

> 2026-08-25 Android 专项应用启动兼容已完成本地交付：性能、稳定性和流畅度执行器统一支持显式 Activity 或 Launcher Intent 自动发现，流畅度任务避免前置启动后的重复启动，专项任务表单空 Activity 不再写入 `.MainActivity`。后端非集成 `2295 passed`，四个受影响测试文件独立 `3/25/19/15 passed`，前端 `67 files / 275 tests passed`，类型检查、生产构建、Ruff、差异检查和代码审查通过。真实 Karing APK/包名、Windows Android Worker/ADB、专项媒体和报告仍待环境验收；下一入口是 Karing 单设备闭环，N4 真实 Kubernetes/Prometheus/独立 MinIO 继续保持独立外部门禁。

> 2026-08-25 Windows Android 验收探针已补齐：`windows-android-acceptance.ps1` 支持 `-LaunchActivity`，指定包名后以 Package Manager 非破坏性解析显式 Activity 或 `MAIN/LAUNCHER` 入口，脱敏报告记录应用元数据；脚本契约 `2 passed`、脚本目录 `93 passed`、质量/发布文档回归 `15 passed`，PowerShell 语法和当前设备自动/显式 Activity 探针通过。`com.android.settings` 仅用于验证探针行为，当前仍未发现 Karing，下一入口是 Karing APK/包名确认后的单设备闭环。

> 2026-08-25 计划同步：当前执行版已升级为 1.0。N4 的 Worker/目标服务采样、Kubernetes 容量预检、保留清理和 MinIO 跨端点恢复已具备本地可执行入口，后续只做真实 Kubernetes/Prometheus/独立 MinIO 环境验收；随后按 N2 Karing 真实包名、N5-N8 外部依赖和 N9 发布收口推进。真实环境未提供时保留 `[E]`，不以 q19 Compose、mock 或跳过项替代。

> 最新进度：API 工作台已在 q19 受控目标完成 HTTP、显式会话复用、gRPC TLS Unary、GraphQL、WebSocket、三种流式 gRPC、OpenAPI/Postman 解析及导入预览/落库/回读/清理；运行详情可导出 HTML/JUnit/PDF，报告中心已有按用例类型的本地统计展示。N4 性能验收脚本新增 Kubernetes 多节点/副本/Worker 资源预检和跨端点 MinIO 复制/恢复 smoke，但目标主机仍无 Kubernetes/独立灾备 MinIO，生产 Prometheus/MinIO 生命周期和跨主机恢复未关闭。证据见 [`evidence/api-protocol-targets-2026-08-25.json`](evidence/api-protocol-targets-2026-08-25.json) 与 [`evidence/report-closure-2026-08-25.json`](evidence/report-closure-2026-08-25.json)，不替代生产协议服务、生产对象存储或外部环境验收。

> 2026-08-25 N1 前端保存边界已补齐：GraphQL、WebSocket、gRPC 用例在 `CaseFormDrawer` 发起创建/更新请求前检查必填配置，空格和空消息会使用既有中英文提示直接返回；纯函数回归 `8 passed`，前端全量 `66 files / 272 tests passed`，类型检查、生产构建和差异检查通过。该项只改善本地反馈，不替代后端校验和真实多协议/完整报告环境验收。

> 2026-08-25 P0-B.3.1 APK 身份边界已补齐：Manifest 可解析包名时拒绝不一致的手工包名，并在上传对象或写入数据库前返回 `400`；匹配包名会归一化，无法解析 Manifest 时继续兼容手工值。定向 `22 passed`、后端非集成全量 `2284 passed`、Ruff/差异检查和代码审查通过；真实 Karing APK、下载端点、专项任务和完整报告仍待环境验收。

> 2026-08-25 Android 设备目标边界已补齐：专项任务创建、更新和手工触发在写入/投递前校验 `device_id` 存在，不存在或已下线直接返回 `400`；定向 `32 passed`、后端非集成全量 `2287 passed`、Ruff/差异检查和代码审查通过。设备仍是全局资产，真实在线、租约、ADB 和 Karing 应用验收保持独立门禁。

> 2026-08-25 Android 专项 APK 选择体验已补齐：选择器只展示已解析包名的 APK，选中后自动绑定并锁定包名，清空 APK 同步清除包名；未选择 APK 仍可手工填写。工具函数回归 `2 passed`，前端全量 `67 files / 274 tests passed`，`vue-tsc`、生产构建和差异检查通过，代码审查未发现可操作问题。该项与后端 Manifest 包名一致性保护对齐，不关闭真实 Karing APK、设备执行、专项任务和完整报告门禁。

> 2026-08-25 P0-A Windows API/Web 复核已重新关闭：当前账号登录、依赖 readiness、Web Worker、Playwright `12 passed`、浏览器矩阵、文件传输、Web 低代码下载、HTML/JUnit 报告和临时项目清理均通过；脱敏证据为 [`evidence/windows-full-readiness-2026-08-25.json`](evidence/windows-full-readiness-2026-08-25.json)，来源提交 `35ad777`。Android 检查为可选跳过，不改变 Karing 真机阻塞；下一执行入口为 N1 其他协议/完整报告真实证据。

## 0.1 导航对齐执行版计划（2026-08-24）

这份执行版把参考导航拆成可交付模块，作为后续开发和验收的跟踪口径。导航入口已经完成的模块不重复造执行器，只补齐“配置 → 执行 → 过程 → 报告/证据”闭环以及真实环境证据。

| 阶段 | 导航范围 | 本阶段重点 | 完成标准 | 当前状态 |
| --- | --- | --- | --- | --- |
| A | 工作台：首页、我的待办、项目中心、任务中心 | 统一项目筛选、待办聚合、重试/终止/批量操作、状态轮询和入口跳转 | 五类任务可查询；越权操作被拒绝；操作结果和失败原因可追踪 | `[E]` 本地完成，待真实角色和执行器验收 |
| B | 测试能力：API、APP、UI、性能、AI | 分别补齐各工作台的配置、执行、过程观察、报告和证据入口 | 每种测试方式都能从工作台完成一条最小闭环，旧 URL/API 保持可用 | `[E]` 本地完成，外部 Worker/设备/协议/模型待验收 |
| C | 测试资产：用例、计划、缺陷、报告、评审 | 打通运行结果、失败证据、缺陷、报告趋势和评审记录的双向跳转 | 失败运行可关联缺陷和证据；报告可追溯到用例/计划；评审有审计记录 | `[E]` 本地完成，待真实项目数据验收 |
| D | 智能中枢：Hermes、需求与用例、知识中枢 | 保证 AI 结果可编辑、可追溯、按项目隔离，并记录调用审计 | AI 生成/诊断结果有来源、权限和失败反馈；敏感信息不泄露 | `[E]` 本地完成，待真实模型和项目数据验收 |
| E | 系统：远程工具箱、配置中心 | 统一依赖诊断、配置版本、差异、审计和单资源回滚 | 管理员可定位运行问题并安全回滚；普通角色不可越权；输出脱敏 | `[E]` 本地完成，待真实数据库和生产配置验收 |
| F | 质量与发布收口 | Windows、Android、性能、通知、外部缺陷平台和发布证据 | 真实证据与本地证据分开；未验证项明确阻塞原因；发布清单可复核 | `[~]` Android 真机、通知供应商、外部平台仍待补齐 |

### 后续执行顺序

1. **先完成环境可验证项**：用当前有效账号重跑 Windows API/Web，恢复 ADB 到 `device` 后完成 Android 单设备闭环。
2. **再完成不依赖新凭据的代码项**：补性能多节点/容量限制、长期趋势和报告留存；用服务层、API、前端和独立测试覆盖。
3. **再做外部联调**：用临时目标验收真实通知供应商和 Jira/禅道/GitHub/GitLab，验证完成后清理测试数据和凭据。
4. **最后发布收口**：更新能力矩阵、用户手册、验收总结和证据索引，确认每个导航入口都有明确状态。

### 单模块交付门禁

- [ ] 先在本计划和 `Task.md` 登记模块范围、依赖和验收标准。
- [ ] 完成代码/配置后运行定向测试，并补充 bug 回归测试。
- [ ] 独立检查改动 diff，修复审查发现的问题，再运行受影响的全量质量门禁。
- [ ] 同步 `Task.md`、`MEMORY.md`、本路线图及必要的操作手册/验收文档。
- [ ] 使用 Conventional Commit 提交并推送；真实环境未验证的模块只能保留 `[E]`，不能改成 `[x]`。

## 0.2 2026-08-25 导航对齐持续开发计划（当前跟踪版本）

本节将参考导航固化为后续开发的唯一产品边界。侧栏只展示五组高频入口；设备、APK、专项任务、报告、Mock、数据集、Web/API 资产和平台治理等页面继续保留原 URL，但分别从对应工作台或配置中心进入，避免把所有功能继续堆在“系统管理”下面。

### 目标导航与职责

| 导航分组 | 侧栏入口 | 职责边界 | 当前状态 |
| --- | --- | --- | --- |
| 工作台 | 首页、我的待办、项目中心、任务中心 | 统一项目上下文、待办聚合、任务队列、重试/终止/批量操作和状态跳转 | `[E]` 本地闭环已完成，待真实账号、角色和任务数据验收 |
| 测试能力 | 接口测试、APP 自动化、UI 自动化、性能测试、AI 智能测试 | 从配置、用例选择、执行、过程观察到报告/证据的最小闭环 | `[E]` 各工作台已完成，待协议服务、Android/Web Worker、性能节点和真实模型验收 |
| 测试资产 | 测试用例、测试计划、缺陷管理、测试报告、用例评审 | 管理可复用测试资产，串联运行、失败证据、缺陷、趋势和评审历史 | `[E]` 本地关联和审计已完成，待真实项目数据及外部平台验收 |
| 智能中枢 | Hermes 助手、需求与用例生成、知识中枢 | 提供可追溯的查询、草稿生成、需求追踪和知识检索，不静默写入业务数据 | `[E]` 本地工作台已完成，待真实模型、需求/知识数据和调用审计验收 |
| 系统 | 远程工具箱、配置中心 | 诊断依赖和 Worker，查看脱敏配置版本、差异、审计并执行单资源回滚 | `[E]` 本地权限和脱敏闭环已完成，待真实数据库、密钥和部署配置验收 |

### 按导航推进的剩余开发项

| 顺序 | 模块 | 继续完善内容 | 验收出口 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 导航壳与工作台 | 复核侧栏折叠/刷新/窄屏、中文/英文文案、深层路由选中、旧 URL 映射和动态徽标 | 每个入口可打开；刷新与深链不丢项目上下文；权限隐藏和面包屑一致 | `[E]` 本地回归完成，随真实账号复核 |
| 2 | 工作台与任务中心 | 用当前有效账号验证待办、任务队列、轮询、重试、终止和批量操作 | 五类任务均能查询；越权操作拒绝；失败原因和操作事件可追踪 | `[E]` 本地完成，Windows API/Web smoke 仍需复跑 |
| 3 | API 测试工作台 | 用真实项目验证环境变量、认证复用、OpenAPI/Postman 导入及 HTTP/GraphQL/WebSocket/gRPC 执行 | 请求、断言、变量提取、依赖传递和报告证据完整 | `[E]` q19 协议目标、导入链路和 HTML/JUnit/PDF 报告已通过；生产环境独立跟踪 |
| 4 | APP 自动化工作台 | 先配对 Windows Android Worker 与 q19 Backend 的 Redis 注册通道，再验证设备扫描、租约、截图、APK 包名、低代码和专项任务 | 单设备执行、日志/截图/录屏/结果回传完整；设备冲突可解释 | `[~]` ADB 基础检查通过，但 `/devices/workers` 尚未看到在线 Worker |
| 5 | UI 自动化工作台 | 复核 Playwright 登录、录制、元素/页面对象/视觉基线、Trace/HAR/网络日志和多浏览器链路 | Chromium/Firefox/WebKit 均能录制、停止、回放并查看失败证据 | `[E]` q19 证据已有，Windows 最新 smoke 需用当前有效账号复跑 |
| 6 | 性能测试工作台 | 补多节点容量、资源限制、生产 Prometheus/MinIO 生命周期、跨主机恢复和长期趋势治理 | 压测执行、采样、基线门禁、趋势、保留清理和报告可复核 | `[~]` 本地趋势/门禁/保留清理完成，生产多节点仍待验收 |
| 7 | AI 智能测试与 Hermes | 验证三方模型、多模态/思考参数、生成草稿、失败诊断、调用限额和审计 | 结果带来源且可编辑；无模型/无权限时阻断；敏感值不回显 | `[E]` 本地完成，待真实模型和项目数据验收 |
| 8 | 测试资产与智能中枢 | 复核用例—计划—运行—报告—缺陷—评审双向跳转，需求/知识来源和权限边界 | 失败运行可追溯到证据/缺陷；AI/知识结果可追溯到来源 | `[E]` 本地完成，待真实数据及外部缺陷平台验收 |
| 9 | 远程工具箱与配置中心 | 在目标部署核对 PostgreSQL、Redis、MinIO、Worker、ADB、配置版本和回滚 | 诊断结果脱敏；回滚必须精确确认；普通角色不能越权 | `[E]` 本地完成，待真实部署和密钥验收 |
| 10 | 发布质量收口 | 绑定同一提交 SHA 汇总 Windows、Android、性能、通知和外部缺陷平台证据 | 发布状态索引、能力矩阵、操作手册和记忆文档一致，未验收项明确阻塞 | `[~]` Android、真实通知、外部平台和生产性能仍未关闭 |

### 当前执行顺序与记录规则

当前按 `P0-B.3 Android 真实门禁 → P0-A Windows API/Web 复核 → P1-C 真实通知 → P1-D 外部缺陷平台 → P1-E 性能生产环境 → P1-F 发布收口` 推进。每个模块必须在本计划和 `Task.md` 登记范围，完成“实现/调整 → 定向与全量测试 → 代码审查 → 修复 → 文档与 `MEMORY.md` 同步 → Conventional Commit 提交推送”后，才能进入下一项。环境未提供时继续做不依赖凭据的本地代码和回归，但不得把 mock、跳过项或 `offline` 设备写成通过。

## 0.3 2026-08-25 参考导航后续执行台账

本台账是参考导航落地后的下一轮执行版本。它把“导航入口已经存在”和“业务闭环已经在真实环境验收”分开记录；先完成当前阻塞链路，再进入真实外部依赖验收。

| 台账项 | 对应导航 | 本轮交付内容 | 必须留下的验收证据 | 当前状态 |
| --- | --- | --- | --- | --- |
| A1 | 工作台 / 任务中心 | 用当前有效账号验证项目筛选、待办聚合、轮询、重试、终止、批量操作和失败事件 | 页面深链、角色边界、操作事件、失败原因和 q19 日志无新增错误 | `[E]` 本地完成，真实账号链路随 P0-A smoke 已复核 |
| A2 | 接口测试 | 验证环境变量、认证复用、OpenAPI/Postman 导入、HTTP/GraphQL/WebSocket/gRPC 最小执行 | 请求、断言、变量提取、依赖传递、报告和权限边界 | `[E]` q19 已通过 HTTP、会话复用、gRPC TLS Unary、GraphQL、WebSocket、流式 gRPC、导入解析、导入预览/落库和 HTML/JUnit/PDF 报告；生产环境独立跟踪 |
| A3 | APP 自动化 | Redis/DB/注册前缀已配对并确认 Worker 在线；扫描、租约绑定控制、通用 APK 上传/包名识别、低代码、录屏和设备产物回传已通过，继续验证 Karing 专项任务和完整报告 | Worker 在线记录、目标设备、租约冲突、APK ID/包名快照、每个阶段事件、结果/日志/媒体和设备冲突说明 | `[~]` 通用 APK 链路已通过；Karing 专项动作、APK 下载端点和完整报告闭环仍待验收 |
| A4 | UI 自动化 | 复核录制、元素库、页面对象、视觉基线、回放、Trace/HAR/Console/网络日志和多浏览器 | Chromium/Firefox/WebKit 的录制、停止、回放、失败证据和报告链接 | `[E]` q19/本地证据与 Windows 最新 smoke 已形成 |
| A5 | 性能测试 | 补生产多节点调度、容量限制、Prometheus/MinIO 生命周期、跨主机恢复和长期趋势治理 | 节点分片、资源采样、基线门禁、趋势、清理、报告和恢复演练 | `[~]` 本地 P1-E.1～P1-E.4 完成，生产环境待验收 |
| A6 | AI / Hermes | 验证模型拉取、三方模型、多模态/思考参数、生成草稿、诊断、调用限额和审计 | 来源、权限、可编辑草稿、失败反馈、敏感信息不回显 | `[E]` 本地完成，待真实模型和项目数据 |
| A7 | 测试资产 / 智能中枢 | 复核用例—计划—运行—报告—缺陷—评审、需求追踪和知识来源双向跳转 | 运行/证据/缺陷/评审审计链和项目权限隔离 | `[E]` 本地完成，待真实项目数据/外部平台 |
| A8 | 远程工具箱 / 配置中心 | 核对 PostgreSQL、Redis、MinIO、Worker、ADB、配置版本/差异/单资源回滚 | 脱敏诊断、权限拒绝、精确 `ROLLBACK`、审计和部署配置 | `[E]` 本地完成，待目标部署复核 |

### A3 Android 当前阻塞与解除条件

- 当前 Windows ADB 已发现可用在线目标，`windows-android-acceptance.ps1` 对目标设备的基础命令、属性、包管理和 logcat 检查通过；这只说明 ADB 设备可用，不代表平台已经能调度它。
- 已通过临时 SSH 隧道让 Windows Agent 与 q19 Backend 使用同一个 Redis 实例、DB、认证配置和注册前缀；配对校验、Worker doctor 和 `/devices/workers` 在线注册查询均已通过，当前注册记录为 `android-win-HPS`。这只证明控制面连通，不代表设备扫描和执行链路完成。
- 扫描回调已在 10 秒窗口内完成并返回 2 台真实设备；根因是 `scan_adb_devices` 原先声明 `ignore_result=True`，导致任务虽已执行成功但 API 永远读到 `PENDING`。现已保留 Celery 结果并补回归测试；q19 Beat 继续保持受控停用，避免在单设备闭环前生成无关维护任务。
- 当前重新发现的设备不能直接假定已安装用户指定的 Karing；在执行应用专项任务前，必须用 `pm list packages` 确认包名，或在设备页选择实际存在的 APK 包名。
- Redis 配对完成后，验收顺序固定为：Worker 在线 → 设备扫描 → 租约/释放 → 截图与控件属性 → APK 包名 → Android 低代码 → 专项任务 → 事件、日志、录屏和报告回传。任一步失败都保留失败证据，不创建无设备的脏运行。

### 台账更新规则

1. 每个台账项开始前，在本节和 `Task.md` 写明依赖与最小验收出口。
2. 完成实现或配置后，先跑定向测试，再跑受影响全量测试；代码审查发现的问题必须在进入下一项前修复。
3. 只有同时具备代码/配置、测试、审查、修复和文档记录，才可将本地项标为 `[x]`；真实环境仍需 `[E]` 证据，不用 mock、跳过项或单纯心跳代替。
4. 凭据只通过本机环境变量或临时部署配置提供，不写入仓库、日志、截图或证据 JSON。

## 0.4 2026-08-25 参考导航下一阶段开发计划（当前跟踪版）

本节把参考导航从“菜单分组”推进到“可验收业务闭环”。侧栏继续保持五组职责：工作台、测试能力、测试资产、智能中枢、系统；设备、APK、专项任务、报告、Mock、数据集、Web/API 资产和平台治理页面保留原 URL，但必须从所属工作台或配置中心进入，不再全部堆在系统管理下。

### 分阶段交付与出口

| 阶段 | 目标模块 | 本阶段开发内容 | 最小验收出口 | 当前状态 |
| --- | --- | --- | --- | --- |
| P0-A | Windows API/Web | 认证诊断、API/Web smoke、录制、文件传输、报告导出和浏览器矩阵 | 当前有效账号下完成登录、执行、证据和失败诊断 | `[x]` 当前账号完整 smoke 已通过，脱敏证据已更新；其他协议与生产外部环境仍独立跟踪 |
| P0-B.1 | Android Agent 配对 | Agent/Backend 共用 Redis 实例、DB、认证、队列和注册前缀；清理重复 Worker | 配对脚本全通过，`/devices/workers` 有且仅有预期在线 Worker | `[x]` 配置配对与 registry 已通过 |
| P0-B.2 | Android 单设备扫描 | 修复扫描任务结果丢弃问题，清理旧心跳链并复跑 Worker smoke | 在线设备扫描回调在限定窗口内完成，并返回设备序列号/状态 | `[x]` 配对、registry、扫描回调和 2 台设备结果已通过 |
| P0-B.3 | Android 执行闭环 | 租约/释放、控件属性、截图、点击/滑动控制和 APK 资产选择/包名传递已完成；继续真实 APK 上传、录屏、低代码、专项任务和报告回传 | 单设备完成一次真实执行，事件、日志、媒体和结果可追踪 | `[~]` 控制面与资产选择已通过，真实用例执行仍待验收 |
| P1-C | 通知供应商 | SMTP/企业微信/钉钉真实投递、重试、限流和送达回执 | 临时目标完成投递并清理凭据/测试数据 | `[E]` 本地链路已完成，真实供应商待验收 |
| P1-D | 外部缺陷平台 | Jira/禅道/GitHub/GitLab 创建、去重、状态同步、权限和清理 | 临时项目全链路可追踪且异常脱敏 | `[E]` 本地适配器已完成，真实平台待验收 |
| P1-E | 性能生产能力 | 多节点调度、容量限制、Prometheus/MinIO 生命周期、跨主机恢复和长期趋势 | 真实节点完成短压、取消、采样、报告和恢复演练 | `[~]` 本地闭环完成，生产环境待验收 |
| P1-F | 发布收口 | 统一能力矩阵、发布状态、用户手册、证据索引和回滚边界 | 所有未关闭门禁有负责人、证据、复验命令和禁止事项 | `[~]` 持续同步 |

### 本轮执行顺序

1. 继续 P0-B.3：按“真实 APK 上传/包名 → 低代码 → 录屏 → 专项任务 → 报告”顺序逐项验收；当前设备是否安装 Karing 必须以包管理结果为准。
2. 保持 q19 Beat 受控停用，直到单设备执行闭环需要调度维护任务；恢复前确认队列没有历史积压。
3. Windows API/Web 和 N1 受控 API/报告闭环已复核通过；在 Karing 真机条件具备前，下一执行入口转为 N4 性能真实环境，再按 P1-C、P1-D、P1-E 的顺序补真实外部环境证据。
4. 每个模块均执行“实现/调整 → 定向测试 → 全量质量门禁 → 代码审查 → 修复 → 文档与记忆同步 → Conventional Commit 推送”；任何失败只记录为阻塞，不改写为完成。

### 证据与风险口径

- 本机 `.local-run`、临时 SSH 隧道和远端 Compose 运行态只用于验收，不提交凭据、密码、Token、MinIO 密钥或完整环境文件。
- Worker 心跳只能证明注册控制面可见，不能替代设备扫描、真实操作、媒体回传和报告验收。
- 扫描任务必须保留 Celery 结果；API 的 `scan_id` 轮询状态是扫描回调的必要验收条件，不能只根据 Worker 日志判断成功。
- 设备离线、未授权、包名未确认、扫描超时、队列积压和重复 Worker 都必须留下脱敏失败证据，并阻止后续真实执行任务创建。

### 2026-08-25 P0-B.3 Android 租约绑定控制验收

- 修复设备点击/滑动接口缺少租约校验的问题；现在必须通过 `X-Device-Lease-Token` 携带当前有效设备租约，缺少或过期令牌返回 `409`，避免绕过工作台直接控制真机。
- Android 低代码抽屉在选择设备后自动申请租约，持续编辑期间每 4 分钟续租，切换设备、切换脚本模式、保存关闭或卸载时释放租约；截图读取保持兼容，控件属性读取继续用于录制定位器。
- 定向后端 `43 passed`，前端 Android 抽屉 `3 passed`，`vue-tsc --noEmit`、Ruff 和差异检查通过；代码审查后补齐异步竞态和租约心跳。
- Windows Worker 真实验证：扫描完成 2 台设备；设备 ID `1` 的租约申请、截图 `29,099` bytes、UI 属性、点击/滑动 `200`、无租约点击 `409`、释放 `204` 均通过。脱敏证据见 [`android-control-lease-2026-08-25.json`](evidence/android-control-lease-2026-08-25.json)。
- 发布边界：本模块只关闭 Android 基础交互控制门禁，APK 包名、低代码真实执行、设备录屏、专项任务、事件/日志/报告回传仍待完成；不以截图或控制成功代替完整用例执行。

### 2026-08-25 P0-B.3 APK 资产选择与包名传递验收

- 修复 APP 自动化工作台选择 APK 后只传包名、不传资产 ID 的问题；触发专项任务现在携带 `apk_id`，后端验证资产属于当前项目，并将选中的 APK ID 与包名写入运行记录和配置快照，Worker 可继续根据运行记录取得 APK 对象存储路径。
- 任务默认 APK 的 ID 也会保留在手工触发运行快照中；跨项目 APK 选择返回 `400`，不会创建运行记录。
- 定向后端 `53 passed`，前端 APP 工作台 `3 passed`，`vue-tsc --noEmit`、Ruff 和 `git diff --check` 通过；脱敏证据见 [`android-apk-selection-2026-08-25.json`](evidence/android-apk-selection-2026-08-25.json)。
- 发布边界：当前工作区没有可用于真实上传的 APK，连接设备的 `pm list packages` 也未发现 Karing 包名；本项完成代码链路和隔离回归，不替代真实 APK 上传、包名识别、低代码执行和专项任务验收。

## 0.5 2026-08-25 当前开发计划与跟踪台账

本节是当前执行入口，细化 P0-B.3 的剩余工作。目标是让 APP 自动化工作台形成“选择资产/设备 → 执行 → 过程事件 → 媒体与日志 → 报告”的单设备闭环；代码链路已经存在但没有真实设备或 APK 证据的项目，仍只能标记为“已实现，待环境验收”。

### 当前模块拆分

| 顺序 | 模块 | 开发内容 | 依赖 | 最小验收出口 | 状态 |
| --- | --- | --- | --- | --- | --- |
| P0-B.3.1 | APK 资产与包名 | 上传 APK、自动识别包名/版本、项目隔离、工作台选择并把 `apk_id`/包名写入运行快照 | 可用 APK 文件、对象存储 | 上传后可查看包名；选择/跨项目校验；运行记录保留资产 ID | `[~]` 代码链路已完成，真实 APK 上传待验证 |
| P0-B.3.2 | Android 低代码最小执行 | 校验 `config.steps`、设备与租约，调度单设备运行，执行点击/滑动/输入/等待/启动等步骤，保存步骤状态和截图 | 在线 Worker、`device` 状态、已确认包名 | 一个最小用例从创建到终态，步骤、失败原因、截图和设备序列号可追踪 | `[E]` 本地调度、租约、APK 前置和步骤回归完成，真实运行待验收 |
| P0-B.3.3 | 录屏与异常回放 | 按配置启动/停止录屏，上传视频，异常时保留回放片段，在运行详情和报告中展示告警 | 低代码运行成功、设备录屏权限、MinIO | 成功/失败运行分别能看到视频或明确不可用原因，停止和清理不残留 | `[E]` 失败状态与报告告警已完成本地回归，真实设备采集待验证 |
| P0-B.3.4 | Android 专项任务 | 通过已确认包名安装/启动应用，执行专项动作，采集 logcat/事件并处理超时、崩溃和取消 | APK、在线设备、Worker 专项队列 | 任务可启动、执行、取消并进入终态；Karing 只在包管理确认后执行 | `[E]` 本地包名/失败终态回归完成，真实任务待验收 |
| P0-B.3.5 | 事件、日志与报告回传 | 保存阶段/操作/参数摘要/结果/耗时，回传设备日志、截图、录屏和报告引用，前端提供时间线 | P0-B.3.2～P0-B.3.4 | 运行详情和报告能按时间查看全过程，敏感参数脱敏，失败可定位 | `[E]` 本地事件/脱敏/日志/截图/报告引用闭环完成，真实设备与 MinIO 待验收 |

### 后续阶段顺序

1. 对真实环境先完成 P0-B.3.1～P0-B.3.2：确认真实 APK 上传包名、核对低代码到 Worker 的调度入口，再执行一个无破坏性的最小步骤用例。
2. P0-B.3.5 本地实现已完成：新任务可配置设备日志/结束截图，Worker 收尾统一写入产物与状态，事件和 JSON 报告统一脱敏；下一步转为在线设备、真实 APK、MinIO 的端到端验收。
3. Android 单设备闭环后，重跑 P0-A Windows API/Web 完整 smoke，确认认证、浏览器矩阵、文件传输、报告导出和 Web 低代码未被 Android 改动影响。
4. 之后按 P1-C → P1-D → P1-E → P1-F 处理真实通知供应商、外部缺陷平台、性能生产环境和发布收口；外部凭据只进入临时环境变量，不进入仓库或证据。

### 每个子模块的交付门禁

- 开发前：在本节和 `Task.md` 登记范围、依赖、最小验收出口和阻塞条件。
- 开发后：运行定向测试，并为每个修复补回归测试；再执行受影响的完整后端/前端质量门禁。
- 审查后：独立检查 diff，修复代码审查问题，再复跑定向和全量门禁。
- 收口时：同步 `Task.md`、`MEMORY.md`、发布状态和必要的证据 JSON；提交信息使用 Conventional Commits 并推送。
- 状态口径：`[x]` 只表示代码、测试、审查、修复和文档均完成；`[E]` 表示本地实现完成但真实环境未验收；`[~]` 表示正在推进；`[ ]` 表示尚未开始。

### 当前阻塞与禁止事项

- 当前没有可用于真实上传的 APK，设备包管理结果没有确认 Karing，不能把专项任务写成通过。
- Worker 在线、设备扫描、截图或控件属性通过，不能替代低代码真实执行、录屏、事件和报告回传。
- 没有在线 `device`、有效租约或项目归属校验失败时，不创建脏运行；离线/未授权状态只记录为阻塞。
- 不在计划、日志、截图、证据 JSON 或提交中写入密码、Token、MinIO 密钥、完整环境文件或敏感请求参数。

### 2026-08-25 P0-B.3.2 Android 低代码最小执行本地交付记录

- **问题与实现**：单设备低代码执行原先直接操作 ADB，没有申请/释放设备租约；选择的 `apk_id` 只停留在用例配置，未进入低代码 Worker 的 APK 安装前置。现在统一在单设备入口占用 `DeviceLease`，执行结束或异常时释放；设备矩阵继续由子运行独立持有租约。
- **APK 隔离**：Worker 按 `apk_id` 查询 APK 资产，并通过用例模块校验项目归属；兼容旧 `apk_object_name` 时限制在当前项目 `apks/projects/{project_id}/` 前缀内。选中资产后复用 Android preflight 下载并安装，安装失败会结束为可解释的 error。
- **代码审查与回归**：审查发现并修复了矩阵与默认设备同时配置时的重复占用，以及旧对象路径绕过项目边界的问题；Android 相关回归 `145 passed`，后端非集成全量 `2245 passed`，Ruff 和 `git diff --check` 通过。
- **证据**：脱敏记录见 [`android-lowcode-execution-2026-08-25.json`](evidence/android-lowcode-execution-2026-08-25.json)。
- **发布边界**：当前没有真实 APK 文件，尚未在在线 Android 设备上执行最小低代码用例；录屏、专项任务和完整事件/日志/报告回传继续保持未验收。

### 2026-08-25 P0-B.3.3 录屏与异常回放可观测性本地交付记录

- **问题与实现**：低代码开启录屏但 Worker 无法启动 `screenrecord` 时，原结果没有留下资产告警；专项任务异常回放启动或上传失败时，报告也无法区分“未发现异常”和“回放不可用”。现在分别写入 `result_summary.android_artifacts.screen_recording_error` 与 `summary_json.incident_replay`，并在专项报告详情展示不可用原因。
- **状态一致性**：专项回放成功上传后清除过时的启动失败告警，避免报告同时显示视频和“回放不可用”；远端录屏文件仍由既有清理逻辑处理。
- **代码审查与回归**：审查修复了“后续轮换成功仍显示旧告警”的误导问题；Android 执行器定向 `64 passed`，后端非集成全量 `2248 passed`，前端 Vitest `66 files / 269 tests passed`，`vue-tsc`、生产构建、Ruff 和 `git diff --check` 通过。脱敏证据见 [`android-recording-observability-2026-08-25.json`](evidence/android-recording-observability-2026-08-25.json)。
- **发布边界**：本项只完成失败可观测性和展示链路，不证明真实设备 `screenrecord` 权限、MinIO 视频上传、Crash/ANR 触发或远端报告媒体回传；这些仍需真实 APK 和在线 `device` 验收。

### 2026-08-25 P0-B.3.4 Android 专项任务本地交付记录

- **包名边界**：任务新建、编辑和手工触发现在都以项目内 APK 的已确认 `package_name` 为准；绑定 APK 缺少包名或手工包名不一致时返回 `400`，不会创建运行记录，也不会让应用显示名替代包名进入 Worker。
- **失败终态**：性能、稳定性和流畅度执行器在应用启动失败、Monkey 启动/异常退出或流畅度动作失败时将运行标记为 `failed`，写入 `summary_json.error_message`，记录完成事件并推送失败状态；正常取消仍保持 `stopped`，设备掉线既有摘要保留。
- **代码审查与回归**：审查修复了默认任务 APK 未参与包名解析、已绑定 APK 被手工包名覆盖、执行异常被误报为完成，以及异常路径依赖可替换 `asyncio` 取消类型的问题。专项相关定向回归 `86 passed`，4 个受影响测试文件独立运行 `28/25/19/14 passed`，后端非集成全量 `2255 passed`，前端 Vitest `66 files / 269 tests passed`，`vue-tsc`、生产构建、Ruff 和 `git diff --check` 通过。证据见 [`android-special-task-2026-08-25.json`](evidence/android-special-task-2026-08-25.json)。
- **发布边界**：本项完成本地代码和失败状态闭环，不证明真实 APK 安装、在线设备启动/动作、Monkey 超时取消、Crash/ANR logcat、录屏或报告媒体回传；当前设备包管理仍须确认 Karing 的真实包名，真实专项任务继续待在线 `device` 和 APK 验收。

### 2026-08-25 P0-B.3.5 事件、日志与报告回传本地交付记录

- **公共收尾采集**：新增 `backend/app/services/mobile_special_artifacts.py`，由 Android 专项 Worker 在执行成功、失败或前置异常后按配置尝试采集最多 10000 行 logcat 与结束 PNG 截图；成功上传 MinIO 后登记 `MobileRunArtifact`，并将文件名、大小、保存状态或失败原因写入 `summary_json.android_artifacts`。采集失败不会改变原始 `completed`/`failed`/`stopped` 状态。
- **配置与展示**：专项任务配置页新增设备日志和结束截图开关；报告详情页展示设备产物状态，已有报告文件表继续按产物 ID 获取短期下载 URL；性能专项已有 CSV/录屏/异常日志/回放与三类专项的事件时间线保持兼容。
- **脱敏**：事件参数、结果、消息以及 JSON 报告的运行摘要/配置快照统一遮盖常见凭据键和值，并处理 URL 查询 Token；平台生成的凭据不会写入事件、报告或证据，设备原始 logcat 仅在用户开启产物开关后按原样保存，需按真实环境数据合规策略复核。
- **代码审查与回归**：审查修复了事件达到 5000 条上限时产物摘要可能未提交的问题，并补充上传异常摘要脱敏；定向 84 项、后端非集成 `2260 passed`，前端 `66 files / 269 tests passed`，类型检查、生产构建、Ruff 和差异检查通过。证据见 [`android-event-artifact-reporting-2026-08-25.json`](evidence/android-event-artifact-reporting-2026-08-25.json)。
- **发布边界**：当前仍没有真实 APK/在线 `device`/MinIO 上传证据；结束日志/截图、性能录屏/异常回放、报告下载和对象清理必须在真实设备环境单独验收，不能把本地回归记为 Android 生产通过。

### 2026-08-25 P0-A Windows 本地 E2E 回归交付记录

- **问题与修复**：共享登录 fixture 放宽中文按钮空格匹配；补齐主布局侧栏徽标的 `/workbench/overview` 和运行详情内部缺陷的 `/defects` mock，避免真实 401 清理会话后造成假失败。
- **代码审查**：改动仅在 `frontend/e2e/`，没有修改生产认证、路由、API 或执行器；未新增密码、Token、Webhook 或外部平台凭据。
- **验证**：登录定向 `3 passed`，运行详情定向 `1 passed`，全量 Playwright `12 passed`；前端 Vitest `66 files / 265 tests passed`，`vue-tsc --noEmit`、生产构建和 `git diff --check` 通过。
- **边界**：Windows 真实 smoke 仍需用当前有效账号重跑；本地 mock 只证明浏览器回归隔离正确，不替代认证读接口、文件传输、报告导出和真实部署验收。

### 2026-08-25 P0-A 已认证浏览器冒烟复核记录

- **验证**：复用当前有效登录会话加载统计看板、工作台概览、我的待办、用例管理、执行记录、测试套件、存储管理和 API 契约资产；页面均未回到登录页，用户菜单显示 `admin`。
- **运行状态**：q19 Backend 最近 5 分钟无 `Traceback`、`ERROR` 或 `invalid input value for enum`；脱敏证据见 [`windows-browser-smoke-2026-08-25.json`](evidence/windows-browser-smoke-2026-08-25.json)。
- **边界**：本轮只覆盖已认证页面链路，不替代文件传输、报告导出、浏览器矩阵和 Web 低代码的既有完整 readiness；P0-A 仍需目标环境完整复验。

### 2026-08-25 P0-B Android 单设备验收前置复核

- **验证**：执行 `scripts/windows-android-acceptance.ps1`，`adb.exe` 和 ADB 命令响应正常；脱敏报告记录 `online=0, unauthorized=0, offline=1, other=0`。
- **审查结论**：脚本在离线设备状态下只记录必需检查失败和重连提示，不继续执行设备命令、包管理、日志读取或创建 Android 运行任务；本地报告位于被忽略的 `.local-run/android-acceptance-current-20260825.json`。
- **边界**：ADB 恢复为 `device` 前，P0-B 的扫描、预约、截图、APK 包名、Android 低代码、专项任务和结果回传仍不能标记为通过；下一步继续等待真实设备恢复。

### 2026-08-25 Android 录屏证据展示补齐

- **问题**：Android Worker 已将录屏上传地址写入 `result_summary.android_artifacts.screen_recording`，但运行详情页和 HTML 报告只读取通用 `video_url`，导致录屏已经产生却不可见。
- **实现**：运行详情优先使用通用 `video_url`，否则回退到 Android 录屏资产；当设备端返回 `screen_recording_error` 时展示可解释告警。HTML 报告同步嵌入 Android 录屏，PDF 的既有无视频行为保持不变。
- **实时性**：运行详情收到 WebSocket `completed` 事件后重新获取运行详情，执行页无需手动刷新即可显示刚上传的录屏或告警。
- **审查与验证**：提交 `279b254`；后端导出 `13 passed`，RunDetail `6 passed`，前端全量 `66 files / 268 tests passed`，`vue-tsc`、生产构建、Ruff 和 `git diff --check` 通过。脱敏验证记录见 [`android-recording-evidence-2026-08-25.json`](evidence/android-recording-evidence-2026-08-25.json)。
- **远端部署**：q19 已从 `origin/main` 的 `257c479` 独立工作树重建并启动；迁移 `20260824_0065 (head)`、健康 `200`、Prometheus 4 个 target `up`、Celery 2 节点响应、后端最近 3 分钟错误匹配数为 0。证据见 [`q19-android-recording-deployment-2026-08-25.json`](evidence/q19-android-recording-deployment-2026-08-25.json)。
- **边界**：本模块证明结果展示/报告链路，不证明设备端真实录屏采集；本轮后续已发现 ADB 在线但 Worker 注册通道未配对，P0-B 仍需完成真实设备调度和结果回传验收。

### 2026-08-25 P1-D 外部缺陷平台错误安全收口

- **问题**：缺陷跟踪连接、创建和状态刷新入口会把第三方异常原文直接返回，异常可能包含 Token、密码、Webhook 查询参数或 URL 用户信息；缺陷创建入口还存在查询结果变量复用导致的 mypy 类型错误。
- **实现**：新增统一 `safe_external_error`，覆盖敏感字段、URL 查询密钥和 URL 用户信息脱敏，并限制错误长度；连接测试、创建缺陷、刷新状态分别保持失败响应结构或返回 502，不泄露供应商原文。
- **审查与验证**：提交 `31df065`；外部缺陷定向 `40 passed`，完整后端非集成 `2234 passed`，Ruff、格式、mypy 和 `git diff --check` 通过。脱敏证据见 [`external-tracker-error-safety-2026-08-25.json`](evidence/external-tracker-error-safety-2026-08-25.json)。
- **边界**：本模块只证明本地错误边界和脱敏逻辑，真实 Jira、禅道、GitHub Issues、GitLab Issues 的创建、去重、状态同步、权限和清理仍待临时目标与凭据。

### 2026-08-25 P1-D q19 运行态部署

- **部署**：q19 从 `origin/main` 的 `cec8eaf` 独立工作树重建，显式复用 `atp-q19-acceptance-20260824` Compose 项目名，不删除已有数据卷。
- **验证**：迁移 `20260824_0065 (head)`、Backend `200`、Redis `PONG`、Prometheus ready/`4` targets up；通用 Worker、性能 Worker、Beat、Web Recorder 共 `4` 个容器运行，最近 3 分钟 Backend/Worker 错误匹配数为 `0`。
- **边界**：这是 q19 Compose 运行态证据，不关闭真实 Jira/禅道/GitHub/GitLab 联调、Kubernetes 多节点或 Android 真机门禁。证据见 [`q19-external-tracker-deployment-2026-08-25.json`](evidence/q19-external-tracker-deployment-2026-08-25.json)。

### 2026-08-25 P1-C 通知验收脚本错误脱敏修复

- **问题**：`notification-channel-acceptance.py` 原先只移除异常文本问号后的部分，Token、密码或 URL 用户信息仍可能进入控制台和 JSON 报告；全量运行时新增测试还会被历史通知测试的模块桩污染。
- **实现**：复用通知服务统一 `_safe_exception_message`，并让回归测试在唯一模块名下加载真实通知服务，保持单文件和完整套件均可运行。
- **审查与验证**：提交 `9852387`；通知定向 `12 passed`，完整后端非集成 `2236 passed`，改动文件 Ruff、格式和 `git diff --check` 通过。证据见 [`notification-acceptance-redaction-2026-08-25.json`](evidence/notification-acceptance-redaction-2026-08-25.json)。
- **边界**：本模块只收口本地验收脚本和错误脱敏，不代表真实 SMTP/企业微信/钉钉供应商送达、限流或重复投递已验收。
- **追加修复**：提交 `8fd129b` 补齐 `notification-channel-smoke.py` 对 `access_token`、`sign` 和 URL 用户信息的脱敏，避免 DingTalk/Webhook 错误变体泄露；定向与完整回归保持 `12 passed` / `2236 passed`。证据见 [`notification-smoke-redaction-2026-08-25.json`](evidence/notification-smoke-redaction-2026-08-25.json)。

### 2026-08-25 工作台任务状态枚举隔离修复

- **问题**：q19 日志显示 `/workbench/overview` 将跨任务域失败状态集合直接用于每张运行表，普通 `TestRun` 查询因此收到 Android `stopped`，触发 PostgreSQL enum 错误。
- **修复与审查**：工作台按五个任务域映射状态过滤，空交集显式返回无匹配条件；重试按钮按域状态集合判断。未改执行器、权限或数据库结构，工作台定向 `8 passed`、完整后端非集成 `2229 passed`、Ruff/diff-check 通过。
- **验收边界**：q19 已按 `36cacb9` 受控重建，迁移、Backend、Worker、Prometheus 和 Celery 基础状态通过，重启后未出现新的 enum 错误；脱敏证据见 [`q19-workbench-status-filter-2026-08-25.json`](evidence/q19-workbench-status-filter-2026-08-25.json)。鉴权工作台请求仍需当前有效账号，本轮未认证 `401` 不代表真实项目聚合、角色和任务操作验收通过。

## 0. 当前执行总表（2026-08-24 更新）

本表是当前开发入口的唯一排序依据。`[x]` 表示代码、测试和审查已经完成；`[E]` 表示本地实现已完成但仍需真实环境证据；`[~]` 表示正在推进；`[ ]` 表示尚未开始。每完成一个模块，都必须完成实现、测试、代码审查、问题修复、文档同步和提交推送，再进入下一项。

| 顺序 | 模块 | 状态 | 交付与验收边界 |
| --- | --- | --- | --- |
| P0-A | Windows API/Web 可用性复核 | `[x]` | 当前有效账号下完整 smoke 已通过登录、认证读接口、文件传输、Web 低代码、报告导出和浏览器矩阵；脱敏证据已绑定 `35ad777`。账号密码只通过本机环境变量传入，不写入仓库或证据。 |
| P0-B | Android Worker 单设备验收 | `[~]` | ADB 基础设备检查已通过；仍需统一 Backend/Windows Agent 的 Redis 实例、DB、注册前缀，确认 Worker 心跳可被 `/devices/workers` 读取，再完成扫描、预约、截图、APK 包名、Android 低代码、专项任务和结果证据。 |
| P1-C | 真实通知供应商验收 | `[E]` | 在不提交凭据的前提下，用临时 SMTP/企业微信/钉钉目标验证投递、失败重试、历史记录、敏感信息脱敏和性能摘要；本地 SMTP sink 仅作为链路通过，不替代供应商送达。 |
| P1-D | 外部缺陷平台验收 | `[E]` | 在提供临时测试项目和凭据后，验证 Jira/禅道/GitHub/GitLab 的创建、重复识别、状态同步、内部状态回写、权限和错误脱敏；所有临时 Issue 和凭据使用后清理。 |
| P1-E | 性能能力增强 | `[~]` | 单节点 q19 k6 短压已有证据；P1-E.1～P1-E.4 的趋势、基线门禁、运行记录清理和逐节点分片容量校验已完成本地实现；仍需真实 Kubernetes 多节点、节点资源限制、生产 MinIO 生命周期和跨主机恢复验收。生产历史不能由本地 mock 替代。 |
| P1-F | 发布收口 | `[~]` | 本地能力矩阵、Q18 状态、Release-Readiness Runbook 和 [`release-status-2026-08-25.md`](release-status-2026-08-25.md) 已同步；Android、真实通知供应商、外部缺陷平台和生产性能证据仍待环境验收，不把 Actions 绿灯当作唯一发布依据。 |

### 执行规则

1. P0-A 和 P0-B 依次执行；P0-A 只修复诊断/验收入口，不把未知账号密码写入 `.env`、脚本、日志或文档。
2. P0-B 必须以真实 ADB 在线状态为前置；`offline`、`unauthorized`、无设备都只能记录为阻塞或跳过，不能伪造成功结果。
3. 每个模块结束时记录命令、通过数量、证据文件和未完成边界；代码审查发现的问题必须在同一模块内修复并复测。
4. 真实环境证据和本地自动化证据分开保存，报告只允许脱敏值；涉及外部服务的测试数据和凭据必须清理。

### 当前推荐动作

P0-A 的 Windows smoke 账号来源与 401 提示改进已经完成；P1-E.1～P1-E.4 的性能趋势、基线门禁、运行记录/报告清理和逐节点分片容量校验本地实现也已完成，P1-F 已建立统一发布状态索引，但真实 Android 调度、多节点调度、生产 MinIO 生命周期、通知供应商和外部缺陷平台仍未验收。下一步先解除 Android Agent/Backend Redis 注册配对，再按单设备顺序取得执行证据；若设备或外部供应商不可用，继续完善不依赖外部环境的测试、诊断和文档，不把阻塞项改成完成项。

## 0.6 2026-08-25 参考导航版开发计划（执行跟踪）

本节按参考导航固化最终信息架构：侧栏只保留“工作台、测试能力、测试资产、智能中枢、系统”五组高频入口；设备、APK、专项任务、Mock、数据集、Web/API 资产和治理页面保留原 URL，但从所属工作台或配置中心进入。入口已经存在不代表对应执行器、真实依赖和报告证据已经验收。

### 导航分组与模块状态

| 分组 | 计划入口 | 本阶段交付重点 | 当前状态 |
| --- | --- | --- | --- |
| 工作台 | 首页、我的待办、项目中心、任务中心 | 项目上下文、待办聚合、统一任务队列、轮询、重试、终止、批量操作和失败原因 | `[E]` 本地闭环完成，真实账号/角色/任务数据待复核 |
| 测试能力 | 接口测试、APP 自动化、UI 自动化、性能测试、AI 智能测试 | 每个工作台完成“配置 → 执行 → 过程 → 报告/证据”，复用现有 API 和执行器 | `[E]/[~]` 本地能力已形成，APP 真机、性能生产和真实模型待验收 |
| 测试资产 | 测试用例、测试计划、缺陷管理、测试报告、用例评审 | 用例、计划、运行、报告、缺陷、证据和评审的双向追踪与审计 | `[E]` 本地关联完成，真实项目数据/外部平台待验收 |
| 智能中枢 | Hermes 助手、需求与用例生成、知识中枢 | 查询、生成、诊断和检索均保留来源、项目权限、人工确认和敏感信息脱敏 | `[E]` 本地工作台完成，真实模型/数据待验收 |
| 系统 | 远程工具箱、配置中心 | PostgreSQL、Redis、MinIO、Worker、ADB、浏览器节点、配置版本、差异、审计和单资源回滚 | `[E]` 本地权限与脱敏闭环完成，目标部署待复核 |

### 分阶段开发与验收计划

| 阶段 | 目标 | 开发/验证内容 | 最小验收出口 | 状态 |
| --- | --- | --- | --- | --- |
| P0-1 | 导航壳与工作台 | 侧栏分组、折叠/窄屏、深层路由选中、旧 URL 映射、项目上下文和动态徽标 | 五组入口可打开；刷新/深链不丢上下文；权限隐藏一致 | `[E]` |
| P0-2 | API 测试工作台 | 环境变量、认证复用、OpenAPI/Postman 导入、HTTP/GraphQL/WebSocket/gRPC、断言、提取、依赖传递 | 至少一个接口场景可配置、执行、查看步骤结果和报告 | `[E]` q19 HTTP、会话复用和 gRPC TLS Unary 已通过；导入、其他协议和完整报告待验收 |
| P0-3 | APP 自动化工作台 | Windows Worker/ADB 配对、设备扫描/租约、APK 包名、低代码、录屏、专项任务、事件/日志/报告 | 在线单设备完成真实 APK 用例，结果、媒体和失败原因可追踪 | `[~]` 本地闭环完成，真实设备/APK/MinIO 待验收 |
| P0-4 | UI 自动化工作台 | Playwright 录制、元素库、页面对象、视觉基线、Trace/HAR、网络/Console 日志、多浏览器回放 | Chromium/Firefox/WebKit 均能录制、执行、查看失败证据 | `[E]` 本地/q19 证据已有，本轮 Windows smoke 已复核基础浏览器矩阵；真实录制/回放门禁仍单独跟踪 |
| P1-1 | 性能测试工作台 | 多节点容量、资源采样、长期趋势、基线门禁、报告保留、Prometheus/MinIO 生命周期和恢复 | 真实节点完成短压、取消、采样、报告和恢复演练 | `[~]` 本地闭环完成，生产环境待验收 |
| P1-2 | AI/Hermes | 模型拉取、三方模型、多模态/思考参数、用例/数据/Mock 生成、失败诊断和调用审计 | 结果是可编辑草稿，有来源和权限；失败可解释且不泄密 | `[E]` 本地完成，真实模型待验收 |
| P1-3 | 测试资产与智能中枢 | 报告趋势、缺陷双向关联、评审审计、需求追踪、知识来源和权限隔离 | 失败运行可追到证据/缺陷/用例；AI/知识结果可追溯 | `[E]` 本地完成，真实数据/外部平台待验收 |
| P1-4 | 远程工具箱与配置中心 | 依赖诊断、配置快照、字段差异、审计、精确单资源回滚 | 输出脱敏；普通角色拒绝越权；回滚可审计且不半更新 | `[E]` 本地完成，目标部署待复核 |
| P1-5 | 发布质量收口 | 汇总同一提交 SHA 的代码、测试、审查、环境证据和回滚边界 | 能力矩阵、操作手册、发布状态、证据索引和任务台账一致 | `[~]` 持续同步 |

### 当前执行顺序

当前不新增重复执行器，按以下顺序推进：

1. 解除 APP 自动化真实门禁：在线 `device`、真实 APK 上传/包名、单设备低代码、录屏/专项任务及事件/日志/报告回传。
2. 复跑 Windows API/Web 完整 smoke：当前有效账号、认证读接口、浏览器矩阵、文件上传/清理、HTML/JUnit 报告和 Web 低代码。
3. 取得临时目标后，验收真实通知供应商和外部缺陷平台；凭据只进入临时环境，不写入代码或证据。
4. 验收生产性能多节点、Prometheus/MinIO 生命周期和跨主机恢复。
5. 绑定同一提交 SHA，完成发布状态、能力矩阵、操作手册、证据索引和回滚说明。

### 2026-08-25 APP 真实门禁前置复核

- ADB 选定设备 `172.16.102.15:5555` 与另一台设备均为 `device`；Windows Android Worker 和专用队列 doctor 通过，设备前置证据见 [`android-device-control-preflight-2026-08-25.json`](evidence/android-device-control-preflight-2026-08-25.json)。
- 在线设备未发现 Karing 的真实包名，控制面认证因 bootstrap 账号 HTTP 401 未完成 `/devices/workers`/`/devices/scan`；未创建运行记录，不把设备在线或 Worker 心跳写成 Android 执行通过。
- 下一步只在当前账号完成认证并确认真实 APK/package name 后，继续扫描、租约、低代码、录屏、专项任务和产物报告回传。

### 模块追踪模板

每个模块必须在 `Task.md` 和本路线图登记：范围、依赖、最小验收出口、测试文件/命令、代码审查结论、修复记录、证据路径、提交 SHA 和未关闭边界。交付顺序固定为：

`实现/调整 → 定向测试 → 全量质量门禁 → 代码审查 → 修复 → 文档与 MEMORY 同步 → Conventional Commit 提交推送`

只有代码、测试、审查、修复和文档全部完成才可标记 `[x]`；`[E]` 仅表示本地实现完成、真实环境待验收；mock、跳过项、Worker 心跳或 `offline` 设备不得写成通过。

## 1. 当前基线

当前导航实现位于 `frontend/src/layouts/MainLayout.vue`，路由实现位于 `frontend/src/router/index.ts`。现有能力已经覆盖项目、用例、套件、计划、运行记录、Web/Android、性能、AI、数据集、Mock、环境和系统配置，但仍存在以下结构问题：

- 页面按历史开发顺序分散在“测试设计、测试资产、执行中心、Android 专项、平台配置”等菜单中。
- 没有独立的“我的待办”和“任务中心”，失败任务、待评审用例、逾期计划、异常设备不能统一处理。
- 缺陷、报告和评审已有部分能力，但入口和数据流没有形成测试资产闭环。
- API、APP、UI、性能和 AI 能力已有基础实现，但缺少面向用户的一站式工作台。
- 远程工具箱和配置中心已形成独立工作台；配置中心已完成 N5.2.1～N5.2.5 的聚合、版本、差异、回滚后端治理与前端统一入口，知识中枢和需求与用例追踪已形成独立工作台。

本轮参考导航的对齐原则：

- **工作台**只放用户每天需要进入的首页、待办、项目中心和任务中心，不承载具体测试配置。
- **测试能力**按测试方式组织：接口测试、APP 自动化、UI 自动化、性能测试和 AI 智能测试；每个入口最终都应覆盖配置、执行、观察和结果。
- **测试资产**按可复用、可评审、可追踪的产物组织：测试用例、测试计划、缺陷管理、测试报告和用例评审。
- **智能中枢**只放跨模块的 AI/知识能力：Hermes 助手、需求与用例生成、知识中枢；模型、密钥和调用审计仍属于系统配置。
- **系统**只放平台运行和治理能力：远程工具箱、配置中心以及存储、通知、用户、审计等管理页面，不再把测试业务页面全部堆在系统管理下。
- 旧 URL、旧 API 和已有领域页面保持兼容；页面迁移优先调整入口和上下文，避免为了菜单重构重复实现执行器。

本路线不删除既有页面；旧 URL 和 API 保持兼容，新工作台优先复用现有服务和组件，避免重复实现执行器。

## 2. 目标导航

| 分组 | 目标入口 | 现状 | 目标 |
| --- | --- | --- | --- |
| 工作台 | 首页 | 已有 `/dashboard` | 增加待办、任务、质量摘要和快捷入口 |
| 工作台 | 我的待办 | 未独立实现 | 汇总待评审、失败任务、逾期计划、未处理缺陷和设备异常 |
| 工作台 | 项目中心 | 已有 `/projects` 和项目总览 | 作为项目、成员、资产和项目配置的统一入口 |
| 工作台 | 任务中心 | 未独立实现 | 统一管理执行队列、重试、终止、批量执行和定时任务 |
| 测试能力 | 接口测试 | API/GraphQL/WebSocket 等基础能力已有 | 建立请求编排、依赖、变量、断言和导入的一站式工作台 |
| 测试能力 | APP 自动化 | 已有 `/mobile-special/workbench`，聚合 Android 设备、APK、Worker、用例、专项任务和报告 | 继续补齐录制回放、日志、性能数据和兼容性矩阵的真实环境闭环 |
| 测试能力 | UI 自动化 | 已有 `/ui-workbench`，聚合 Playwright、录制、Web 用例、元素库、页面对象、视觉基线和运行观察 | 补齐真实 Worker、浏览器录制、Trace/网络日志、定位器修复和报告联调 |
| 测试能力 | 性能测试 | 已有 `/performance-workbench`，兼容 `/system/performance` | 补齐真实性能 Worker、节点容量、多执行器、资源采样和报告联调 |
| 测试能力 | AI 智能测试 | AI 用例、数据、Mock、诊断和自愈基础能力已有 | 统一 AI 能力中心，补齐覆盖率缺口和修复建议闭环 |
| 测试资产 | 测试用例 | 已有 `/cases` | 作为标准用例和自动化用例资产中心 |
| 测试资产 | 测试计划 | 已有 `/plans` | 与任务中心、套件和报告联动 |
| 测试资产 | 缺陷管理 | 已有外部缺陷跟踪和失败运行关联 | 增加内部缺陷记录、分派、状态流转和重复识别 |
| 测试资产 | 测试报告 | 已有运行详情和 Android 报告 | 增加多项目、趋势、对比、覆盖率和质量评分中心 |
| 测试资产 | 用例评审 | 已有审核、评论、版本快照和差异 | 建立独立待评审队列和批量评审入口 |
| 智能中枢 | Hermes 助手 | 已有 `/hermes` 项目助手 | 查询失败任务、复用运行诊断、生成可编辑测试计划草稿和汇总质量指标；结论保留来源 |
| 智能中枢 | 需求与用例追踪 | 已有 AI 生成基础能力 | 需求解析、验收标准、需求—用例追踪和变更影响分析 |
| 智能中枢 | 知识中枢 | 已实现，待环境验收 | 统一搜索规范、缺陷、方案、手册和历史运行经验，展示来源与项目权限边界 |
| 系统 | 远程工具箱 | 已有分散的依赖、Worker、ADB 和性能检查 | 统一远程诊断、环境检查和结果导出 |
| 系统 | 配置中心 | 已有启动、环境、变量、AI、存储、通知等页面 | 统一配置入口、差异、审计和回滚 |

## 3. 分阶段实施计划

### 阶段 N0：导航壳与信息架构（P0，基础完成）

目标：先完成导航和路由基础，不重复开发业务能力。

- [x] 重构 `MainLayout.vue` 为五个目标分组，并统一图标、标题、展开状态和权限控制。
- [x] 通过路由 `meta` 完成能力入口注册，声明标题、说明、角色和已有入口；旧路由仍由原页面承载。
- [E] 已增加项目上下文徽标、统一页面标题和五组导航面包屑；项目选择器和动态菜单徽标数据由 N1 工作台聚合承接，深层用例/项目路由和 Android 专项详情保持正确菜单选中与页面标题。
- [E] 侧栏已按参考导航精简为工作台、五项测试能力、五项测试资产、三项智能中枢和系统远程工具箱/配置中心；设备、APK、专项任务/报告、Mock、数据集、Web/API 资产和治理旧页面保留深链接并映射到所属工作台或配置中心。配置中心新增管理员专属用户、审计、保留策略和告警快捷区，避免入口丢失。
- [x] 为未实现模块提供明确的功能占位页和路线说明，避免点击后无响应或空白页面。
- [x] 保留 `/cases`、`/runs`、`/plans`、`/system/performance`、Android 专项等既有 URL，新增工作台入口以跳转或嵌入已有页面。
- [x] 增加导航路由注册回归测试，并修复详情/项目子路由的菜单选中兼容。

验收（基础导航）：登录后可以按目标五层导航找到所有现有功能；不同角色只能看到有权限的入口；旧链接仍能打开原页面。项目选择器、动态徽标和聚合待办在 N1 验收。

### 2026-08-24 N0 导航壳体验收口交付记录

- **实现**：新增 `frontend/src/layouts/navigation.ts`，统一菜单分组、系统/Android 专项路由标题、深层路由选中和面包屑生成；`MainLayout.vue` 使用同一份导航状态，避免标题、分组和选中状态分别维护。顶栏显示“导航分组 → 当前页面”，保留项目上下文徽标和原有权限菜单。
- **回归**：新增 `frontend/src/layouts/navigation.spec.ts`，覆盖系统页面包屑、用例详情/项目用例选中、Android 专项详情标题和未知路由降级；导航定向 `6 passed`，前端全量 `66 files / 262 tests passed`，`npm run type-check` 和 `git diff --check` 通过。
- **代码审查**：检查了路由前缀顺序、深层菜单选中、角色菜单未授权行为和移动端顶栏布局，未发现需要修复的问题；`components.d.ts` 为自动导入面包屑组件生成的类型声明，随改动保留。
- **边界**：本模块只调整导航呈现和路由状态，不改变后端权限、执行器或旧 URL；动态待办/执行中徽标继续由 N1 工作台聚合接口提供，Android 真机和外部平台仍按 N6 计划单独验收。

### 2026-08-24 N0 侧栏精简与平台治理入口交付记录

- **实现**：侧栏测试能力只保留 API、APP、UI、性能和 AI 工作台；测试资产只保留用例、计划、缺陷、报告和评审；系统只保留远程工具箱与配置中心。设备、APK、专项任务/报告、Mock、数据集、Web/API 资产和系统治理页面仍可通过工作台快捷入口、配置中心或原 URL 打开。
- **路由状态**：旧页面进入时映射到所属工作台高亮（例如 Web 资产→UI、API 合约→API、数据集/Mock→AI、设备/APK/专项→APP、性能中心→性能、系统治理→配置中心），不修改路由权限和后端接口。
- **治理入口**：配置中心仅向管理员显示用户、审计、保留策略和看板告警快捷按钮；工程师仍只能看到自身有权限的配置和工具入口。
- **验证与审查**：前端配置中心/导航定向 `9 passed`，后端静态契约 `7 passed`，全量前端 `66 files / 263 tests passed`，`npm run type-check`、生产构建和 `git diff --check` 通过；审查修复了 `/system/toolbox` 被系统配置兜底高亮的问题，并核对窄屏布局与旧 URL 可达性。

### 阶段 N1：工作台与任务中心（P0）

- [E] 1.1 “我的待办”：聚合待评审用例、五类失败运行、逾期计划和设备异常；内部缺陷数据暂以 N2 为边界。
- [E] 1.2 “任务中心”：统一展示 Case、Suite、Plan、Android 和 Performance 任务，并保留原领域列表/详情入口。
- [x] 1.3 统一任务字段：状态、时间、项目、错误信息、来源标识和可用操作已统一。
- [x] 1.4 任务操作：服务端权限/状态校验、重试、Android/Performance 终止、批量操作已完成；Suite/Plan/Case 终止保持不可用并明确反馈。
- [x] 1.5 状态刷新：工作台 30 秒、任务中心 10 秒轮询；已处理请求竞态和组件卸载清理。
- [x] 1.6 动态导航：项目上下文、待办数量和执行中任务徽标接入统一聚合接口。

验收：用户可以从一个页面找到所有待处理和执行中的任务，并完成重试、终止和查看结果。

N1 模块完成定义：待办和任务中心页面可打开；五类任务均能按项目筛选和跳转已有领域入口；服务端拒绝越权操作；重试/终止/批量操作有成功与失败反馈；轮询可停止；后端 API、前端组件、权限和独立测试通过；代码审查发现的问题已修复。当前只剩真实数据库、权限角色和执行器环境验收。

### 阶段 N2：测试资产闭环（P1）

- [E] **N2.1 内部缺陷闭环（P0 子模块）**：内部缺陷模型、创建、分派、状态流转、优先级、严重程度、重复识别和项目权限已完成本地实现、审查和回归；待真实项目角色验收。
- [E] **N2.2 失败证据关联（P0 子模块）**：失败运行、截图、日志、Trace、用例和缺陷双向关联，创建时默认带入脱敏上下文已完成本地实现、审查和回归；待真实失败执行和对象存储证据验收。
- [E] **N2.3 测试报告中心（P1 子模块）**：已支持项目/周期筛选、历史趋势、两次同用例运行结果对比、用例执行覆盖率、缺陷健康度和质量评分；可跳回运行详情、用例、套件和计划入口。待真实项目数据、权限和套件/计划联动验收。
- [E] **N2.4 用例评审工作台（P1 子模块）**：已将现有评审能力提取为独立页面，支持项目/状态/关键词筛选、待评审列表、批量通过/驳回、评论、版本差异入口和评审记录；待真实项目角色、数据和审计联动验收。
- [E] **N2.5 外部缺陷兼容（P1 子模块）**：保持 Jira、禅道、GitHub、GitLab 等现有集成兼容；内部缺陷支持项目级外部 Issue 映射、按当前缺陷创建外部 Issue、状态同步和可选内部状态回写。真实外部平台凭据、权限和网络联调仍待环境验收。

验收：从失败运行可以创建内部缺陷并带入脱敏证据，从缺陷可以回到运行和用例；重复失败能提示已有缺陷；报告可以按项目查看趋势和对比；评审过程可追溯；外部缺陷集成不回归。

### 阶段 N3：测试能力工作台（P1）

- [E] **API 测试工作台**：已将 `/api-workbench` 从占位页替换为真实工作台，支持项目/模块/协议筛选、请求目录、详情和最近执行观察；新建/编辑复用 `CaseFormDrawer`，OpenAPI/Postman/cURL/样例导入与 AI 生成复用 `AIGenerateDrawer`，环境变量选择执行复用 `caseApi.run`，结果跳转原有运行详情。请求编辑、认证、断言、变量提取、依赖、数据集和 API 登录态复用继续由现有 Case/Worker 能力承载，未重复实现执行器；待真实项目角色、环境变量、协议服务和运行结果验收。
- [E] **APP 自动化工作台**：`/mobile-special/workbench` 已提供设备池、设备预约、APK 选择、截图预览、Android 用例/专项任务执行、最近运行和兼容性概览；公网后端继续通过 Windows Android Worker 执行本地 ADB，录制回放、日志、性能数据和真实设备链路待环境验收。
- [E] **UI 自动化工作台**：`/ui-workbench` 已提供项目/模块工作区、Web 用例目录与详情、低代码步骤预览、Playwright 录制创建、视觉基线截图采集、元素库/页面对象/视觉基线快捷入口、最近运行和 Trace 状态；新建/编辑复用 `WebCaseDrawer`，录制复用 `WebRecorderModal`，执行复用现有 `caseApi.run`。q19 Firefox/WebKit 录制与跨 API 副本停止快照读取已验收，Android/真实性能/通知/外部平台仍待环境证据。
- [E] **性能测试工作台**：`/performance-workbench` 已提供项目级场景库、环境/多节点发起、实时运行队列、RPS/P95/P99/错误率、阈值门禁、资源采样时间线、基线对比和 JSON/CSV 报告导出；复杂场景编辑、PromQL、调度、容量分析和多执行器继续复用 `/system/performance`，不把系统诊断配置混入工作台表单。q19 已验收真实性能 Worker、四类执行器、Prometheus 和一次真实 k6 短压；多节点容量、长期趋势和生产报告仍待环境增强。
- [ ] 每个能力工作台完成后，必须提供“配置 → 执行 → 过程 → 报告/证据”的单入口链路，并保留原有深链接。
- [ ] 每个工作台复用现有执行器和 API，不在前端重新实现执行逻辑。

验收：用户从测试能力菜单进入后，可以在同一工作台完成配置、执行、观察和查看报告。

### 阶段 N4：智能中枢（P1/P2）

- [E] N4.1 新增 AI 智能测试中心，统一项目模型门禁、AI 用例/数据/Mock 生成、自动化覆盖缺口、失败热点、诊断入口和自愈反馈；待真实模型、权限和调用审计验收。
- [E] N4.2 新增 `/hermes` Hermes 助手，支持项目级失败任务查询、Case 运行诊断、质量指标汇总和可编辑测试计划草稿；查询和建议均保留任务/报告/统计来源，待真实数据、模型诊断和权限验收。
- [E] N4.3 新增 `/requirements` 需求与用例追踪工作台：规则化文本解析可编辑草稿、项目级需求版本、验收标准、需求负责人、需求—用例关联、覆盖率和影响候选分析；待真实项目角色、需求数据和用例联动验收。
- [E] N4.4 新增 `/knowledge` 知识中枢：知识条目支持全局/项目范围、来源/状态/标签、版本和来源引用；统一检索手工知识、需求、缺陷和失败运行，按项目权限过滤并对敏感信息脱敏；待真实知识数据、角色和生产迁移验收。
- [ ] 保留人工确认、权限、调用限额、审计和敏感信息脱敏边界。

验收：AI 生成内容默认是可编辑草稿；Hermes 的查询和建议可追溯；需求验收标准和用例关联可审查；知识搜索结果包含来源、项目权限边界和脱敏结果。N4.1/N4.2/N4.3/N4.4 已完成本地工作台，真实模型调用、需求/知识数据、Case 诊断、调用限额/审计和生产迁移仍需环境验收。

### 阶段 N5：远程工具箱与配置中心（P1/P2）

- [E] N5.1 新增远程工具箱，统一检查 Worker、Redis、MinIO、PostgreSQL、ADB、浏览器节点和性能节点；入口为 `/system/toolbox`，接口为 `/api/v1/remote-toolbox/overview`。
- [E] N5.1 支持诊断结果、检查时间、失败原因和安全 JSON 导出，不输出密码、Token、密钥、连接地址或原始异常；仅展示脱敏队列、能力和节点状态。
- [E] N5.2.1 配置只读聚合与权限边界：新增 `/api/v1/configuration-center/overview`，统一展示启动配置、环境、全局变量、AI 模型、存储策略、通知和性能节点；支持项目筛选、脱敏摘要、原页面深链接和资源级可管理标记。启动配置只读，AI/存储遵循管理员边界，项目资源遵循现有项目角色；待真实环境验收。
- [E] N5.2.2 版本快照与变更审计：新增 `configuration_revisions` 与迁移 `20260824_0064`，保存 Fernet 加密原始快照、脱敏快照、指纹、创建人、原因和时间；新增版本创建/查询 API，统一写入安全审计日志；待真实数据库、角色和密钥轮换环境验收。
- [E] N5.2.3 配置差异与影响提示：新增 `/api/v1/configuration-center/revisions/{revision_id}/diff`，提供历史快照与当前状态的字段级差异；敏感字段只显示变更状态，并提示对执行、AI、通知、存储和性能调度的影响；待真实角色、密钥轮换和生产配置验收。
- [E] N5.2.4 单资源可控回滚：新增精确确认词、权限/项目范围/资源存在性/HMAC 复核；单资源恢复、敏感字段重新加密、新回滚版本和脱敏审计在同一事务中完成，失败自动回滚；不在线重启服务，不批量删除重建。
- [E] N5.2.5 配置中心前端工作台：新增 `/system/config` 统一入口，提供配置域导航、资源列表、版本时间线、差异面板和 `ROLLBACK` 回退确认交互；保留既有配置页与旧 URL，补齐项目筛选、加载/空历史/敏感状态、权限菜单、窄屏和键盘焦点。
- [E] N5.2.6 代码审查与质量收口：补齐跨项目历史版本拒绝、空历史、资源/变量行锁、刷新失败事务回滚和配置版本迁移链/降级断言；完成后端非集成、独立文件隔离、前端 Vitest、type-check、生产构建、Ruff、mypy、Bandit、全量格式和 diff-check，N5.2 标记为“已实现，待真实环境验收”。

N5.1 本地验收：管理员/工程师可以从一个入口判断服务是否可用，查看安全失败原因并导出脱敏结果；真实依赖、Worker、ADB、浏览器和性能节点仍需环境验收。N5.2.1～N5.2.6 已完成本地实现、审查和回归，下一开发入口是 N6 质量收口与发布验收。配置中心不负责直接重启进程，启动配置仍通过现有启动配置页和部署方式变更。

N5.2 验收口径：管理员/工程师能够从统一入口定位可见配置、查看脱敏当前值、历史版本和字段差异；普通角色不能越权读取或回滚；回滚只影响选中的单个资源且可审计；任何失败不得泄露敏感值或造成半更新。真实数据库、生产密钥、三方 AI、通知、Worker 和性能节点联调只作为独立环境证据记录。

### 阶段 N6：质量收口与发布（P0）

- [E] N6.1 已为配置中心新增管理员回退链、普通角色路由拦截和版本差异浏览器回归；其余新增路由、权限、聚合 API、迁移和关键交互继续按模块补齐回归。
- [E] N6.2 已同步能力矩阵、用户操作手册、导航说明、Task、MEMORY 和 Q18 实施日志；真实环境验收证据仍需按发布环境补齐。
- [E] N6.1 已完成前端 Vitest、type-check、生产构建和 Playwright 必要 E2E；后端回归、独立测试扫描和其余发布门禁继续收口。
- [E] N6.3 已完成远端 Redis/验收栈恢复和 Windows API/Web 最小发布 readiness；N6.4 已完成完整 Windows 登录、认证读接口、Playwright、浏览器矩阵、文件传输和报告导出证据，并修复项目删除级联阻断；N6.5 已完成 q19 真实迁移、项目删除回归和临时 Web Worker 低代码验收；N6.6 已完成 q19 持久通用 Web Worker 的队列隔离、低代码执行、监控和重启恢复；N6.7 已完成 q19 独立 Web 录制 Worker 的持久部署、Chromium 录制、截图、停止和重启恢复；N6.8 已完成 Chromium 证据链；N6.9 已完成 Firefox/WebKit 录制、Linux Worker WebKit 启动修复和跨 API 副本停止快照读取；N6.10 已完成 q19 性能节点、Prometheus 和真实 k6 短压。
- [ ] 按环境单独验收真实 Android/Worker、性能节点、通知和外部缺陷平台。

### 2026-08-24 N6.1 配置中心浏览器回归交付记录

- **覆盖范围**：新增 `frontend/e2e/configuration-center.spec.ts`，通过脱敏接口桩验证管理员从 `/system/config` 查看配置资源、历史版本、字段差异，使用精确 `ROLLBACK` 确认回退并提交请求；验证普通测试角色访问同一路由会被前端权限守卫拦截至 `/dashboard`。
- **代码审查**：首次执行暴露测试选择器命中同名摘要文本的严格模式问题，已改为按资源标题语义定位；修复后定向 E2E 通过，未发现产品逻辑、权限边界或敏感信息输出问题。
- **验证**：配置中心 E2E 定向 `2 passed`，全量 Playwright `12 passed`；前端 Vitest `65 files / 258 tests passed`，type-check、生产构建和 `git diff --check` 通过。真实配置、数据库迁移、生产密钥和外部依赖仍待环境验收，下一入口为 N6.2。

### 2026-08-24 N6.2 发布文档、能力矩阵与操作手册交付记录

- **文档同步**：能力矩阵新增五组导航、远程工具箱、配置中心和发布质量门禁的当前状态/实现入口/验收证据；用户手册修正旧七组导航，补充工作台、任务中心、远程工具箱、配置中心的使用、权限、脱敏和回退边界；Q18 最新状态与实施日志增加本轮记录。
- **一致性结论**：所有本地文档均明确区分“代码/自动化证据”和“真实环境验收”，没有把 mock、协议桩或本地浏览器回归描述为生产通过；`Task.md`、`MEMORY.md` 与路线图当时的下一入口统一为 N6.2 后续真实环境证据与发布 readiness。
- **验证**：文档链接和状态经代码审查核对，`git diff --check` 通过。真实数据库、生产密钥、Android/Windows Worker、性能节点、通知和外部缺陷平台仍需按环境生成带日期证据。

### 2026-08-24 N6.3 Windows 发布 readiness 与远端依赖恢复交付记录

- **远端恢复**：复核 `172.31.27.133` 发现旧 Redis 容器的 6379 发布状态与实际容器网络不一致，清理异常容器元数据并保留挂载数据目录；重启 Docker 网络层后重新启动 Redis 和 q19 验收栈的依赖/Backend，Redis 公网 6379、PostgreSQL 5432、MinIO 9000 可从 Windows 访问，q19 后端 `/health` 返回 HTTP 200；其他 Worker/目标服务仍需单独复核。
- **Windows 证据**：`scripts/windows-local-smoke.ps1 -SkipPlaywright -SkipBrowserMatrix -SkipFileTransfer -SkipReports -SkipLiveLogin` 通过 Windows doctor、后端健康、前端登录页、PostgreSQL/Redis/MinIO、ADB 可执行文件、k6/Locust/gRPC 和性能队列检查，脱敏结果见 [`windows-release-readiness-2026-08-24.json`](evidence/windows-release-readiness-2026-08-24.json)。Android 设备仅保留无在线设备 warning。
- **发布边界**：本次命令显式跳过管理员登录、认证读接口、Playwright/浏览器矩阵、文件上传、报告导出和 Android 执行；本机仓库 readiness 默认检查通过，严格模式因 Windows 未安装 Docker Compose 工具而失败，不能将该结果解释为完整发布通过。
- **审查结论**：本模块只涉及远端运行态恢复和脱敏证据归档，无仓库业务代码变更；文档与 JSON 证据已检查敏感值未输出。下一入口是补做完整 Windows 登录/Web/报告验收，再分别完成 Android Worker/真机、真实性能节点、通知和外部缺陷平台证据。

### 2026-08-24 N6.4 Windows 完整 API/Web 验收与项目删除级联修复交付记录

- **Windows 验收**：在当前 Windows 运行档案和远端 q19 PostgreSQL/Redis/MinIO 上，管理员登录、认证读接口、Web Recording 状态、Playwright mock E2E `12 passed`、Chromium/Firefox/WebKit 登录页矩阵、47 bytes 文件上传/清理、HTML/JUnit 报告导出均通过；脱敏证据见 [`windows-full-readiness-2026-08-24.json`](evidence/windows-full-readiness-2026-08-24.json)，必需失败数为 `0`。Web 低代码真实执行和 Android 项目因未指定用例/未启用 Worker 保持 optional skip。
- **问题修复**：清理临时项目时发现删除项目被 `environments_project_id_fkey` 阻断。模型与迁移 `20260824_0065` 统一补齐 APK、环境、模块、计划、套件的项目级 `ON DELETE CASCADE`，环境变量使用 `CASCADE`，计划环境引用使用 `SET NULL`；不在 API 中维护容易遗漏的手工删除清单。
- **审查与回归**：项目路由及新迁移契约 `34 passed`，迁移目录 `66 passed`，Alembic head 为 `20260824_0065`，Ruff 和 `git diff --check` 通过；远端 q19 临时项目/环境已清理并复核为不存在。该条记录当时仍等待目标数据库迁移，已由 N6.5 的真实迁移记录覆盖。
- **边界**：本轮证明 Windows API/Web 和远端三项依赖可用，不代表 Android Worker/真机、真实性能节点、通知渠道或外部缺陷平台已验收；严格 readiness 仍受当前 Windows 缺少 Docker Compose 工具影响。

### 2026-08-24 N6.5 q19 真实迁移与 Web Worker 临时验收交付记录

- **真实迁移**：从提交 `5179826` 构建 q19 最新 Backend、性能 Worker、Beat 和迁移镜像，在目标数据库完成 Alembic `20260814_0059 -> 20260824_0065` 升级；Backend、PostgreSQL、Redis、MinIO、性能 Worker 和 Beat 健康，Backend `/health` 返回 HTTP 200。
- **删除回归**：通过项目 API 创建并删除临时项目，HTTP 返回 `204`，项目、环境和模块残留均为 `0`，证明项目级资源级联和计划环境 `SET NULL` 迁移已在目标数据库生效。
- **Web 执行**：临时启动默认队列 Worker，完成 Web 低代码下载执行 `run 3`，下载对象回传和清理均通过；验收后已移除临时 Worker，未把临时进程写成持久 Worker 部署。脱敏证据见 [`q19-migration-web-worker-readiness-2026-08-24.json`](evidence/q19-migration-web-worker-readiness-2026-08-24.json) 与 [`windows-web-worker-readiness-2026-08-24.json`](evidence/windows-web-worker-readiness-2026-08-24.json)。
- **边界**：本条记录对应临时 Worker 验收阶段；后续 N6.6 已补齐通用 Worker 持久部署和重启恢复。Android 因 ADB 设备 offline 且本轮未启用 Android Worker 未执行，真实性能节点、通知渠道和外部缺陷平台仍待独立验收。

### 2026-08-24 N6.6 q19 持久通用 Web Worker 部署与恢复验收交付记录

- **编排实现**：`docker-compose.performance-acceptance.yml` 新增独立 `worker` 服务，固定监听 `default,maintenance`；性能 Worker 保持 `performance.worker-a,performance`，避免 Web/API 执行占用性能队列。
- **监控与回归**：通用 Worker 使用 `9091` 指标端口，q19 Prometheus target `atp-worker`、Backend、性能 Worker 和 Prometheus 均为 `up`；部署契约回归 `24 passed`，YAML 和 `git diff --check` 通过，代码审查无可修复问题。
- **真实执行**：q19 使用提交 `f1473d2` 重建持久 Worker，Celery ping 和队列隔离通过；首次 Web 低代码下载 `run 4`、Worker 重启后的 `run 5` 均成功并回传 1 个下载对象，临时项目和对象清理均通过。脱敏证据见 [`q19-persistent-web-worker-readiness-2026-08-24.json`](evidence/q19-persistent-web-worker-readiness-2026-08-24.json)、[`windows-persistent-web-worker-readiness-2026-08-24.json`](evidence/windows-persistent-web-worker-readiness-2026-08-24.json) 与 [`windows-persistent-web-worker-restart-readiness-2026-08-24.json`](evidence/windows-persistent-web-worker-restart-readiness-2026-08-24.json)。
- **边界**：本模块只完成通用 Celery Web/API 执行 Worker；独立 Playwright 录制 Worker、Trace/报告完整链路、Android Worker/真机、真实性能节点、通知渠道和外部缺陷平台仍待后续环境验收。

### 2026-08-24 N6.7 q19 独立 Web 录制 Worker 持久部署与真实录制验收交付记录

- **编排实现**：q19 Compose 新增独立 `web-recorder`，Backend 固定为 `WEB_RECORDER_MODE=worker`，API 与 Worker 共用 Redis 路由前缀；服务使用 Xvfb `:99`，清理残留 X99 锁并等待 socket 就绪后才启动 Worker，Compose `init: true` 负责回收 Chromium 子进程。
- **代码审查与修复**：本地 API/传输/smoke/部署契约回归 `61 passed`；审查发现 Xvfb 重启锁和 Chromium 僵尸进程两个问题，分别由 `7a94e62`、`dfe86b1` 修复；Ruff 与 `git diff --check` 通过。
- **真实证据**：q19 使用 `dfe86b1` 重建后，Worker 注册、Chromium 录制、2 步快照、PNG 截图 17,117 bytes、停止和临时项目删除通过；重启后再次通过，Worker `active_sessions=0`、容器 `zombie_count=0`，Backend `/health` 和 Prometheus 四个 targets 均正常。证据见 [`q19-web-recorder-readiness-2026-08-24.json`](evidence/q19-web-recorder-readiness-2026-08-24.json)、[`q19-web-recorder-restart-readiness-2026-08-24.json`](evidence/q19-web-recorder-restart-readiness-2026-08-24.json) 和 [`q19-web-recorder-init-readiness-2026-08-24.json`](evidence/q19-web-recorder-init-readiness-2026-08-24.json)。
- **边界**：本模块关闭 q19 独立 Chromium 录制基础链路，不等同于 Trace/HAR/Console/网络日志、Firefox/WebKit、跨 API 副本、运行报告完整链路或 Android/性能/通知/外部平台验收。

### 2026-08-24 N6.8 Web 录制证据链交付记录

- **实现**：录制会话采集 Playwright Trace、HAR、Console、页面异常、请求/失败请求/错误响应事件；停止时脱敏并上传 Trace、HAR 和运行报告到 MinIO，前端录制弹窗展示证据链接和网络事件预览。
- **持久查询**：独立 Worker 模式停止后在 Redis 保留最终快照，`GET` 可继续查询，重复 `stop` 幂等，已结束会话截图返回 409；报告中不保存 Cookie、请求体、响应体或敏感请求头。
- **代码审查与回归**：审查修复 JSON 凭据和完整 URL 文本脱敏边界；后端目标回归 `45 passed`，前端 Web Recorder `3 passed`，type-check、生产构建、Ruff 和 `git diff --check` 通过。
- **q19 证据**：提交 `9e93379` 重建并重启 `web-recorder` 后，首次和重启后录制均通过 Worker 注册、2 步快照、PNG 截图、停止、Trace/HAR/报告 URL 和停止后查询；证据见 [`q19-web-recording-evidence-2026-08-24.json`](evidence/q19-web-recording-evidence-2026-08-24.json) 与 [`q19-web-recording-evidence-restart-2026-08-24.json`](evidence/q19-web-recording-evidence-restart-2026-08-24.json)。测试项目和 6 个对象已清理。
- **边界**：Firefox/WebKit、跨 API 副本、Android Worker/真机、真实性能节点、通知和外部缺陷平台仍待后续环境验收。

### 2026-08-24 N6.9 Firefox/WebKit 与跨 API 副本录制验收记录

- **代码修复**：Linux `WEB_RECORDER_MODE=worker` 下 WebKit 使用无头启动，避免 Xvfb 中 headed WebKit 挂起；Windows/local 与 Firefox 保持 headed 行为，并新增 Worker WebKit 启动回归。提交 `41ff87a` 已推送。
- **代码审查与回归**：Web Recording/传输/smoke/部署目标 `45 passed`，Ruff 与 `git diff --check` 通过；未发现需要继续修复的代码问题。
- **q19 浏览器证据**：Firefox 完成 2 步快照、PNG 截图 `19076` bytes、停止、Trace/HAR/运行报告 URL 和停止后查询；WebKit 同链路通过，截图 `21192` bytes。证据见 [`q19-web-recording-firefox-2026-08-24.json`](evidence/q19-web-recording-firefox-2026-08-24.json) 与 [`q19-web-recording-webkit-2026-08-24.json`](evidence/q19-web-recording-webkit-2026-08-24.json)。
- **跨副本证据**：临时第二 API 副本成功通过共享 Redis 读取副本 A 停止的会话快照，返回 `status=stopped`；证据见 [`q19-web-recording-cross-api-2026-08-24.json`](evidence/q19-web-recording-cross-api-2026-08-24.json)。临时项目、18 个录制对象和第二副本已清理。
- **边界**：Android Worker/真机、真实性能节点、通知渠道和外部缺陷平台仍待后续环境验收。

### 2026-08-24 N6.10 q19 性能节点与真实短压验收记录

- **预检**：Backend `/health` 正常，k6/Locust/gRPC/JMeter 均 ready；`worker-a` online，队列 `performance.worker-a`，节点 allowlist 包含 q19 `http-target`。
- **真实运行**：临时项目执行 1 VU、5 次迭代的 k6 短压，运行状态 `success`，产生 1 条 `performance-worker` 采样；Prometheus ready，Backend、通用 Worker、性能 Worker 三个 target 均为 `up`。
- **清理与证据**：临时项目、测试、运行和脚本对象已删除，脱敏证据见 [`q19-performance-worker-smoke-2026-08-24.json`](evidence/q19-performance-worker-smoke-2026-08-24.json)。多节点容量、长期趋势、Android、通知和外部缺陷平台仍待后续验收。

### 2026-08-24 N6.11 通知链路本地安全验收记录

- **本地链路**：使用回环 SMTP sink 调用生产入口 `send_notification_channel`，12 项检查通过，覆盖 envelope、收件人规范化、MIME、显示名和性能摘要正文。
- **边界**：报告状态为 `local_link_only`，未触达真实邮箱或外部供应商，未记录凭据；证据见 [`notification-smtp-link-check-2026-08-24.json`](evidence/notification-smtp-link-check-2026-08-24.json)。真实 SMTP/企业微信/钉钉投递、供应商侧送达和外部缺陷平台仍待目标环境证据。

### 2026-08-24 N6.12 Android Backend/Agent 配置配对门禁记录

- **校验器**：新增 `scripts/validate-android-worker-config.py`，比较 Backend 与 Windows Agent 的共享基础设施/密钥并检查扫描模式、队列、Worker 队列和 Redis 注册前缀；报告只记录字段名和状态。
- **启动集成**：`windows-android-worker.ps1 doctor -BackendEnvFile <backend-env>` 可在启动前执行配对检查，`startup.ps1` 支持透传；示例配置已统一共享密钥占位符。
- **边界**：定向回归 `55 passed`，示例配置配对通过；该门禁不替代真实网络、服务凭据、Worker 心跳或 Android 真机证据。

### 2026-08-24 P0-A Windows smoke 401 诊断交付记录

- **实现**：`scripts/windows-local-smoke.ps1` 现在提取登录异常的 HTTP 状态；401 明确提示当前账号被拒绝以及 `FIRST_ADMIN_*` 仅用于首次创建管理员，并引导使用当前账号的 `ATP_USERNAME`/`ATP_PASSWORD`；403 提示检查 CSRF、Cookie 和服务端权限链路，其他异常只保留状态码或通用提示。
- **安全边界**：不再把登录原始异常文本写入终端或脱敏报告，账号、密码、Cookie 和 Token 仍只通过当前 PowerShell 进程传递；`docs/windows-local-run.md` 已补充临时环境变量用法和清理命令。
- **验证与审查**：Windows smoke 契约回归 `11 passed`，PowerShell AST 解析通过，`git diff --check` 通过；代码审查未发现需要修复的问题。
- **待验收**：当前只证明诊断逻辑和脚本语法，尚未使用真实有效账号重跑 Windows 登录、认证读接口、Playwright、浏览器矩阵、文件传输和报告导出，因此仍标记为“已实现，待环境验收”。

### 2026-08-24 P0-B Android Worker 设备状态诊断交付记录

- **实现**：`scripts/windows-android-worker.ps1 doctor` 新增 ADB 状态汇总，区分 `online`、`unauthorized`、`offline` 和其他状态，并排除 `List of devices attached` 标题行；针对未授权和离线分别提示 RSA 授权、USB 重连或 TCP ADB 可达性。
- **验证**：Worker doctor 在当前环境完成配置、Python 依赖、ADB、PostgreSQL、Redis、MinIO 检查，输出 `online=0, unauthorized=0, offline=1, other=0`；Windows 脚本契约 `11 passed`，PowerShell AST 解析和 `git diff --check` 通过；审查发现并修复了标题行误计数问题。
- **待验收**：Windows Android Worker 进程和队列正常，但真实设备仍为 `offline`；只有 ADB 状态恢复为 `device` 后，才能继续扫描、预约、截图、APK 包名、Android 低代码和专项任务执行，不能将本次 doctor 通过解释为 Android 执行通过。

## 4. 推荐执行顺序

```text
N0 导航壳
  -> N1 工作台与任务中心
  -> N2.1/N2.2 内部缺陷与失败证据闭环
  -> N2.3 测试报告中心
  -> N2.4 用例评审工作台
  -> N2.5 外部缺陷兼容
  -> N3 API/APP/UI/性能工作台
  -> N4 AI 与智能中枢（N4.1/N4.2/N4.3/N4.4）
  -> N5.1 远程工具箱
  -> N5.2.1 配置只读聚合与权限边界
  -> N5.2.2 版本快照与变更审计
  -> N5.2.3 配置差异与影响提示
  -> N5.2.4 单资源可控回滚
  -> N5.2.5 配置中心前端工作台
  -> N5.2.6 审查、回归与文档收口
  -> N6 质量收口与发布
```

预计工作量：单人约 30～45 个开发人日。N0、N1 完成后先做 N2.1/N2.2，形成失败运行到缺陷的最小闭环，再扩展报告、评审和测试能力工作台，避免继续增加分散入口。

## 5. 跟踪规则

- 代码完成但未联调：标记为“已实现，待环境验收”。
- 页面完成但后端/Worker 不支持：标记为“部分实现”，记录具体阻塞点。
- 每项完成内容至少记录一个测试文件、运行命令或外部验收证据。
- 新增模型必须有 Alembic 迁移；新增敏感配置不得写入文档、日志或报告。
- 每完成一个阶段，先做代码审查和回归，再更新 `Task.md`、`MEMORY.md` 与本计划状态。

## 6. 单模块交付清单

| 顺序 | 必做项 | 完成证据 |
| --- | --- | --- |
| 1 | 明确范围、角色权限、旧入口兼容和验收场景 | 计划条目与验收标准 |
| 2 | 实现后端 API/服务和前端入口 | 代码路径、接口契约或组件说明 |
| 3 | 补充回归测试 | `backend/tests`、`frontend/src/**/*.spec.ts` 或必要 E2E |
| 4 | 执行代码审查 | 审查结论、问题清单和修复记录 |
| 5 | 做质量门禁 | `make test` 或等价分层命令、type-check、build、`git diff --check` |
| 6 | 同步文档与记忆 | `Task.md`、`MEMORY.md`、本路线实施记录 |
| 7 | 提交并推送 | Conventional Commit、远端分支和提交号 |

模块状态只允许使用：`[ ] 未开始`、`[~] 实现中/待审查`、`[x] 已完成并有本地证据`、`[E] 已实现待环境验收`、`[-] 暂缓或有明确阻塞`。

## 7. 实施记录

### 2026-08-24 N0 基础导航

- 已完成五层导航壳：工作台、测试能力、测试资产、智能中枢、系统；原有 Android、性能、API 契约、数据集、Mock、通知和审计入口保留。
- 新增工作台、能力中心、缺陷、报告、评审、Hermes、需求、知识、远程工具箱和配置中心路由；未实现模块统一显示路线占位页，并提供已有页面入口。
- 路由元数据补充标题、说明、权限和复用入口；项目 URL 参数/项目路由参数在顶栏显示项目上下文，详情路由保持正确的菜单选中状态。
- 代码审查修复：恢复重构后丢失的 Android 旧入口；移除 CaseList 尚未支持的 `type` 查询链接，避免入口看似筛选但实际不生效。
- 验证：`npm run test -- --run`（51 个测试文件、211 项通过；含新增导航路由测试）、`npm run type-check`、`npm run build`、`git diff --check`。
- N1 已补齐项目选择器、待办聚合、统一任务队列和动态徽标数据；占位页仍不代表 API/APP/UI/性能等后续工作台已经完成。

### 2026-08-24 N1 工作台与任务中心

- 已确认实施边界：不新增重复执行器，优先通过聚合只读层复用现有 Case、Suite、Plan、Android 和 Performance 运行记录与执行接口。
- 新增 `/workbench/overview`、`/workbench/tasks` 及统一 retry/stop/batch-action API；新增 `workbench.py` 聚合层和 `schemas/workbench.py` 数据契约，复用原执行接口，不重复创建执行器。
- 新增“我的待办”和“任务中心”真实页面、项目筛选、状态/类型筛选、统一操作、轮询、请求竞态保护、卸载清理、双语文案和动态侧栏徽标。
- 代码审查修复：Case/Suite/Plan 不再显示不可用的终止按钮；统一重试先做来源项目 editor 权限校验再检查状态；聚合卡片使用完整计数而非截断列表；性能执行器从 `PerformanceTest.executor` 读取。
- 新增后端 `backend/tests/api/test_workbench_routes.py` 6 项边界测试和前端工作台挂载测试 2 项。
- 本地验证：后端非集成 `2131 passed`；前端 Vitest `52 files / 213 tests passed`；`npm run type-check`、`npm run build`、工作台定向测试、ruff 和 `git diff --check` 通过。
- 状态：本地实现、审查和回归完成；标记为“已实现，待环境验收”。待在真实数据库、不同项目角色、Android Worker 和性能节点上验证聚合、重试/终止权限及状态刷新。

### 2026-08-24 参考导航对齐与后续计划

- 已将参考导航固化为五组边界：工作台（首页/待办/项目中心/任务中心）、测试能力（接口/APP/UI/性能/AI）、测试资产（用例/计划/缺陷/报告/评审）、智能中枢（Hermes/需求与用例/知识）和系统（远程工具箱/配置中心及平台治理）。
- 明确系统管理不再承载测试业务页面；存储、通知、用户、审计、AI 模型、环境变量等属于系统治理，API/APP/UI/性能场景和执行属于测试能力。
- 将 N2 拆为 N2.1 内部缺陷、N2.2 失败证据关联、N2.3 报告中心、N2.4 评审工作台和 N2.5 外部缺陷兼容，按“失败运行 → 缺陷 → 证据/用例 → 报告/评审”顺序建设。
- N2.1/N2.2/N2.3/N2.4/N2.5 已完成本地实现、回归、代码审查和问题修复，状态为“已实现，待环境验收”；下一开发入口切换为 N3 测试能力工作台。每个子模块完成后，必须完成实现、回归、代码审查、问题修复、`Task.md`/`MEMORY.md`/本路线同步，再提交并推送。

### 2026-08-24 N2.1/N2.2 本地交付记录

- **内部缺陷**：新增 `backend/app/models/defect.py`、`backend/app/schemas/defect.py` 和 `backend/app/api/v1/defects.py`，支持项目级 CRUD、角色权限、指派、状态流转、重复指纹、五类执行记录来源和审计日志。
- **失败证据**：新增 `defect_run_links`，保存脱敏后的失败摘要、截图/日志/Trace/Android 产物/性能结果引用；创建前限制为失败或异常状态，嵌套 JSON 同样执行敏感字段脱敏，并支持缺陷详情与运行详情双向跳转。
- **前端入口**：`/bugs` 已替换为缺陷工作台，支持项目与状态筛选、创建/编辑、指派、证据详情和执行记录跳转；`RunDetail` 增加“创建内部缺陷”和已关联缺陷展示。
- **验证**：后端非集成 `2142 passed`；前端 `53 files / 214 tests passed`；`npm run type-check`、`npm run build`、迁移回归 `21 passed`、ruff、`git diff --check` 通过。真实数据库、项目角色、失败执行、MinIO/Trace 证据仍需环境验收。

### 2026-08-24 N2.3 测试报告中心本地交付记录

- **后端契约**：新增 `/reports/overview` 和 `/reports/compare`；按当前用户可见项目聚合已完成用例运行，返回周期趋势、通过率、平均时长、用例执行覆盖率、活动缺陷数、缺陷健康度和质量评分；对比接口限制为同项目、同用例、已完成运行，并统计时长、步骤和失败/异常步骤变化。
- **评分口径**：质量评分 = 通过率 60% + 用例执行覆盖率 25% + 缺陷健康度 15%；无完成运行时评分为 0；执行覆盖率明确表示周期内至少执行一次的用例比例，不等同于代码覆盖率；错误信息继续做敏感字段脱敏。
- **前端入口**：`/reports` 已替换为报告中心，支持项目/周期深链接、质量信号卡、趋势图、CSV 导出、最近运行记录、运行详情跳转和两次运行对比，并保留用例/套件/计划入口。
- **验证**：后端非集成 `2147 passed`；前端 `54 files / 217 tests passed`；`npm run type-check`、`npm run build`、迁移 heads、ruff、`git diff --check` 通过。真实数据库、项目角色、套件/计划报告联动和运行数据仍需环境验收。

### 2026-08-24 N2.4 用例评审工作台本地交付记录

- **后端契约**：新增 `/case-reviews` 队列查询、项目可见范围/状态/关键词筛选、分页和评审统计；新增批量通过/驳回接口，限制 editor 权限、只处理 pending 且非 deprecated 用例，并返回处理/跳过 ID。
- **评审追踪**：提交、通过、驳回和工作台批量操作统一写入评审审计事件；新增评审历史接口，兼容从已有版本快照回读历史；队列补充步骤数、快照数、最新版本和评审人信息。
- **前端入口**：`/case-reviews` 已替换占位页为独立评审工作台，支持项目/状态/关键词筛选、状态统计卡、批量操作、评论、详情抽屉、评审时间线、版本历史/差异入口和原用例跳转；旧 `/cases` 入口保持兼容。
- **代码审查修复**：批量评审返回的跳过 ID 改为保持用户请求顺序；修复 Vue 表格插槽 `record` 的类型推断问题。
- **验证**：后端定向评审/工作流回归 `8 passed`，非集成全量 `2150 passed`；前端全量 `55 files / 219 tests passed`（新增评审工作台 `2 passed`）；`npm run type-check`、`npm run build`、迁移 head `20260824_0060`、ruff 和 `git diff --check` 通过。真实数据库、不同项目角色、审计数据、版本差异和生产数据仍需环境验收。

### 2026-08-24 N2.5 外部缺陷兼容本地交付记录

- **后端契约**：新增 `defect_external_links` 映射模型和 Alembic `20260824_0061`；内部缺陷可按项目关联 Jira、禅道、GitHub Issues、GitLab Issues，记录外部标识、地址、标题、状态、同步状态、错误和同步时间。
- **同步能力**：复用现有 `bug_reporter` 四平台创建、重复查询和状态查询服务；支持手工关联、从内部缺陷创建外部 Issue、同步外部状态并按开关回写内部状态。创建描述做敏感信息脱敏，外部 URL 仅接受安全的 HTTP/HTTPS 地址，异常只返回通用错误。
- **前端入口**：缺陷详情抽屉增加外部关联卡片、创建/手工关联、状态同步和解除关联；关联信息对查看者可读，变更操作按全局角色隐藏，旧缺陷列表/详情接口保持兼容。
- **代码审查修复**：同步成功后立即更新详情表单中的内部状态，避免后续保存覆盖同步结果；查看者不再加载无权访问的跟踪配置，也不再看到会返回 403 的同步/解除关联按钮；创建、手工关联、同步和解除关联四个后端写接口统一要求管理员/工程师权限，避免绕过前端调用外部集成。
- **验证**：后端 `tests` 非集成全量 `2155 passed`；前端全量 `55 files / 220 tests passed`，缺陷详情定向回归 `3 passed`；`npm run type-check`、`npm run build`、Ruff、`git diff --check` 和迁移 head `20260824_0061` 通过。真实 Jira/禅道/GitHub/GitLab 凭据、网络、项目权限和平台状态差异仍需环境验收。

### 2026-08-24 N3.1 API 测试工作台本地交付记录

- **前端入口**：`/api-workbench` 已替换能力导航占位页，提供项目选择、模块树、协议图例、API 用例目录、关键词/协议筛选、用例详情、最近运行、执行环境选择和运行详情跳转；旧 `/cases`、API 契约资产和 Mock URL 保持兼容。
- **能力复用**：新建/编辑复用 `CaseFormDrawer`，OpenAPI/Postman/cURL/样例导入和 AI 生成复用 `AIGenerateDrawer`；新增 `allowedCaseTypes`，工作台只允许 API、GraphQL、WebSocket、gRPC，执行复用 `caseApi.run` 和既有 API Worker，不新增后端模型或迁移。
- **真实协议补充**：gRPC 用例现支持 TLS 根证书 PEM 和 SNI 服务名配置；q19 受控 Unary TLS 目标已完成评审、执行、断言、提取、清理和根证书脱敏证据，见 [`evidence/api-grpc-tls-2026-08-25.json`](evidence/api-grpc-tls-2026-08-25.json)。
- **导入解析补充**：OpenAPI/Postman 导入保留合法假值，解析 Postman 字符串 URL 查询参数、表单体并跳过 disabled 项；q19 `/ai/cases/parse-schema` 在 `75ed756` 上真实返回结构化接口清单，见 [`evidence/api-import-parser-2026-08-25.json`](evidence/api-import-parser-2026-08-25.json)。
- **导入落库补充**：q19 在 `a8f6e26` 上真实完成 OpenAPI 解析到导入预览、落库、回读状态码断言/步骤结果和项目清理；同时修复异步 SQLAlchemy 导入时 `Module.project` 懒加载导致的 500，见 [`evidence/api-import-persistence-2026-08-25.json`](evidence/api-import-persistence-2026-08-25.json)。
- **权限与状态**：项目查看者保持只读；新建、编辑、导入、执行按项目编辑权限控制；用例未满足 `is_ready_for_execution` 时禁止执行；最近运行状态、通过率和环境变量选择均从现有接口读取。
- **代码审查修复**：修复刷新按钮并发触发项目/环境/用例重复加载的问题，并补充 AI 类型过滤的类型约束。
- **验证**：前端定向工作台/导航 `4 passed`，前端全量 `56 files / 223 tests passed`，`npm run type-check`、`npm run build` 和 `git diff --check` 通过。该模块未修改后端，因此未重复运行后端全量；真实项目角色、环境变量、协议服务和运行结果仍待环境验收。

### 2026-08-24 N3.2 APP 自动化工作台本地交付记录

- **前端入口**：`/mobile-special/workbench` 已替换占位页，按项目加载设备池、Worker 状态、Android 用例、专项任务、APK、最近运行和兼容性家庭；设备、APK、专项任务和报告旧 URL 保持兼容。
- **执行与观察**：设备池支持扫描、在线设备选择、预约/心跳/释放和截图轮询预览；Android 用例复用 `caseApi.run`，专项任务复用 `mobileSpecialApi.triggerTask`，提供显式目标设备、可选 APK 包名覆盖和结果详情跳转。
- **边界与审查**：项目查看者只读；扫描、预约和执行按项目编辑权限控制。设备预约锁定项目切换和设备焦点，专项任务执行要求先释放 UI 租约，因为 Worker 会独立获取执行租约；设备池焦点不会覆盖专项任务默认目标，Android 用例仍使用用例自身配置。
- **验证**：定向工作台/导航 `2 files / 4 tests passed`，前端全量 `57 files / 225 tests passed`，`npm run type-check`、`npm run build`、`git diff --check` 通过。未新增后端模型、迁移或执行器；Windows Worker/ADB、在线真机、截图回传、专项任务报告和 APK 包名仍需真实环境验收。

### 2026-08-24 N3.3 UI 自动化工作台本地交付记录

- **前端入口**：`/ui-workbench` 已替换导航占位页，按项目/模块聚合 Web 用例、Playwright Worker 状态、Web 资产、最近运行和 Trace 状态；旧 `/cases`、`/system/web-assets`、`/runs` 深链接保持兼容。
- **配置与录制**：支持 Web 用例目录、关键词筛选、详情和低代码步骤预览；复用 `WebCaseDrawer` 新建/编辑，复用 `WebRecorderModal` 录制步骤并自动带入新用例草稿；视觉基线支持录制截图后填写阈值并保存。
- **资产与观察**：工作台提供元素库、页面对象、视觉基线快捷入口，资产页可按 `tab` 查询参数切换目标标签；运行按钮复用 `caseApi.run`，最近运行跳转既有运行详情，避免重复实现 Worker/执行器。
- **审查修复**：补齐项目清空状态清理、项目切换异步响应保护、空项目模块筛选边界和资产页查询标签响应式更新；定向覆盖录制草稿、项目清空、Web 类型筛选、详情和运行跳转。
- **验证**：定向 `3 files / 10 tests passed`，前端全量 `58 files / 230 tests passed`，`npm run type-check`、`npm run build`、`git diff --check` 通过。未新增后端模型、迁移或执行器；真实 Web Worker、浏览器录制、截图上传、Trace/网络日志、定位器修复和运行报告仍需环境验收。

### 2026-08-24 N3.4 性能测试工作台本地交付记录

- **前端入口**：新增 `/performance-workbench`，项目上下文下聚合性能场景、环境、节点、执行器、运行队列和资源证据；测试能力导航改指向新工作台，旧 `/system/performance` 保持完整控制台兼容入口。
- **配置与执行**：支持快速新建脚本型场景、选择环境、多选在线节点、覆盖 options、幂等键发起运行、活动运行 2.5 秒轮询和停止运行；完整控制台继续承载可视化 HTTP、压力阶段、PromQL、调度和容量分析。
- **过程与结果**：支持运行进度、RPS/P95/P99/错误率、阈值门禁、Worker/Prometheus/ATP 资源采样时间线、基线对比、成功运行设基线以及 JSON/CSV 报告导出。
- **审查修复**：修复执行器 API 失败时的错误放行、辅助接口失败导致全页不可用、资源来源本地化映射、证据请求竞态和完整控制台项目查询上下文丢失；权限边界保持项目查看者只读。
- **验证**：性能工作台定向 `4 passed`，性能中心/导航联动定向 `11 passed`，前端全量 `59 files / 235 tests passed`，`npm run type-check`、`npm run build`、`git diff --check` 通过。未新增后端模型、迁移或执行器；真实性能 Worker、节点容量、Prometheus、Locust/JMeter/gRPC 和生产报告仍需环境验收。

### 2026-08-24 N4.1 AI 智能测试工作台本地交付记录

- **前端入口**：新增 `/ai-workbench`，替换 AI 测试能力占位页；按项目聚合模型绑定、模块、用例自动化覆盖缺口、数据集、Mock、质量总览、失败热点和失败任务。
- **生成与上下文**：提供用例草稿、测试数据和 Mock 响应三个生成通道，分别复用现有 `/cases`、`/system/datasets` 和 `/mock-rules` 的编辑/预览链路；用例生成带入首个模块和 `ai_generate` 查询参数，不在工作台重复实现模型调用。
- **诊断与反馈**：失败任务跳转既有任务/运行详情；管理员可见生成漏斗、自愈反馈采纳率和高质量示例；模型密钥不展示，项目未绑定或禁用模型时生成入口禁用，查看者保持只读。
- **审查修复**：修复项目清空时 loading 残留、刷新并发下旧项目响应覆盖、项目级数据清理和管理员统计降级边界；补充自动化覆盖缺口信号和模型门禁。
- **验证**：定向导航/工作台 `2 files / 6 tests passed`，补充权限与模型门禁后工作台 `5 passed`，前端全量 `60 files / 240 tests passed`，`npm run type-check`、`npm run build`、`git diff --check` 通过。未新增后端模型、迁移或执行器；真实三方模型、调用限额/审计和生产 AI 结果仍需环境验收。

### 2026-08-24 N4.2 Hermes 助手本地交付记录

- **前端入口**：新增 `/hermes`，替换智能中枢中的占位页；项目选择器加载模块、用例、任务、报告和失败热点，旧任务/运行/报告/计划页面继续作为来源与确认入口。
- **可追溯对话**：四类快捷意图和关键词提问分别查询失败任务、触发 Case 运行诊断、汇总报告/统计指标、生成计划草稿；消息带来源链接，失败任务可直接选中或跳转详情。
- **计划草稿**：根据模块、用例数量、通过率和失败任务生成名称、目标及覆盖点，允许前端编辑、添加和删除覆盖点；只跳转 `/plans` 保存，不在后台静默持久化。
- **代码审查修复**：修复清空项目时变更参数未写回选中项目、旧请求覆盖新项目上下文、诊断失败无明确降级反馈；Case 以外任务类型展示原始错误并跳转专属详情。
- **验证**：Hermes 定向 `1 file / 5 tests passed`，前端全量 `61 files / 245 tests passed`，`npm run type-check`、`npm run build`、`git diff --check` 通过；未新增后端模型、迁移或执行器，真实项目数据、Case 诊断模型、任务权限和生产审计仍待环境验收。

### 2026-08-24 N4.3 需求与用例追踪本地交付记录

- **数据与迁移**：新增 `test_requirements`、`requirement_case_links` 两个模型和 Alembic `20260824_0062`；需求按项目隔离，保存状态、优先级、来源、版本、负责人和 JSON 验收标准，关联关系保存覆盖/验证、验收标准 ID 和备注。
- **后端契约**：新增 `/requirements` 列表/详情/创建/编辑/删除、规则化文本解析、关联/解除关联和影响分析 API；读操作要求项目 viewer，写操作要求 editor，负责人必须是有效项目成员，跨项目用例和未知验收标准拒绝，审计事件保留创建/更新/删除/关联动作。
- **前端入口**：需求导航从占位页替换为追踪工作台，支持项目/状态/关键词筛选、覆盖率信号、需求版本、验收标准状态、关联用例维护、未覆盖标准和影响候选；解析结果先进入可编辑草稿，不静默创建需求或用例。
- **代码审查修复**：移除需求列表无效条件表达式；创建响应兼容 ORM Python 默认值尚未回填的场景；补充负责人项目成员校验；前端增加项目/详情请求序列隔离和空状态处理。
- **验证**：后端需求/迁移/零状态回归 `10 passed`，前端需求追踪定向 `1 file / 3 tests passed`，前端全量 `62 files / 248 tests passed`，`npm run type-check`、`vite build`、Ruff 和 `git diff --check` 通过。真实需求数据、项目角色、用例联动、审计和生产迁移仍待环境验收。

### 2026-08-24 N4.4 知识中枢本地交付记录

- **数据与权限**：新增 `knowledge_entries` 和 Alembic `20260824_0063`；支持全局/项目条目、来源类型、草稿/发布/归档、版本、标签和来源引用，项目删除级联清理项目知识。全局知识仅发布后对普通用户可见，项目知识按成员范围可见，写入按管理员/项目 editor 控制。
- **统一检索**：新增 `/knowledge` API 和知识中枢工作台，支持项目、关键词、来源和状态筛选；手工条目可创建/编辑/删除，需求、内部缺陷和失败/错误运行以带来源的只读证据聚合展示。
- **安全与跳转**：标题、摘要、正文、来源引用和标签统一脱敏；结果显示项目/全局边界、命中词和来源链接，内置需求/缺陷来源可以精确打开对应记录，避免仅跳到默认列表项。
- **代码审查修复**：修复项目筛选漏掉全局条目、普通用户可见全局草稿、标签敏感信息未脱敏、知识结果和目标页面异步竞态/精确跳转不足等问题。
- **验证**：知识/需求/迁移回归 `15 passed`，知识中枢/需求/缺陷定向前端 `3 files / 9 tests passed`，前端全量 `63 files / 251 tests passed`，`vue-tsc --noEmit`、生产 `vite build`、Ruff 和 `git diff --check` 通过。真实知识数据、项目角色、生产迁移和数据量性能仍待环境验收。

### 2026-08-24 N5.2.2 配置版本快照与变更审计本地交付记录

- **数据与加密**：新增 `configuration_revisions` 模型和 Alembic `20260824_0064`；支持环境、全局变量、AI、存储、通知和性能节点的单资源版本，原始快照使用 Fernet 加密，另存脱敏快照、HMAC 指纹、创建人、原因和时间。
- **后端契约**：新增 `POST/GET /api/v1/configuration-center/revisions`；创建与查询均沿用管理员、工程师和项目角色边界，非管理员不能查看 AI/存储版本，项目版本按可见项目隔离，响应不返回加密原文。
- **审计与审查**：创建事件只保留配置域、资源 ID 和指纹，不记录原因或任何原始配置；代码审查修复普通明文环境变量被误判为密文、存储策略未统一走脱敏路径的问题，并补充回归覆盖。
- **验证**：配置版本与迁移定向 `7 passed`，后端非集成全量 `2185 passed`，Ruff 和格式检查通过；真实数据库迁移、项目角色、密钥轮换和生产审计数据仍待环境验收。下一入口为 N5.2.3 配置差异与影响提示。

### 2026-08-24 N5.2.3 配置差异与影响提示本地交付记录

- **后端契约**：新增 `GET /api/v1/configuration-center/revisions/{revision_id}/diff`，对历史 Fernet 快照与当前配置执行字段级 added/removed/changed 比较，提供指纹、资源状态、变更计数和最多 500 项变更保护。
- **安全输出**：普通字段仅返回脱敏前后值；密钥、Token、密码、Endpoint、Webhook、URL、secret 变量及性能节点出口/能力只返回 changed 状态；历史快照解密后校验 HMAC 指纹，校验失败不返回配置内容。
- **影响提示**：按环境、全局变量、AI、存储、通知和性能节点返回执行、引用方、智能能力、产物保留、消息投递和调度容量影响说明；资源删除时保留历史记录并明确无法计算当前字段差异。
- **代码审查与验证**：修复性能节点无项目范围权限、viewer 差异读取误用 editor、敏感变量名脱敏遗漏和路由 dataclass 响应转换问题；差异/路由定向 `11 passed`，后端非集成 `2191 passed`，Ruff/mypy/格式/diff-check 通过。真实环境仍待验收，下一入口为 N5.2.4 单资源可控回滚。

### 2026-08-24 N5.2.4 单资源可控回滚本地交付记录

- **后端契约**：新增 `POST /api/v1/configuration-center/revisions/{revision_id}/rollback`；请求必须携带 `confirmation=ROLLBACK`，只接受可见历史版本，响应返回源版本 ID、回滚结果和脱敏的新版本记录。
- **恢复范围**：支持环境及变量、全局变量、AI 模型、存储策略、通知配置和性能节点的单资源恢复；敏感环境值、全局变量、AI Key 和通知配置重新加密；启动配置仍只读且不提供在线重启。
- **安全与事务**：再次校验写权限、资源存在性、项目范围、历史 HMAC 和域/资源 ID；目标资源及环境变量行使用行锁，成功后在同一事务中创建“配置回滚”版本并写 `configuration_revision_rollback` 审计，审计只保留域、资源 ID 和指纹；唯一约束、篡改历史或其他失败均回滚。
- **审查与验证**：修复提交前刷新版本记录以保证刷新失败可回滚；回滚/路由定向 `15 passed`，后端非集成 `2195 passed`，前端 type-check/生产构建、Ruff、改动文件格式检查和 `git diff --check` 通过。全量格式扫描剩余仓库原有 `backend/app/api/v1/workbench.py`，留待 N5.2.6；真实配置、角色和密钥轮换仍待环境验收，下一入口为 N5.2.5。

### 2026-08-24 N5.2.5 配置中心前端工作台本地交付记录

- **统一入口**：`/system/config` 替换导航占位页，仅对管理员/工程师显示和开放；按配置域展示启动档案、环境、全局变量、AI、存储、通知和性能节点，支持全部项目/指定项目筛选及原页面深链接，旧页面和旧 URL 保持不变。
- **变更账本**：资源详情提供当前状态、历史版本时间线、版本字段差异、影响提示、缺失资源和敏感字段脱敏状态；可保存当前快照，回退操作必须在确认弹层输入精确 `ROLLBACK`，沿用后端单资源权限和事务边界。
- **交互与安全**：补充加载失败/重试、无权限、无历史、空资源和敏感字段状态；页面采用窄屏堆叠布局、可见键盘焦点和 `prefers-reduced-motion` 适配，快速切换资源/版本时使用请求序列防止旧响应覆盖当前选择。
- **验证与审查**：新增配置工作台 4 组回归场景（共 `6 passed`，与导航路由一起运行），前端全量 `65 files / 258 tests passed`，`type-check`、生产构建和 `git diff --check` 通过；代码审查修复资源/版本异步请求竞态及普通角色仍可看到受限菜单的问题。真实角色、配置数据、生产密钥和三方依赖仍待环境验收，下一入口为 N5.2.6。

### 2026-08-24 N5.2.6 质量收口本地交付记录

- **回归补齐**：新增配置版本空历史、跨项目差异拒绝、资源与环境变量行锁、提交前刷新失败事务回滚，以及 `20260824_0064` 迁移链/外键/索引/降级契约测试；配置中心相关 API/迁移定向 `24 passed`。
- **全量验证**：后端非集成 `2201 passed`；独立文件隔离扫描 `290 passed / 0 failed`；Ruff check、全量 Ruff format-check（600 个文件）、mypy（151 个源文件）和 Bandit 均通过；此前前端全量 `65 files / 258 tests passed`、type-check、生产构建和 `git diff --check` 通过。
- **审查结论**：新增测试桩继续采用 fill-missing-only，锁断言覆盖资源行和环境变量行，刷新失败验证回滚入口；未发现业务或安全问题，补做原有 `backend/app/api/v1/workbench.py` 的纯格式收口。N5.2 已完成本地质量收口，真实数据库、项目角色、密钥轮换和三方依赖仍待环境验收，下一阶段为 N6。

### 2026-08-25 P1-E.1 性能长期趋势本地交付记录

- **后端能力**：新增 `GET /api/v1/projects/{project_id}/performance/trend`，提供 1～365 天固定 UTC 日窗口、空日期补齐、成功/失败/取消/进行中计数、运行摘要平均指标和最大 P95；支持按性能场景筛选，并按项目权限隔离。
- **数据口径**：优先使用完成时间，其次开始/创建时间归档运行；分片父运行不重复计入，实际分片子运行保留；无效或非有限指标忽略。趋势明确标注为运行摘要算术平均值，不把不同运行的百分位错误合并。
- **前端入口**：`/system/performance` 和 `/performance-workbench` 均接入服务端趋势，支持近 7/30/90 天切换、加载/空状态和中英文文案；项目切换或清空时丢弃旧响应并复位加载状态。
- **审查修复**：代码审查发现并修复清空项目期间趋势请求被取消后加载状态残留的问题，补充项目清空回归断言；未发现权限、跨项目或分片去重问题。
- **验证**：性能趋势/容量/API 定向后端 `79 passed`，完整非集成后端 `2220 passed`；性能中心和工作台前端定向 `15 passed`，前端全量 `66 files / 265 tests passed`，`vue-tsc --noEmit`、生产构建、Ruff、格式检查和 `git diff --check` 通过。
- **边界**：本次只完成本地趋势能力；真实多节点容量、节点资源限制、生产 MinIO 生命周期和跨主机恢复仍待后续环境验收，不能用本地 mock 历史替代。

### 2026-08-25 P1-E.2 性能基线回归门禁本地交付记录

- **门禁策略**：性能运行门禁接口和 Webhook 门禁新增可选 `require_baseline=true` 与 `fail_on_baseline_regression=true` 查询参数；默认不传时保持原阈值门禁行为不变。
- **基线校验**：只接受当前压测定义、项目范围内的成功基线；基线缺失时 `require_baseline` 返回 `not_configured`，运行本身失败/取消时保留原始失败/取消状态，不被基线策略覆盖。
- **CI 工具**：`scripts/performance-gate.py` 新增 `--require-baseline` 和 `--fail-on-baseline-regression`，轮询时把策略传递到 Webhook；退出码继续保持 `passed=0`、`failed/cancelled=1`、`not_configured=2`。
- **审查修复**：修复 FastAPI `Query(False)` 在直接调用路由函数的历史单测中表现为 truthy 对象的问题，改用普通布尔默认值；补充基线回归、无基线、运行失败和 URL 参数测试。
- **验证**：性能报告/API/Webhook/CLI 定向 `88 passed`，完整非集成后端 `2224 passed`，Ruff 和格式检查通过；Android ADB 仍为 `offline`，未把真实设备或性能环境证据写成本地门禁通过。

### 2026-08-25 P1-E.3 性能报告与运行记录保留清理本地交付记录

- **清理范围**：现有运行记录保留服务新增终态 PerformanceRun 根运行统计，保留项目级覆盖和全局排除语义；pending/running/cancelling 不会被清理。
- **分片与证据**：删除根运行时依赖数据库级级联清理分片和 `performance_metric_samples`；预览与执行会收集根运行及直接分片的 `raw_result_object_name`，数据库提交成功后再删除对应 MinIO 报告。
- **管理入口**：管理员执行记录清理页新增压测执行数、全局/项目级统计和确认总数；旧后端响应缺少新字段时前端按 0 兼容，支持滚动发布。
- **审查修复**：审查发现旧前端响应缺少 `performance_runs` 会导致清理确认总数为 `NaN`，已用空值回退 0 修复；同时兼容旧测试替身缺少新统计字段。
- **验证**：保留清理/API 定向 `24 passed`，完整非集成后端 `2226 passed`，3 个受影响测试文件独立运行 `3 passed, 0 failed`，前端全量 `66 files / 265 tests passed`，`vue-tsc --noEmit`、生产构建、Ruff 和 `git diff --check` 通过。
- **边界**：本次只补本地运行记录与 MinIO 报告清理闭环；真实 MinIO 生命周期、生产保留周期、跨主机恢复和 Kubernetes 多节点仍需目标环境验收。

### 2026-08-25 P1-E.4 性能多节点分片容量校验记录

- **实现**：多节点运行先按节点数拆分总负载，再按每个节点的 `max_vus`、执行器能力和出口 allowlist 校验；提交 `f9e7c54`。
- **验证**：两台上限 6 的节点承载总 VU 10 时生成 5/5 分片，上限 4 时返回 400 且不创建运行；性能 API `72 passed`、服务/Worker `24 passed`、完整后端非集成 `2231 passed`，Ruff/diff-check 通过。脱敏证据见 [`performance-shard-capacity-2026-08-25.json`](evidence/performance-shard-capacity-2026-08-25.json)。
- **边界**：本次完成本地分片容量校验，不替代真实 Kubernetes 多节点调度、生产 Prometheus/MinIO 生命周期和跨主机恢复验收。
- **q19 部署**：已按 `ca79937` 重建并启动，迁移 `20260824_0065 (head)`、Backend 健康 `200`、Prometheus 4 targets、Celery 2 节点在线；重启后 Backend 日志无 enum/Traceback/ERROR。证据见 [`q19-performance-shard-deployment-2026-08-25.json`](evidence/q19-performance-shard-deployment-2026-08-25.json)。

### 2026-08-25 P1-F 本地发布收口记录

- **统一索引**：新增 [`docs/release-status-2026-08-25.md`](release-status-2026-08-25.md)，集中维护能力域、证据链接、外部阻塞原因、复验顺序和发布禁止事项。
- **文档同步**：能力矩阵、Q18 最新状态、Q18 实施日志、`Task.md`、`MEMORY.md` 和 Release-Readiness Runbook 均指向同一状态索引，明确区分本地质量证据与真实环境证据。
- **审查回归**：新增发布状态文档契约，锁定 ADB `offline`、通知 `local_link_only`、外部缺陷平台和生产性能门禁不能被误标为通过；文档差异检查通过。
- **当前结论**：P1-F 仅完成本地发布收口，仍不能无条件发布；Android 真机、真实通知供应商、外部缺陷平台、生产多节点/MinIO 和跨主机恢复需取得目标环境证据后再关闭。

## 0.7 2026-08-25 计划登记与当前执行跟踪

本节是当前继续开发的执行入口，覆盖参考导航中的五组模块，并记录环境阻塞与代码修复的先后关系。上一节的 `[E]` 仍表示“本地实现完成、待目标环境验收”，不因本次计划登记自动变更。

| 顺序 | 模块 | 本轮目标 | 最小出口 | 状态 |
| --- | --- | --- | --- | --- |
| 0 | 运行基础与账号初始化 | 修复管理员改名后启动时按重复邮箱插入导致 Backend 重启循环；补幂等回归并重建 q19 | Backend 健康、迁移 head、登录与基础读接口可用 | `[~]` 已定位根因，待代码修复与部署复验 |
| 1 | APP 自动化工作台 | 使用当前有效账号确认真实 APK/package name，完成低代码最小步骤、录屏、专项任务和事件/日志/报告回传 | 单设备真实运行可追踪，失败不创建脏运行 | `[~]` ADB/Worker 前置通过，账号和 APK 仍阻塞 |
| 2 | API/UI 工作台 | 在 Android 闭环后复跑认证、环境/认证复用、浏览器矩阵、文件传输、报告导出和 Web 低代码 | 当前提交 SHA 下 API/Web smoke 与 q19 日志无新增错误 | `[E]` 本地和历史 q19 证据已有，需用当前账号复核 |
| 3 | 通知与外部缺陷平台 | 使用临时目标完成真实投递、创建/去重/状态同步并清理凭据 | 供应商回执、外部 Issue 状态和错误脱敏可复核 | `[E]` 本地适配完成，待目标与凭据 |
| 4 | 性能与发布收口 | 验证生产多节点、Prometheus/MinIO 生命周期、跨主机恢复，汇总能力矩阵和回滚边界 | 所有未关闭门禁有证据、负责人和复验命令 | `[~]` 本地闭环完成，生产证据待补 |

### 本轮执行规则

1. 先完成顺序 0 的代码修复与 q19 恢复，再继续 APP 真机门禁；不在 Backend 重启循环期间创建运行记录。
2. 每个模块严格执行“实现/调整 → 定向测试 → 代码审查 → 修复 → 文档与记忆同步 → Conventional Commit 推送”。
3. Karing 必须以设备包管理返回的真实 `package_name` 为准；未安装、未授权、无 Worker 或无当前账号时保持阻塞，不用显示名、心跳或跳过项替代执行证据。
4. 任何密码、Token、MinIO 密钥和临时环境文件只通过进程环境或受控部署注入，不写入仓库、日志或证据 JSON。

### 2026-08-25 P0-0 管理员初始化幂等修复（本地交付）

- **根因**：管理员账号改名但继续占用 `FIRST_ADMIN_EMAIL` 时，启动初始化只按用户名查找，误判为不存在并插入重复邮箱，导致 q19 Backend 启动失败；并发启动也可能出现同类唯一键竞争。
- **修复**：`backend/app/main.py::_init_admin` 现在按用户名或邮箱查找，已有身份不重复创建；提交遇到并发 `IntegrityError` 时回滚并复查，确认另一实例已创建后才继续，否则保留异常。不会自动覆盖已有账号密码、角色或邮箱。
- **回归与审查**：新增 `backend/tests/services/test_admin_bootstrap.py`，覆盖改名保留邮箱和首次创建；定向测试 `2 passed`，后端非集成全量 `2262 passed`，Ruff、格式检查和 `git diff --check` 通过。
- **真实验收**：q19 已按 `65eef50` 更新 Backend，迁移 `20260824_0065 (head)`，Backend `running/healthy`，登录、依赖 readiness、Web/Android Worker registry 和 2 台设备扫描通过；未创建 Android 运行记录。脱敏证据见 [`q19-admin-bootstrap-2026-08-25.json`](evidence/q19-admin-bootstrap-2026-08-25.json)。
- **剩余边界**：Android 仍需确认/安装真实 APK 并取得包名，才能继续低代码、录屏、专项任务和事件/日志/报告执行；远端现场 Compose 端口映射等未提交配置已保留。

### 2026-08-25 P0-B.3 单设备真实执行与大对象上传修复

- **真实设备执行**：使用已安装且包管理确认存在的 `com.microsoft.emmx`，在 `172.16.102.214:5555` 完成一次临时 Android 低代码运行，启动应用/等待/截图 3/3 通过；开启录屏和设备产物采集后，3/3 步骤截图、设备信息、logcat 和录屏均成功。临时项目、用例和运行均已清理；这不代表用户指定的 Karing 已安装。
- **真实问题**：上传约 262 MB APK 时，q19 MinIO 客户端将读超时复用 5 秒连接超时，multipart 分片超时并返回 HTTP 500；Backend 日志确认 `minio` read timeout，临时项目已自动清理。
- **代码修复**：新增 `MINIO_READ_TIMEOUT_SECONDS`（默认 60 秒、范围 1～3600），MinIO 连接池调整为最多 10 个并发连接且阻塞等待；启动配置 UI、`.env.example`、Windows/启动文档和中英文说明同步补齐。定向 `19 passed`，前端配置页 `6 passed`，后端非集成全量 `2262 passed`，`vue-tsc`、生产构建、Ruff 和 `git diff --check` 通过。
- **当前出口**：代码已完成本地审查，待以最新提交重建 q19 并重试同一真实 APK 上传，确认自动解析包名、对象绑定和清理；Karing 仍需用户安装或提供 APK 后单独验收。

## 0.8 2026-08-25 导航分组开发跟踪同步

参考导航的五组结构已进一步拆成 N0～N9 可追踪阶段，完整台账和当前执行游标见 [`development-plan-2026-08-25.md`](development-plan-2026-08-25.md) 的“参考导航学习版执行台账”。本次同步只统一计划和状态口径，不改变代码完成状态：

- 当前先处理 N2 APP 自动化的 Karing 真实包名与单设备闭环；没有真实包时保持阻塞。
- N0/N3 Windows 导航、API/Web 和 Playwright 复核不等待 Android，可并行推进。
- N1 的 GraphQL、WebSocket、流式 gRPC 和完整报告仍是待环境验收项；已有执行器代码不视为真实协议通过。
- N4～N9 的生产性能、通知、外部缺陷平台和发布收口依赖目标环境，继续保留 `[E]`/`[~]` 状态。

后续每个阶段固定执行“实现/调整 → 测试 → 代码审查 → 修复 → 文档与记忆同步 → Conventional Commit 推送”，并在 `Task.md`、当前开发计划和 `MEMORY.md` 保持同一状态。

### 0.8.1 N1 协议空步骤执行边界

GraphQL、WebSocket 和 gRPC 派发前现在要求 `config.steps` 为非空数组；缺失、空数组或 `null` 会直接结束为 `error`，避免零步骤运行被误记为通过。API 仍保留旧配置的默认主请求兼容行为。定向回归 `94 passed`、后端非集成全量 `2279 passed`，代码审查无问题；真实协议目标和完整报告仍待后续环境证据。

### 0.8.2 N1 协议配置保存校验

创建/更新协议用例时已在 API 层校验 GraphQL 的 endpoint/query、WebSocket 的 url/messages 以及 gRPC 的 target/proto/service/method；无效请求返回 `422`，快照创建在校验之后，避免保存后才在 Worker 失败。用例管理回归 `35 passed`、后端非集成全量 `2282 passed`；真实协议目标和完整报告仍未关闭。

### 0.8.3 N1 报告中心按用例类型统计

报告概览 API 已按当前用户可见项目聚合已完成运行的用例类型，返回总运行、通过、失败、异常和通过率；报告中心以卡片、进度条和空态展示这些统计，并提供中英文文案。报告定向 `5 passed`、前端报告页 `3 passed`、后端非集成全量 `2282 passed`，`vue-tsc`、生产构建、Ruff 和差异检查通过，代码审查未发现问题。该项只完成本地报告能力，不关闭真实 GraphQL/WebSocket/流式 gRPC 和完整报告环境验收。

### 0.8.4 N0/N3 Windows smoke 当前账号边界

Windows smoke 现在默认只接受当前账号 `ATP_USERNAME/ATP_PASSWORD`，不再自动回退或混用 `FIRST_ADMIN_*`；只有显式 `-UseBootstrapCredentials` 才读取全新数据库的初始化账号。缺少当前账号时脚本会失败并给出操作提示，不会把 bootstrap 账号当作当前登录成功。脚本契约回归和操作手册已同步；真实完整 smoke 仍需使用当前有效账号复验。

### 0.8.5 N1 报告对比同用例选择边界

报告中心默认从最近完成运行中选择同一用例的基线和当前记录；切换任一选择器时自动对齐另一侧的同用例记录，前端调用前也检查用例 ID，避免用户先看到可用按钮、提交后才收到后端拒绝。报告页回归覆盖混合用例顺序和切换场景，真实报告环境验收边界不变。

## 0.9 2026-08-25 当前开发计划登记

本节是参考导航的当前执行口径，覆盖并行关系和阶段出口。五组导航保持为：工作台（首页、我的待办、项目中心、任务中心）、测试能力（接口、APP、UI、性能、AI）、测试资产（用例、计划、缺陷、报告、评审）、智能中枢（Hermes、需求与用例、知识）和系统（远程工具箱、配置中心）。旧设备、APK、专项任务、Mock、数据集、Web/API 资产和治理页面继续保留 URL 兼容，但从所属工作台或配置中心进入。

| 阶段 | 当前状态 | 近期目标 | 验收出口 |
| --- | --- | --- | --- |
| N0 工作台 | `[E]` | 真实账号复核项目上下文、深链、角色和任务事件 | 刷新/窄屏/权限不丢上下文，任务失败和操作可追踪 |
| N1 接口测试 | `[E]` | 维护受控协议和报告证据，生产协议服务独立跟踪 | 创建、审批、执行、断言/提取、报告、清理均有脱敏证据 |
| N2 APP 自动化 | `[x]` | Karing 单设备专项/事件/报告闭环已通过；后续按 N4 外部真实性能环境推进 | 包名确认后低代码、录屏、专项、日志/报告和清理通过 |
| N3 UI 自动化 | `[E]` | 维护多浏览器录制/回放和失败证据 | Chromium/Firefox/WebKit 均可复核定位失败 |
| N4 性能测试 | `[E]` | Worker/目标服务采样、Kubernetes 多节点/副本/Worker 资源预检、保留清理和跨端点 MinIO 复制/恢复 smoke 已完成；继续做真实目标验收 | 多节点容量、取消、采样、报告、生命周期和恢复可复核 |
| N5-N8 | `[E]` | 复核真实模型、项目数据、角色、外部平台和目标部署 | 来源、权限、脱敏、审计、回滚边界成立 |
| N9 发布收口 | `[~]` | 汇总 SHA、测试、环境证据、手册和阻塞 | 每个未关闭门禁都有负责人、原因和复验命令 |

### 0.9.1 执行顺序和交付规则

1. N2 Karing 单设备性能、流畅度专项和总体验收已完成；N1 的生产协议环境仍独立跟踪，当前进入 N4 真实性能环境门禁。
2. N1 完成后推进 N4 可验证部分，再按真实目标条件推进通知、外部缺陷平台和 N5-N8 环境复核。
3. 最后进入 N9 发布收口；任何 `[E]` 或 `[-]` 项都必须保留边界，不能按本地 mock、跳过项、Worker 心跳或页面截图关闭。
4. 每个模块都执行“实现/调整 → 定向测试 → 受影响全量门禁 → 独立代码审查 → 修复 → 文档与记忆同步 → Conventional Commit 推送”。

## 0.10 2026-08-25 N1 受控协议目标交付

N1 的真实协议目标已从“执行器存在”推进到 q19 网络闭环：

- `acceptance-target` 新增 GraphQL POST、WebSocket 握手/文本回显，并继续提供四种 gRPC RPC 形态。
- GraphQL run `19`、WebSocket run `20`、gRPC Server Streaming run `23`、Client Streaming run `24`、Bidi Streaming run `25` 全部通过；临时项目清理后同名项目匹配数为 `0`。
- 目标/部署契约和协议执行器回归 `90 passed`，后端非集成全量 `2288 passed`，Ruff、格式检查、差异检查和独立代码审查通过；代码提交 `5b07a3e` 已推送。
- 证据见 [`docs/evidence/api-protocol-targets-2026-08-25.json`](evidence/api-protocol-targets-2026-08-25.json)。本项不关闭完整报告导出/详情治理、生产协议服务或外部环境门禁，下一步为 N1 完整报告闭环。

## 0.11 2026-08-25 N4 Kubernetes 性能容量预检

- `scripts/performance-environment-smoke.py` 新增可选 Kubernetes 预检参数：`--min-ready-nodes`、`--min-worker-replicas`、`--require-worker-resources`。
- 本地脚本回归 `29 passed`，Ruff、格式检查、差异检查和独立代码审查通过；默认不改变既有单 Deployment/Pod smoke。
- 当前主机只有 Docker Compose，真实 Kubernetes 多节点、生产 Prometheus/MinIO 生命周期和跨主机恢复继续保持待验收；下一步是取得可控 Kubernetes/Prometheus/MinIO 目标并运行 Runbook 命令。

## 0.12 2026-08-25 N4 跨端点 MinIO 恢复验收入口

- 新增 `scripts/minio-dr-acceptance.py` 与 `make minio-dr-acceptance`，验证独立 source/target MinIO 的对象复制、回读、恢复回源、SHA-256 和临时对象清理。
- 默认审计两端生命周期规则，可用 `--require-lifecycle-rule PREFIX=DAYS` 强制精确规则门禁；凭据仅从进程环境读取并从报告排除，同一端点会拒绝。
- 本地脚本回归 `3 passed`，质量门禁一致性/灾备文档回归 `17 passed`，Ruff、格式检查、差异检查和独立代码审查通过；当前没有独立灾备端点，真实恢复仍待环境证据。

## 0.13 2026-08-25 N2 Karing 稳定性/Monkey 单设备闭环

- q19 受控依赖与 Windows Android Worker 已完成配对；设备 `153 / 172.16.102.91:5555`、包名 `com.nebula.karing`，Worker `android-win-HPS` 在线并消费 `mobile_special` 队列。
- 稳定性运行 `6` 和固定随机种子 `20260825` 的回放运行 `7` 均完成；运行 `7` 产生 78 条事件且序号无重复，包含 Monkey 日志/动作、Crash、logcat、screenshot 和操作时间线。
- JSON 报告导出和产物短期 URL 均返回 200；临时项目 `50` 删除 204，删除后查询 404。脱敏证据见 [`evidence/android-karing-special-task-2026-08-25.json`](evidence/android-karing-special-task-2026-08-25.json)。
- 真实运行先暴露 `AsyncSession` 并发提交，独立审查再发现调度/执行器多记录器序号重复；现已用写锁串行化同一记录器并由调度层共享记录器。定向 `82 passed`、后端非集成全量 `2306 passed`，Ruff、格式和差异检查通过。
- **状态**：稳定性/Monkey 子模块已完成；性能与流畅度在 0.14 补齐后，N2 单设备总体验收关闭。

## 0.14 2026-08-25 N2 Karing 性能与流畅度专项真实闭环

- 同一 Windows Android Worker、设备 `153 / 172.16.102.91:5555` 和包名 `com.nebula.karing` 上，性能运行 `10` 完成 CPU/内存/电量/温度/FPS/卡顿采样，流畅度运行 `13` 完成滑动/点击两个阶段并采集 FPS/jank。
- 两个运行事件序列均无重复，JSON 报告导出均 HTTP 200；临时项目 `51` 删除 204，删除后查询 404。脱敏证据见 [`evidence/android-karing-performance-fluency-2026-08-25.json`](evidence/android-karing-performance-fluency-2026-08-25.json)。
- 真实复验发现并修复 Android 14 包级 `dumpsys meminfo` 标题-only 的 VmRSS 兜底，以及 `gfxinfo` `Total frames`/等号 `HISTOGRAM` 解析和 GPU 直方图排除；相关定向回归 `60 passed`，Ruff 通过。
- **状态**：N2 Karing 单设备专项、事件/报告下载和临时清理闭环完成；N4 真实性能环境仍缺 Kubernetes、Prometheus 和独立 MinIO，保持待环境验收。
