# Copy outside the repository, set mode 0600, and fill every value before use.
# Generate each password/key independently (for example: openssl rand -hex 32).
HERMES_EVAL_POSTGRES_PASSWORD=
HERMES_EVAL_REDIS_PASSWORD=
HERMES_EVAL_MINIO_ROOT_USER=
HERMES_EVAL_MINIO_ROOT_PASSWORD=
HERMES_EVAL_APP_SECRET_KEY=
HERMES_EVAL_ADMIN_USERNAME=
HERMES_EVAL_ADMIN_EMAIL=
HERMES_EVAL_ADMIN_PASSWORD=
# Non-secret Git revision of the six overlaid runtime files.
HERMES_EVAL_REVISION=
