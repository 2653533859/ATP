apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "atp.fullname" . }}-config
  labels:
    {{- include "atp.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
