# 2026-08-07 工作日志

## Q16-10 分布式压测节点与资源隔离

- 新增 `PerformanceNode` 模型、`performance_nodes` 迁移 `20260529_0043_add_performance_nodes.py`，并为 PerformanceTest/PerformanceRun 增加可选节点外键。
- performance worker 支持显式节点 ID 注册与心跳；手动触发、定时调度和 worker 启动前均检查节点在线状态、峰值 VUs、活动并发和 egress allowlist，指定节点任务进入对应 Celery 队列。
- 性能中心增加节点状态/容量/最近心跳卡片，以及手动运行和定时执行的节点选择；Helm dedicated worker 增加节点配置和可选 Kubernetes Egress NetworkPolicy。
- 定向回归：后端节点/API/迁移/worker/调度 `72 passed`，前端 type-check 通过，Helm values schema JSON 校验通过；下一步为 Q16-11 Locust/gRPC、数据集参数化和复杂用户行为编排。

## Q16-11 数据集参数化与用户行为编排（已完成）

- `PerformanceTest` 可绑定项目测试数据集；迁移 `20260529_0044_add_performance_dataset_binding.py` 为每次 `PerformanceRun` 固定数据集版本。
- worker 通过内部 `ATP_DATASET_JSON` 注入固定版本 rows，生成脚本按 `__ITER` 轮转数据并解析 `{{FIELD_NAME}}`；数据集中的目标 URL也参与 allowlist 校验。
- Performance Center 已增加数据集选择和多步骤 HTTP 行为编辑器，步骤支持请求方法、URL、Headers、Params、Body、状态检查和 think time。
- Q16-11 已完成；统一执行器和 Locust 接入转入 Q17-01，gRPC 接入转入 Q17-02。

## Q16-09 系统资源指标关联

- 新增 `PerformanceMetricSample` / `performance_metric_samples`，迁移为 `20260529_0042_add_performance_metric_samples.py`，Alembic head 推进到 `20260529_0042`，并按 `run_id + captured_at` 建立查询索引。
- performance worker 在 k6 子进程运行期间定时采集 CPU/内存、PostgreSQL 连接与缓存命中率、Redis 客户端/内存/操作数、MinIO 可达性/探测耗时与周期性对象盘点，样本通过 `run_id` 关联时间线。
- `GET /api/v1/performance/runs/{run_id}/metrics` 提供采样查询；详情抽屉新增指标选择和时间线；Prometheus 新增 `atp_performance_resource{node,metric}` gauge。探测失败只记录样本错误，不改变 k6 结果，`PERFORMANCE_METRICS_ENABLED` 可关闭。
- 最终验证：后端非集成 `1514 passed`，前端 `36 files / 142 tests passed`，Ruff、mypy、type-check 和生产构建通过；下一步为 Q16-10 分布式压测节点与资源隔离。

## Q16-06 性能压测执行控制

- 完成 `pending → running → cancelling → cancelled` 状态链；新增编辑者权限的停止接口。
- 使用 Redis DB2 取消标记，worker 在 k6 运行期间轮询并优雅终止子进程；stdout/stderr 写入临时日志文件，避免 PIPE 缓冲阻塞。
- `PerformanceRunOut` 提供按 duration/stages 估算的 `progress_percent`；前端压测中心每 2 秒刷新活动 run，展示进度、状态和停止确认。
- 文档已同步到 `Task.md` 与 `docs/performance-testing-thin-slice.md`。
- 验证：后端定向测试 52 passed，随后完整非集成回归 1507 passed；前端 36 files / 142 tests passed，Ruff、mypy、type-check 与生产构建通过。

## Q16-07 结果与门禁可读性

- 新增 `/performance/runs/{run_id}/export/json` 与 `/export/csv`，JSON/CSV 均只导出脱敏后的运行快照、摘要、阈值明细和门禁统计。
- 前端详情抽屉增加阈值门禁摘要、通过/失败计数和 JSON/CSV 下载；新增 `performanceReport` 工具及 2 个单元测试。
- 相关后端测试 54 passed，前端报告工具测试 2 passed；下一步为 Q16-08 基线比较、定时执行和 CI 阈值门禁。

## Q16-08 基线、定时执行与 CI 门禁

- `PerformanceTest` 新增 `baseline_run_id`、按时区 Cron 调度配置、Environment/options、`next_run_at` 和 `last_scheduled_run_at`；迁移为 `20260529_0041_add_performance_baseline_schedule.py`，并明确了两条外部 FK 名称。
- 新增基线设置/清除、基线对比和运行门禁接口；基线只接受同一压测定义的成功运行，比较 RPS/P95/P99/错误率并区分改善、回归、无变化和数据不足。
- Celery Beat 新增 `check_performance_schedules`，每分钟处理到期定义；使用行锁和活动 run 检查，避免多 Beat 或长压测造成同一压测重叠运行；压测任务仍路由到 `performance` 队列。
- Webhook 支持 `target_type=performance_test` 和 API Key 门禁轮询；新增跨平台 `scripts/performance-gate.py`，门禁通过返回 0、阈值失败/取消返回 1、未配置返回 2、请求失败或超时返回 3。
- 前端性能中心增加“设为基线”、基线回归详情、定时执行配置及已设基线/已定时标记；新增对应中英文文案。
- 最终验证：完整非集成后端 `1507 passed`，前端 `36 files / 142 tests passed`，Ruff、mypy、type-check 与生产构建通过；目标下一步为 Q16-09 系统资源指标关联。
## Q17-01/Q17-02：统一性能执行器、Locust 与 gRPC

- 新增 `performance_executor` 能力矩阵、`performance_process` 公共 subprocess 生命周期和 `performance_locust` 执行器；k6 通过 dispatch 保持兼容，Locust 使用 `locust==2.32.10` 与 aggregate CSV 结果适配。
- 新增 `/api/v1/performance/executors`，性能中心支持选择 k6/Locust/gRPC；Locust 上传 `.py`，支持 users、spawn_rate、run_time、host、tags、环境变量、数据集、节点能力/出口校验和统一取消/资源采样。
- gRPC 已支持 `.proto` 上传、动态 descriptor、Unary/Server Streaming/Client Streaming/Bidi Streaming 并发调用、TLS/metadata 安全校验、环境变量占位符、取消和统一摘要；worker 通过 `PERFORMANCE_EXECUTORS` 控制启用集合，默认含 gRPC。
- 完整非集成后端 `1578 passed`，独立扫描 `210/210`，覆盖率 `83.78%`（门禁 82），前端 `36 files / 143 tests passed`，mypy `90` 个源文件通过，迁移 head 为 `20260529_0044`；本地真实 gRPC server 联调和隔离验收目标回归已覆盖，剩余 Linux/Docker 外部环境实际启动与压测证据。
- 已补齐 Q17-03 外部验收基础设施：`scripts/performance-environment-smoke.py` 检查 API/节点/队列/allowlist、目标 DNS/TCP/TLS、Kubernetes rollout/Pod 和 Worker 镜像中的 grpcio/grpcio-tools/Locust/k6，并支持显式 smoke、取消、资源采样和脱敏 JSON 证据；`docs/performance-environment-acceptance.md` 固化 Runbook。新增 ARM64 Docker Compose 隔离栈、`.env.performance-acceptance.example`、可复现 TLS gRPC/HTTP 目标、Locust smoke 脚本和 Worker 公有 CA 挂载配置；Worker Dockerfile 增加 grpc import 构建校验，Helm 专用 Worker 增加 metrics readiness/liveness probe。已只读核验 Linux 主机为 ARM64 Docker/Compose 环境，Q17-04 仍待在该隔离栈上执行真实 smoke、取消、allowlist 和资源采样。

## 2026-08-07 审查修复

- 修复性能调度的共享 `performance` 队列消费关系；专用 Worker 继续监听 `performance.<node>`，并额外监听共享队列以接收 Beat 调度、节点心跳等控制任务。
- 增加 `worker_ready` 启动的自调度节点心跳，数据库短暂故障时仍会重新排队；Webhook 执行器透传、Locust/gRPC 进度估算和定时任务 `next_run_at` 提交问题已修复。
- 相关回归：定向 `93 passed`，性能服务/任务 `46 passed`；Ruff、格式检查和 `git diff --check` 通过。完整 Worker 测试剩余 8 个既有模块桩隔离失败，未归因于本次修复。
